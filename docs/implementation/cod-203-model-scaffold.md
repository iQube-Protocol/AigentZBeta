# COD-203 — Model Sub-tab (Scaffold)

## Objective
Add a Model sub-tab under Studio Experience to render/edit journey model structure.

## Model primitives (initial)
- stages, transitions, triggers, blockers, handoffs, agent roles, surface mappings

## Implementation status
**DONE** — Claude Code implemented the Model sub-tab in AgenticDesignParityPanel.tsx (Sprint 2).
