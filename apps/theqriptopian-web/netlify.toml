[build]
  base = "apps/theqriptopian-web"
  command = "cp package-netlify.json package.json && npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
