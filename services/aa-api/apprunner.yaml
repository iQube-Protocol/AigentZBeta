version: 1.0
runtime: nodejs18

build:
  commands:
    build:
      - npm install
      - npm run build

run:
  command: node dist/index.js
  network:
    port: 8080
  env:
    - name: NODE_ENV
      value: "production"
