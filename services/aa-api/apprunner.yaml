version: 1.0
runtime: nodejs20

build:
  commands:
    pre-build:
      - echo "Pre-build phase"
    build:
      - npm ci
      - npm run build

run:
  command: npm run start
  network:
    port: 8080
  env:
    - name: NODE_ENV
      value: "production"
