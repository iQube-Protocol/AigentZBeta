# CFS-015 — Operation Chrysalis 2.0: Constitutional Computing & Constitutional Sovereignty

**Chrysalis Foundation Specification · v1 · Status: Implementation Ready · Ratified 2026-07-06 (operator + agent co-authored)**
Constitutional anchor: `codexes/packs/polity-core/constitutional-records/invariant-intelligence.md`
**This is the canonical implementation PRD for Operation Chrysalis 2.0.** Priority: Critical.
*(Amended 2026-07-06 per operator + co-agent review of the Phase-1A delivery: capability-pipeline rename, Consequence Engineering elevation, the Constitutional Improvement Loop and principle, quality-authority expansion of the Integration Agent, Phase 1A/1B split, the Constitutional Glossary, and the recursive preamble below.)*

---

## Preamble — the recursive implementation principle

**Operation Chrysalis 2.0 SHALL be implemented according to the same constitutional principles that it seeks to establish within the Human Agency System. The program itself is the first constitutional capability built by the platform.**

The implementation is the first proof that the constitutional operating model is not just a theory — it is the method by which the system evolves itself. Concretely: the program's build order obeys Constitutional Sequencing (Appendix B), its enhancements are gated by the Chrysalis Contract, its changes carry receipts and feed the flywheel, and its own success is judged by the Coherence discipline it installs. Chrysalis isn't just building Constitutional Computing; it's demonstrating it.

---

## Executive summary

Operation Chrysalis 2.0 transforms the Human Agency System from an AI-enabled platform into the world's first **constitutional computing** and **constitutionally sovereign** computing environment, by:

- operationalizing Invariant Intelligence across every reasoning surface and every rendering surface;
- instituting normative constitutional order across the complete operating model;
- internalizing the complete innovation lifecycle within Aigent Z and the AgentiQ harness;
- transforming external AI models into interchangeable constitutional inference providers;
- establishing provider sovereignty and sovereign survivability.

The result is a constitutionally complete Human Agency System capable of reasoning, developing, deploying, operating and continuously improving within its own constitutional framework.

## Mission

Create the first Human Agency System capable of **constitutional reasoning, constitutional computing, constitutional development, constitutional operations, constitutional learning, and constitutional sovereignty** — without requiring external AI operating environments.

## Constitutional Maturity Model

| Level | Name | Meaning |
|---|---|---|
| 1 | AI Enabled | Current state |
| 2 | Invariant Intelligent | Constitutional computing |
| 3 | Constitutionally Governed | Development and operations |
| 4 | Constitutionally Sovereign | Provider independent |
| 5 | Constitutionally Complete | A self-governing Human Agency System |

## Program structure

Three implementation strands (with a shared dependency spine — see §Sequencing), plus a Constitutional Integration Agent. A separate **Constitutional Research Program (CRP-001)** informs future evolution but does not block implementation.

---

## Strand One — Constitutional Computing

**Mission:** operationalize Invariant Intelligence across the complete Human Agency System. Every reasoning surface, every rendering surface, every deployment, every constitutional decision becomes invariant intelligent.

**Scope:** Aigent Z · AgentiQ · Registry · Studio · Founder Office · Experience Guides · VentureQubes · PortfolioQubes · iQubes · Capsules · Pills · Tabs · Cards · Runtime · Deployment · Validation · Learning.

**Constitutional Reasoning Services:**

| Service | Artifact |
|---|---|
| Intent Service | IntentQube |
| Context Service | ContextPack |
| Capability Service | CapabilityGapReport |
| Consequence Service | ConsequenceCanvas |
| Validation Service | ValidationReport |
| Receipt Service | DevelopmentReceipt |

**Consequence Engineering — the constitutional reasoning discipline.** Consequence Engineering becomes the constitutional reasoning discipline through which intended capability is transformed into predictable constitutional outcomes. Not just another service: it is the discipline the reasoning services collectively execute (CFS-006a is its operating model; Law XV grounds it compositionally).

**Canonical Ontology Service** — ensures constitutional concepts always resolve against the invariant ontology before reasoning. Purpose: reduce context drift, maintain canonical terminology, guarantee constitutional consistency. Resolution sources include the terminology canon (`docs/platform-ontology.md`) and the **Constitutional Glossary** (`constitutional-glossary.md`, same directory) — the program's canonical vocabulary lives outside implementation and is resolved at runtime.

**Registry Intelligence** — capability discovery, reuse, ranking, recommendation, version selection.

**Model Router** — invariant-aware routing; each reasoning stage independently routable (intent, context, capability, risk, value, price, consequence, validation).

**Phase One:** operationalize Invariant Intelligence across reasoning and rendering. Claude Code remains implementation provider.
**Phase Two:** operationalize constitutional deployment. Deployment becomes native; deployment ownership transfers to Aigent Z.
**Definition of done:** every reasoning surface, every rendering surface, every deployment governed through Invariant Intelligence.

## Strand Two — Constitutional Development

**Mission:** transform Aigent Z into the sovereign development environment. AgentiQ becomes the constitutional development harness.

**Constitutional Capability Pipeline (the canonical innovation lifecycle):**
*(Renamed from "Development Pipeline", 2026-07-06: what the pipeline ultimately produces is not code — it produces CAPABILITY. Capability sometimes requires code; it may equally require configuration, registry updates, prompt changes, policy, schemas, knowledge, automation, or documentation. Development is one implementation mechanism. Capability remains the invariant.)*

```
Intent → Context → Capability → Risk → Value → Price → Consequence
      → Implementation → Validation → Receipt → Learning
```

The **Implementation Pack** is the artifact produced immediately before the Implementation stage — the pack generator is ONE constitutional service among the others, not the pipeline itself.

**Phase One — Constitutional Development:** Aigent Z becomes the development interface; Claude Code becomes an implementation provider; Implementation Packs become the canonical hand-off artifact into the Implementation stage.
**Phase Two — Constitutional Orchestration:** AgentiQ orchestrates Claude Opus, GPT, Gemini, Fable, Codex, and open-weight models. Provider-specific harnesses disappear; providers contribute inference only.
**Phase Three — Constitutional Sovereignty:** Aigent Z owns planning, development, deployment, validation, and operational learning. Claude Code is no longer required.

**Stubbed Constitutional Services** (extension points; interfaces defined now, implementation deferred where appropriate): RiskQube · ValueQube · PriceQube · Registry Router · Agent Router · Consequence Router · Continuous Consequence Integration · Capability Optimisation · Standing Intelligence · Portfolio Intelligence.

## Strand Three — Constitutional Operations

**Mission:** operationalize the Human Agency System as a living constitutional operating environment.

**Scope:** Polity Passport · Standing · Founder Office · Studio · Registry · Operation Leap · VentureQubes · PortfolioQubes · Experience Guides · runtime telemetry · portfolio management · constitutional governance · continuous operational learning.

**Responsibilities:** operational orchestration, portfolio management, capability deployment, standing evolution, registry interaction, operational telemetry, continuous improvement.

---

## Constitutional Principles

1. **Invariant Intelligence** — every reasoning surface and every rendering surface SHALL be governed by Invariant Intelligence.
2. **Constitutional Development** — all capability development SHALL occur through Aigent Z.
3. **Provider Sovereignty** — no external AI provider SHALL become constitutionally indispensable.
4. **Sovereign Survivability** — the Human Agency System SHALL remain operational in the absence of any frontier AI provider. A sovereign fallback capability SHALL exist using open-weight models. Operational quality may degrade; constitutional operation shall not.
5. **Canonical Ontology** — canonical ontology SHALL take precedence over conversational inference. Invariant Intelligence SHALL resolve names, entities, primitives, protocols, and relationships before reasoning.
6. **Constitutional Learning** — every interaction SHALL improve the constitutional capability of the Human Agency System.
7. **Constitutional Improvement** — every constitutional cycle SHALL increase the capability of the Human Agency System. Subtly and importantly distinct from Learning: **learning is remembering; improvement is becoming better.** Learning accumulates validated knowledge; improvement is the measured increase in what the system can do with it.

## The Constitutional Improvement Loop

The Human Agency System is becoming self-improving. Alongside the Capability Pipeline (which creates capability) runs the improvement loop (which compounds it):

```
Capability → Operation → Observation → Receipt → Learning → Improved Capability
```

Note the terminus: **improved capability, not improved code.** Every deployed capability operates, its operation is observed, observations are receipted (DVN-anchorable), receipts feed learning (the flywheel: Standing on consequence, Reach on citation), and learning re-enters the pipeline as improved capability. This loop is the mechanism by which the Constitutional Improvement principle (7) is satisfied — and the loop's order is constitutional data (pinned in `types/constitutional.ts`).

## Third Constitutional Agent — the Constitutional Quality Authority

**Mission:** constitutional integration — grown into the program's quality authority: **the equivalent of a compiler** for constitutional change. Nothing integrates that it cannot verify.

**Responsibilities:** architectural coherence · ontology governance · invariant validation · capability benchmarking · continuity benchmarking · improvement benchmarking · constitutional regression testing — plus the original integration duties (compare outputs, maintain constitutional consistency, consolidate implementation, prepare integration PRs). *(Implementation note: a VERIFICATION role — canaries, drift benchmarks, continuity tests, integration review — not a third development stream.)*

## Success criteria

**Continuity:** constitutional reasoning, historical architectural understanding, VentureQube/PortfolioQube/Experience Guide consistency, Alethean behavioural continuity, and strategic conversations all continue without reconstruction.
**Improvement (measured):** reasoning quality, implementation quality, development velocity, Time-to-Value, reduction in context drift — from repository/runtime/registry/deployment visibility, Studio skill invocation, direct code visibility, Invariant Intelligence, and Consequence Engineering.
**Sovereignty:** the Human Agency System can reason, plan, develop, deploy, operate, and learn using only Aigent Z, AgentiQ, and sovereign inference providers.

## Final acceptance test — the Chrysalis Test

The Human Agency System successfully develops strategy, performs constitutional reasoning, engineers consequences, generates Implementation Packs, develops capabilities, deploys capabilities, validates outcomes, generates receipts, learns operationally, and improves future constitutional reasoning — **without leaving the Human Agency System**. External AI models function solely as interchangeable constitutional inference providers.

## The Chrysalis Contract

Every enhancement to the Human Agency System must improve at least one constitutional capability: Constitutional Computing, Constitutional Development, Constitutional Operations, or Constitutional Learning. If it improves none of these, it does not belong in Operation Chrysalis. *(This is Law X — every evolution must strengthen the invariant ontology — restated at program level; recorded as equivalent in CFS-009.)*

## Constitutional North Star

The Human Agency System SHALL become the primary environment in which its own future evolution is conceived, reasoned about, consequence-engineered, designed, implemented, validated, deployed, operated and continuously improved.

---

## Appendix A — Organ map (implementation reality, verified 2026-07-06)

Chrysalis 2.0 is largely a **promotion of existing production organs into named constitutional services**, plus an adoption sweep, plus four genuinely new builds.

| PRD service | Existing organ | State |
|---|---|---|
| Intent Service / IntentQube | `services/iqube/intentQube.ts` (`createIntentQube`, packed into `nbe_plans.rationale`, carries `parentIntentId`) | production |
| Context Service / ContextPack | `services/invariants/grounding.ts` (`initializeKnowledge` canon-version-cached, `buildInvariantSlice`) | production |
| Consequence Service | `services/consequence/` (`runConsequencePipeline`/`executeApproved` already sequence Curation→Compression→Risk/Value→Forecast→Disposition→flywheel) | production core |
| Validation Service | `services/coherence/` (3 real validators, 2 honest stubs) + `scripts/verify-spine.mjs` | partial |
| Receipt Service | `createActivityReceipt` + DVN pipeline + `invariants_used` | production |
| Learning | flywheel: `recordConsequence` (Standing) + `citeInvariants` (Reach) | production |
| Capability Service | GAP: registry trust-scoring and the work-order gateway (`services/capabilities/`) are disjoint — no discovery/ranking bridge | new bridge |
| Canonical Ontology Service | terminology canon + invariant ontology exist; no runtime resolver | **NEW** |
| Model Router | provider chain + allowlists + per-AGENT ModelQube catalog exist; per-STAGE routing does not | **NEW (thin)** |
| Implementation Pack | QubeTalk bridge rail exists (`--paths --deploy-ready`, sha-verified apply) | generator **NEW** |
| Deployment | `.amplify-deploy` + `claude/**`→dev auto-merge | CFS-016 v1.0 — **D1 RATIFIED 2026-07-06** (pack-proposed; execution human); D2/D3 unratified |
| RiskQube/ValueQube/PriceQube | `assessRisk`/`assessValue` throwing phase-2 stubs; Capability Composition null | seams exist |

## Appendix B — Sequencing (constitutional)

The three strands share a dependency spine: Strand 2 consumes Strand 1's reasoning services; Strand 3 consumes both. Per the sequencing corollary of Law XV (`inv.constitutional.078`), build order is constitutional data: **contracts single-authored first (one agent), adoption parallelized second (two agents against frozen contracts), verification continuous (the Integration Agent)**. Deployment-ownership transfer is operator-approval-gated per Law XI in every phase of this PRD — deploys are consequence-bearing acts; autonomy is ratified explicitly or not at all.

**Phase split (amended 2026-07-06 — risk reduction):**

- **Phase 1A — Constitutional contracts.** Ontology, interfaces, router. NO adoption.
  *Status: DELIVERED (commit 8ae5decc — CFS-015, CRP-001, types/constitutional.ts, Canonical Ontology Service, Model Router v1, canary suite).*
- **Phase 1B — One complete constitutional cycle.** One reasoning surface + one rendering surface through the full cycle.
  *Status: DELIVERED. Reasoning surface: the main copilot (per-turn ontology resolution, canonical-term prompt guidance, Reach citation). Rendering surface: the invariant video brief path — Context (grounding slice) → Ontology (output-side drift check over the COMPOSED prose, unresolved terms surfaced to the operator) → Composition (CFS-013 laws) → Validation (CFS-014 coherence, pre-existing) → Receipt (`experience_render_validated`, DVN-anchorable, `invariants_used` carried) → Learning (Reach citation on every grounding invariant). Every render now leaves a constitutional trace.*
- **Parallelization gate: OPEN.** Contracts are frozen; the two adoption agents may start.

**Phase 2 — first parallel increment (2026-07-06, two agents + Quality Authority):**

- **Agent A (Strand-1 adoption) — DELIVERED.** The rerank seam (`services/orchestration/nbeLlmRerank.ts`, one seam covering `/api/assistant/brief` + `/api/assistant/move-forward`) gains invariant-slice grounding + ontology resolution + Reach citation on applied passes; the intent route's existing `intent_queued` receipt gains `invariantsUsed`. No receipts added to hot surfaces (Learning flows via Reach); ask-agent confirmed as the pre-existing exemplar and left untouched.
- **Agent B (Strand-2 services) — DELIVERED.** `services/constitutional/implementationPack.ts` (the artifact-before-implementation; LLM-drafted via the model router with an honest deterministic-template fallback), `inferenceProviders.ts` (five provider slots: anthropic/openai frontier, venice open-weight, gemini/codex honest stubs — providers contribute inference only), `capabilityDiscovery.ts` (the registry bridge: capability descriptors matched + ranked by `registry_trust_scores.numeric_score`, honest `evaluated:false` on infrastructure failure), and the admin-gated `/api/constitutional/implementation-pack` route emitting `implementation_pack_generated` receipts (DVN-anchorable) with `invariantsUsed`.
- **Quality Authority verification:** both streams scope-audited (protected files: exactly one added line each — the permitted action-type additions), parse gates green, canaries extended (provider honest-stub + template-fallback tests).
- **Deferred to next increment:** the a2ui/liquid render seam (coherence unwired there; needs its own design pass), Founder Office pack UI, receipts-gated deploy (operator-approval design), Strand-3 ops integrations.

**Phase 2/3 — Strand-3 first slice + seam design (2026-07-06):**

- **Venture blueprint hand-off constitutionally instrumented (Strand-3 entry point):** ontology resolution over the venture's operator-facing text, `invariants_used` on the existing `venture_blueprint_handoff` receipt, consequence preflight folded into the receipt summary (a hand-off whose knowledge footprint reaches a contradiction says so), Reach citation — all enrichment-only.
- **CFS-017 drafted (a2ui/liquid coherence seam)** — DRAFT awaiting ratification: seam at the plan-route chokepoint; observe-first-gate-later (the D1→D2 pattern applied to rendering); honest semantic-only validation at v1. No implementation until ratified.
- Chrysalis Test `reasoning-surfaces-governed` evidence now reports adoption BREADTH (distinct governed action types), not just volume.
- **CFS-017 v1 (observe) RATIFIED + implemented (2026-07-06):** the a2ui surface-plan path carries a `constitutional` block (grounding seed ids — the CFS-007 `invariantSeedIds` semantics finally populated, ontology drift, honest coherence slot, receipted flag); receipts when the caller resolves; renders never blocked. Three implementation amendments recorded in CFS-017 (liquid path is client-only and remains ungoverned pending v1.1; the coherence engine is brief-shaped — plan-generic validator is the named gap; the plan route resolves no persona).

**Phase 2 — second increment (2026-07-06): the dev interface + the sovereignty drill**

- **Capability Pipeline tab (Aigent Z as development interface, v1) — DELIVERED.** Admin-only AgentiQ cartridge tab: capability goal → constitutionally grounded Implementation Pack rendered with full provenance (invariant bindings, resolved terms, nine-mechanism chip, composedBy honesty badge, plans) + byte-identical markdown copy for hand-off to any implementation provider. Stage strip shows live stages vs ratified stubs (risk/value/price render `unevaluated`). Deploy wiring deliberately absent (Law XI).
- **EXP-004 Sovereignty Drill — DELIVERED (runnable; first run operator-paced).** Venice-only by construction (provider pinned in the service; the API accepts no provider parameter): the five EXP-003 constitutional tasks (initialized arm) + one implementation-pack generation. Completion = the sovereignty claim; groundedness/citations/tokens = the degradation report vs the EXP-003 frontier record (reported, never scored). Task failure is recorded as constitutional failure — the drill's core datum. Publishes canonically (EXP-004 added to the results pipeline). The drill is Chrysalis's acceptance test AND the first CRP hand-back (CRP-001).

**Phase 2 — third increment (2026-07-06): Dev Command Center convergence (the Aigent Z cycle joins the constitutional pipeline)**

The Dev Command Center (`app/triad/components/codex/tabs/DevCommandCenterTab.tsx` + `services/devCommandCenter/`) — the earlier Chrysalis Phase-1 MVP — was inventoried as a PARALLEL system: real for its first four stages (LLM `stage_data` proposals → operator approval → capsule cards), but with zero wiring to the constitutional pipeline, no implementation-stage surface at all, and a stage-routing trap. This increment converges it (Extend, Don't Duplicate):

- **The Validate-card trap closed.** The chat route's capsule-override forced the copilot to emit the VIEWED capsule's proposal kind — "validate the build" typed while the Consequence Canvas capsule was open produced another `consequence_canvas` whose narrative read like a validation confirmation, while the Validate card stayed empty. Fix: deterministic operator stage-request detection (`detectRequestedStage`, ordered conservative patterns, validation outranks implementation) now takes precedence over the viewed capsule; the resulting `validation_report` routes to — and auto-opens — the Validation capsule.
- **The implementation stage is a first-class card** (`ImplementationLayout`): the brief (approved proposal or derived), **Generate Implementation Pack** through the real constitutional pipeline (`/api/constitutional/implementation-pack` — invariant bindings, consequence preflight, `implementation_pack_generated` receipt), and **Propose deployment (D1, CFS-016 v1.0)** emitting `deployment_proposed`. The receipts ARE the "development phase initiated" confirmation — execution stays human under D1, stated on the card. A generated pack writes back as the session's implementation brief, closing the previously always-true advance gate (`canAdvance('implementation')` now requires a brief).
- `implementation_brief` proposals route to the new card (was: project-overview); the Implement strip node is clickable; canaries pin the routing map, the detector, and the gate.
- **Honest limits (named, not promised):** the dev-loop session remains in-memory (persistence + per-approval receipts = named follow-on); the embedded execution surface (Claude Code terminal) remains a Phase-2 placeholder — constitutionally gated behind D2, which is time-gated on D1 operating history; the two-tier prompt router remains a stub.

**Regression + same-day fix (2026-07-06): the detector was a suppressor.** The first cut of `detectRequestedStage` ranked bare `implement`/`implementation` above `gaps` and made the detected stage REPLACE the capsule/session stage outright — so gap-analysis requests phrased around implementation ("the gaps in the implementation") were hijacked to the implementation stage, the Gaps card never received a proposal, and the sequence starved downstream (operator-reported same day). Fix: (1) gap analysis outranks implementation, and implementation matches only phrase-level signals ("implementation brief/pack/plan/stage/phase", "start the development", "write the code") — bare mentions no longer hijack; (2) the detected stage is now a HINT, never a suppressor — when it differs from the capsule/session stage, `buildStageInstructionBlock` presents BOTH schemas and the LLM emits the kind the operator's request actually means (the originally-ratified dual-schema design, which the detector-only shortcut had dropped). Regression canaries pin both properties.

**EXP-004 rehearsal arm (2026-07-06, operator-directed — venice credits pending):** the identical drill battery may run on a frontier provider (`REHEARSAL_PROVIDERS = openai | anthropic`; venice deliberately excluded — a venice run IS the sovereign drill) to validate the machinery end-to-end while venice credits are unresolved. Honest semantics enforced at every layer: rehearsal publishes carry `rehearsal: true` and NEVER a `sovereigntyHolds` field; the runner's banner reads "Machinery VALIDATED", never "Sovereignty HOLDS"; the Chrysalis sovereignty criterion reads the latest NON-rehearsal run for pass/fail and reports a rehearsal-only history as `partial` ("machinery validated in rehearsal — the sovereign run remains pending"). ChainGPT was considered (it is a live provider on the codex chat surface — `api.chaingpt.org`, runtime fallback ladder) but is not wired as an experiment adapter: different API shape (streaming-only endpoint), no token-usage numbers for the degradation report, unproven JSON-judge discipline. Canaries pin the rehearsal allowlist and venice/chaingpt exclusions. The sovereign gate semantics are unchanged — this unblocks the machinery, not the claim.

**Amendment (2026-07-06, operator-directed): ChainGPT promoted to experiment adapter; rehearsal chain reordered.** The rehearsal preference chain is now **chaingpt (default) → openai (fallback) → venice (the sovereign run, once credits land)** — `REHEARSAL_PROVIDERS` order is the operator's chain and the runner defaults to the first available. The ChainGPT adapter mirrors the platform's proven chat-surface call byte-for-byte (endpoint, four env-key spellings, `question`/`chatHistory:'off'` payload, `data.bot`-or-raw-text extraction) rather than guessing API capabilities. Honest limits recorded in the adapter: the endpoint accepts no temperature/max_tokens (benchmark temp-0 discipline not enforceable on this provider) and returns no usage tokens (both reported null, never fabricated) — so a chaingpt rehearsal validates machinery and groundedness but not the token-cost dimension of the degradation report. Sovereignty semantics unchanged: rehearsals stay `partial`, only a venice run completes the gate.

**Sovereignty definition refined (2026-07-06, operator): a provider-CLASS property.** Sovereign Survivability is about running on a NON-FRONTIER (open-weight) provider alone — not about venice by name. Venice is the platform's only open-weight adapter today, so it remains the sovereign provider operationally, but the definition now travels with the class: `SOVEREIGN_CLASS = 'open-weight'`, canary-pinned to the constitutional provider inventory's `kind` field. Corollary made explicit in the drill UI: a forced frontier run (openai, selectable in Rehearsal mode with gpt-4o/gpt-4o-mini) tests the FULL capability end-to-end — including the usage-token dimension chaingpt cannot report — and is the right way to validate the machinery completely; it can never satisfy the sovereignty claim, which requires any open-weight provider. When a second open-weight adapter lands, it qualifies for the sovereign run with no definitional change.

**Sovereignty refined again (2026-07-06, operator): a SCALE of operator control, not a boolean.** Sovereignty in itself is the operator's control — the ability to choose, switch, and combine providers free of commercial or platform lock-in. Open weights give maximum control; they are the ceiling, not the definition. Codified as `SOVEREIGNTY_SCALE` in types/constitutional.ts (canary-pinned order): **S0 dependent** (single-provider lock-in) → **S1 interchangeable** (operator choice, no lock-in — the essence) → **S2 substitutable** (validated substitutes complete the constitutional battery) → **S3 open-weight** (maximum). Glossary updated ("Constitutional Sovereignty" redefined as the scale; new resolver-wired "Sovereignty Scale" term; "Sovereign Survivability" = the S3 rung) — verified resolving at runtime. Consequences threaded through the drill: a completed rehearsal now publishes `sovereigntyRung: 's2-substitutable'` (a live S2 datum, upgraded from "merely a machinery drill"), the sovereign run publishes `s3-open-weight`, the Chrysalis sovereignty criterion narrates the rungs (rehearsal = S2 demonstrated, partial; open-weight = S3, pass), and the provider-interchangeability criterion is named as the S1 essence.

**Platform Sovereignty codified as a bundle (2026-07-06, operator — CFS-018).** Sovereignty, like identity, is a bundle of capabilities: model openness (weights + scoring + access), provider choice, commercial independence, infrastructure agency, and infrastructure survivability — assessed per dimension, never collapsed to a boolean. Founding case study: Venice — open-weight models behind a credit wall is a sovereignty infringement despite the open weights (dimensions do not substitute for each other). Codified as substrate: invariants `inv.sovereignty.100`–`107` (new namespace, Appendix A; ingest pending), glossary terms "Platform Sovereignty" + "Platform Sovereignty Experiment Series" (resolver-wired, with CONCEPT_SEEDS attaching the governing invariants), and the **PSE series charter** in CFS-018: PSE-1 = EXP-004 (built), PSE-2 commercial-independence drill, PSE-3 infrastructure survivability, PSE-4 hosting posture map, PSE-5 model-openness audit — named now, designed and ratified individually before spend, per the measurability mandate (an unmeasured sovereignty claim is a claim, not a capability).

**CCRL chartered (2026-07-06, operator PRD — CFS-019).** The deferred research institution begins: the Constitutional Cybernetics Research Laboratory is chartered as the platform's constitutional scientific institution (Phase A: charter + vocabulary + `inv.cybernetics.108`–`111`, the Layer-III invariants). The existing corpus retro-fits as its founding holdings (EXP-001–004 = the four Layer-I experiment families; Foundational Validation + PSE series; CRP-001 programmes). Migration is canonical-surface-first (operator-ratified); the Bitcoin observability statement was operator-corrected and codebase-verified: DVN (ICP) operational, Bitcoin testnet settlement built (Merkle batching + tECDSA signer canisters) with the recurring explorer bug class fixed same day (canonical btcExplorer helper + txid provenance + canary; commit b7f270fd). Phases B–E designed in CFS-019, each ratified before build.

**CCRL Phase B delivered (2026-07-06): the canonical research surface.** The CCRL cartridge is live — Dashboard (live Chrysalis summary, hash-committed results feed, roadmap), Research tabs per constitutional layer, the Experiment Laboratory (EXP-001–004 runners + Results + Report + Chrysalis, admin-gated), Living Knowledge (Invariant Registry + resolver-wired glossary), Publications (the constitutional record), and Programme (CRP-001) — all reusing existing components over content in place (canonical-surface-first). The ccrl pack directory is deferred to Phase D by recorded amendment.

**CCRL Phase C1 delivered (2026-07-06): the research object model, receipted.** Contracts in `types/research.ts` (experiment/series/publication/finding lifecycles order-pinned; the four founding experiments registered with seed-crystal-verified governing invariants); lifecycle state DERIVED from the canonical record, never asserted (published = a hash-committed run exists; replicated = runs on ≥2 distinct providers); operator transitions receipted as `research_lifecycle_transition` (DVN-anchorable — the one-line action-type additions audited) with evidence required and governing invariants carried as invariants_used; Dashboard renders the derived strips. C2 (the ICE research copilot) deliberately split: new proposal kinds get their own pass, per the dev-loop misroute precedent.

**DCIR chartered (2026-07-06, operator spec — CFS-020).** The Dynamic Constitutional Interaction Runtime is chartered as the interaction substrate of Constitutional Computing: a closed bidirectional cognitive-action loop (Conversation → Inference → Action → Observation → Updated Context → …) synchronizing intent, inference, state, artefacts, recommendations, and next actions into one evolving constitutional state — generation is never terminal, it is a state transition. D0 delivered contract-first: charter, `types/dcir.ts` (loop + runtimes order-pinned), `inv.interaction.112`–`118`, resolver-wired glossary terms, canaries. Three constitutional cautions baked into the contract itself: observe-mode-first (the CFS-017 precedent — observation never blocks the surfaces it watches until gating is separately ratified), capsule containment (affordances carry a required capsule scope, never orphan output), and identifier-tier discipline (T0 identifiers inexpressible in the event stream; T2-safe summaries only for anchorable events). Behavioural invariants discovered by observation are proposed patterns, never rules, never auto-canonical — ratification stays with the operator (inv.cybernetics.111). D1+ (event stream + observation seam on one surface, state engine, recommendation/affordance engines, universal adoption) awaiting operator ratification.

**DCIR D1 delivered (2026-07-06): the Observation Runtime's first seam, on the Dev Command Center.** Operator-ratified surface choice: "the Dev Command Center is the most developed feedback loop and the most vertically integrated surface, from the Bitcoin substrate to the metaMe runtime." `services/dcir/eventStream.ts` (session-scoped ring buffer, cap 50; T2-safe label summaries, T0 inexpressible) wires the DCC's existing seams — stage proposals received/approved/dismissed, stage advances, capsule open/close, implementation pack generated, deployment proposed — into an in-session event log, and `copilotGroundContext.recentEvents` feeds the last 12 back into the aigent-z ground truth block as narrate-only observations: the loop's Observation → Updated Context → Inference edge closes for the first time. Strictly observe-mode (CFS-017 pattern): nothing blocks, nothing mutates behavior; no behavioural-invariant mining (D2+), no receipts for UI events, no dynamic affordances (D3). Canaries pin the cap, the contract shape, and the bounded rendering.

**CCRL Phase C2 delivered (2026-07-06): the research copilot, narrate-only by design.** Aigent Z joins the CCRL as a grounded research copilot (CFS-019 §6): a two-pane surface (`CCRLResearchCopilotTab`) where the copilot OBSERVES and NARRATES the live lab state — experiment lifecycles derived from the canonical record, series claims, hash-committed results — degrading honestly when a feed is unavailable. DCIR-conforming from birth: `ccrl-research` is the second instrumented surface after the Dev Command Center D1 reference, via generic surface helpers composed onto `services/dcir/eventStream.ts` (the DCC's typed vocabulary untouched; kinds, tier ceiling, and T0-inexpressibility canary-pinned). Deliberately NO research stage-proposal kinds ship on this surface — the dev-loop misroute regression (recorded above, same day it shipped) is the precedent: new proposal kinds (experiment design, finding drafts) are C2.1, their own increment after usage observation.
