# Differ Scan Package v1.0.0 — Generated (Not Submitted)

**Status:** GENERATED LOCALLY — NOT uploaded, submitted, or transmitted to Differ or any third party
**Date:** 2026-08-24
**Parent:** `2026-08-24_differ-scan-package-v1-financial-services.md` (scope definition), `2026-08-24_aee-differ-phase0-audit-financial-services.md` (audit + vendor-clarification addendum)
**Generator (committed to this repo):** `scripts/build-differ-scan-package.sh`

---

## What happened

Per operator decision (2026-08-24): use a bounded standalone export for the first Differ scan, not repo-level access. `scripts/build-differ-scan-package.sh` was written and run to build that export deterministically from the already-defined file list (`2026-08-24_differ-scan-package-v1-financial-services.md` §2).

The script is committed here so the package is **reproducible**: rerunning it against the same commit produces the same file set and the same per-file SHA-256s. Its output — the actual staged directory and archive — is **not** committed, per this repo's Dense Materials rule (build output/archives are pointers-not-payload; the repo carries the generator, not the generated bytes).

## Package facts

| Field | Value |
|---|---|
| Package name / version | `differ-scan-package` / `v1.0.0` |
| Source commit | `acfa4edbcea49df7870cc2a98ad0f4d638f1ea62` (branch `claude/aee-differ-scan-package-v1`) |
| Included files | **37** (see MANIFEST.md inside the package for the full per-section list) |
| Bounded stubs (dependencies described, not copied) | 26 modules — see `STUBS.md` inside the package |
| Unpacked size | 688 KB |
| Archive | `differ-scan-package-v1.0.0.tar.gz` |
| Archive size | 156,373 bytes |
| **Archive SHA-256** | `8dd990a5e907e9ac5a700d260a12cd5d9e16db5ca088915334a7c86a7f1fe25b` |
| Secret/credential scan | Clean — no `.env`/`.pem`/credential filenames, no secret-shaped strings (`SUPABASE_SERVICE_ROLE`, private-key headers, API-key-shaped literals) found in any included file |

## Declaration carried inside the package (MANIFEST.md, verbatim)

> **Compatibility scan input only. Consequential Runtime execution, authorization, credentials, private data and constitutional authority remain native and are not hosting candidates.**

## Dependency-gap note (things that could make a scan read misleading if not accounted for)

The package's `STUBS.md` documents 26 dependencies represented only by their exported signatures rather than full implementations — most fall out of scope naturally once the giant `moneypenny-horizen/state` route (which alone pulls in ~20 Horizen partner-admission files: registration, PnL verification, Standing, orientation rituals) is itself stubbed rather than copied. Two things worth flagging explicitly for whoever reads the scan result:

1. **`services/financialServices/serviceRequestOrchestrator.ts` is included in full**, but six of its own imports (`services/constitutionalCommerce/*`, the VELA-001 consequence-projection ontology) are stubbed. If the scan's compatibility read depends on seeing HOW consequential execution is actually gated (not just that a boundary exists), the stub-only signatures will underinform it — this is deliberate (native-execution internals are exactly what should never become a hosting candidate), but it is a real limit on what the scan can conclude about Runtime specifically.
2. **`app/api/moneypenny/architect/route.ts` and `.../runtime/route.ts` are included**, but the business logic they call (`moneyPennyArchitect.draftFinancialStructure`, `constitutionalServicePipeline`) is stubbed. The scan can see the route/request/response shape; it cannot see what those functions actually do internally. If Differ's compatibility read needs to reason about response latency/shape under real load, that would need the full implementation — not provided in v1.

Neither of these is a defect in the package; both are the direct, intended consequence of keeping consequential execution un-widened, named here so the scan result is read with that limit in view rather than assumed complete.

## Next step

Operator reviews the package locally (or asks this agent to inspect specific files) before any decision to actually run it through `https://ramp.getdiffer.com/`. No further action has been taken toward submission.
