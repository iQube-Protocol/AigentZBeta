# Index registration note

AEE-XP-001 is intended to be registered in `codexes/packs/agentiq/index.json` as an implementation/build specification after reading the current index SHA and preserving all existing entries.
