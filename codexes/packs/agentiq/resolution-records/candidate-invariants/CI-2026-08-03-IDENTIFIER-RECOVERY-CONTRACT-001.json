{
  "schemaVersion": "candidate-invariant/v1.0",
  "classification": null,
  "ratifiedSource": null,
  "notes": [
    "FLAGGED FOR THE OPERATOR, not decided here: this may be the same shape as CI-2026-08-03-ACTOR-SUBJECT-OWNER-001 — a wallet is the OWNER reference and was substituted for the SUBJECT reference. If the operator judges them one rule, this becomes a fourth occurrence of that candidate rather than a candidate of its own. The operator fixed that candidate's count at three, so it is not merged unilaterally.",
    "`verifiedFailingBeforeFix` is recorded as false: the source doc records the canary being REWRITTEN from the obsolete wallet-keyed assertion, not a fail-before/pass-after run. Recorded honestly rather than assumed."
  ],
  "canaries": [
    {
      "path": "tests/register-ceremony.test.ts",
      "assertion": "The receipt-decode call shape is asserted (`decodeAgentIdFromReceipt` with txHash / expectedOwner / expectedRegistry) AND the old wallet-keyed lookup is asserted GONE from this code path; plus ambiguity refusal and mandatory `ownerOf` verification.",
      "verifiedFailingBeforeFix": false
    }
  ],
  "candidateId": "CI-2026-08-03-IDENTIFIER-RECOVERY-CONTRACT-001",
  "statement": "An identifier may only be recovered from a source whose contract guarantees that identifier; a lookup keyed on a different reference can answer confidently and wrongly.",
  "scope": "local",
  "status": "candidate",
  "derivedFrom": [
    "RES-2026-08-02-HORIZEN-AGENTID-RECOVERY-001"
  ],
  "occurrences": [
    {
      "site": "services/horizen/registrationClient.ts",
      "defect": "`checkAgentRegistrationStatus` recovered a missing agentId via `fetchRegistryAgent(ownerWalletAddress, network)`. ERC-721 gives no reverse (wallet → tokenId) enumeration guarantee — `balanceOf` returns a count, not a list — so a registry read keyed by the owner wallet is a different tool contract than an agentId lookup, with no guarantee the two ever coincide.",
      "resolutionId": "RES-2026-08-02-HORIZEN-AGENTID-RECOVERY-001",
      "evidence": [
        "78747308f",
        "tests/register-ceremony.test.ts"
      ]
    }
  ]
}
