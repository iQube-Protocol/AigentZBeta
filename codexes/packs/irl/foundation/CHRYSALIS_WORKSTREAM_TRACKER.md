# Operation Chrysalis — Workstream Tracker

**Living consolidated plan. Single source of truth for what is done, in flight, and pending across the one Chrysalis 2.0 program.** Update as increments land. Last updated 2026-07-18.

**Program of record: `CFS-022` — Operation Chrysalis 2.0: The Constitutional Operating Environment.** That doc is the *why* and the reframe (COE, not IDE) + the five-pillar architecture + the gap assessment + the P0–P4 roadmap; this tracker is the *what-next*. The program folds IRL / DCIR / Atlas / CDE as workstreams (specs unchanged; roadmap consolidated).

Companion / folded specs (authoritative detail lives there):
- `CFS-022` COE program of record · `CFS-015` Chrysalis PRD (mission expanded by CFS-022) · `CFS-016` deployment
- `CFS-019` IRL charter (Research Lab workstream)
- `CFS-020` DCIR charter (interaction substrate workstream)
- `CFS-021` Constitutional Civic Futurism (representation + Bearing Instrument workstream)

## 0. The dependency graph (CFS-022 §2) — the keystone the program turns on

`Chrysalis 2.0 → Constitutional Runtime → Canonical Invariants (done) → Composable Assets (THE MISSING MIDDLE: Object Model + Asset Registry + Composition) → Atlas Plates → Picture Book.` The Atlas is blocked by the missing runtime, not by artwork. Keystone gaps: **G1 Constitutional Object Model** (contract-first, unblocks all) → **G2 Canonical Asset Registry** → **G3 Composition engine** (compose-not-generate) → G4 Publication unification → G5 Workspace/Factory shell. First leverage milestone: **P2 — compose ONE Atlas Plate end-to-end** (proves the machine; the book becomes routine after).

---

## 1. Workstreams at a glance

| Workstream | What it is | State |
|---|---|---|
| **A. CDE / Dev cockpit infra** | The Constitutional Development Environment surfaces + their reliability | Hardening — hang/flicker/env fixes shipping |
| **B. DCIR** | The interaction substrate (observe → recommend → afford). D0–D2 done; D3 in progress; D4 hook adopted on 5 surfaces (DCC · aigentMe · Studio Composer · IRL research copilot · AssetDetailPanel) | Active |
| **C. Constitutional Atlas / Representation** | CFS-021 representation system + the Bearing Instrument navigation primitive | Active |
| **D. Operator / infra** | Env provisioning, canister health, seed ingests — operator-side actions | Open items tracked below |
| **E. Homecoming — Constitutional Agent Sovereignty** | CFS-023 bridge programme; delegates stood up + Constitutional Presence ladder. Closeout set (2026-07-18): Elythian memory upload + the Constitutional Observer delegate (see backlog #96/#97) | Active — closing out |
| **F. Constitutional primitives (Identity + Artifacts)** | CFS-024 Constitutional Identity Hierarchy (person↔agent binding/assignment) + CFS-025 Artifact Runtime (three consequence tiers; constitutionality earned by promotion) | CFS-024 P0–P1 shipped; CFS-025 Phase 0 shipped, Phase 1/2 ratification-gated |
| **G. Invariant Engine → Resolution + Projection** | CFS-035 — the governing law (every consequential decision is a projection of the constitutional field, not an isolated heuristic); the four-face engine over one Field Snapshot; three levels (Registry / Engine / OS). **2026-07-17: split into two primitives** — **CFS-037 / PRD-IRE-001 the Invariant Resolution Engine** (intent → resolved invariants + constitutional coordinates → Resolved Constitutional Field; the query-planner front-end, resolution precedes reasoning) + CFS-035 reframed as the **Invariant Projection Engine (IPE)** (projects a resolved field). Future PRD family: KRE (reuse-before-create knowledge) / IPE / CFO (coordinate observatory) / CCR (coordinates registry) | Chartered 2026-07-18 (operator × Aletheon × Claude convergence); **Phases 0–4 shipped** — engine seam + 5 Invariant Decision Nodes (discovery · nbe · standing · journey · routing) + Constitutional Observatory (Field view, 5 perspectives) + doctrine canon (CRP-002 §0; `inv.…138–159`) + Evolution loop closed. Deploy + discovery-flip ratification pending (see backlog) |

---

## 2. Shipped this session (2026-07-08 → 07-09)

**A. CDE infra**
- CDE tool viewports hang fix — `withTimeout` on IC/DVN/GitHub/Linear probes; client `personaFetchDeadline` (12s); `getSupabaseAccessToken` bounded (3s, GoTrue-lock hang) → falls through to localStorage.
- CDE pane flicker/stampede fix — non-memoized `onToolUsed` caused an infinite mount-effect loop (→ API stampede → token/key errors). Ref-stabilised each pane + memoised the parent observers.
- CDE route hang guard — `resolvePersonaOrTimeout` (6s) on all six dev-command-center routes → fast honest 503 naming the failing layer instead of a 12s client abort.
- DevTools tabs — Env/Canisters · Telemetry · DVN Pipeline · Escalation, with failure-flag dots.
- Env allowlist — `GITHUB_TOKEN`, `LINEAR_API_KEY`, `GITHUB_REPOSITORY`, `RQH_CANISTER_ID`, `REWARD_HUB_CANISTER_ID` added to `scripts/create-env-production.js` (Amplify console vars only reach the SSR runtime via this allowlist).

**A. House style**
- Corrected `agentiqLiquidGlass` interpretation from the white-hairline residual to the authoritative **slate** house style (hairline `#1E293B`, tint `rgba(15,23,42,0.4)`, no white inset). Codified the canonical surface-styling rule in `CLAUDE.md`.

**B. DCIR**
- D4: migrated the **IRL research copilot** to `useDcirSeam` — the last hand-wired seam. All four originally-named surfaces (DCC, aigentMe, Studio Composer, IRL) now on the one hook.
- D3: exposed the ratified **affordance engine by declaration** through `useDcirSeam` (`affordances` field); migrated the DCC to the single home. Observe-mode, suggest-only, capsule-contained.

**C. Constitutional Atlas**
- **Bearing Instrument v1.0** — `atlas` variant added to `components/representation/BearingInstrument.tsx` (role-driven; compact default + canaries untouched). Canonical anatomy: circular bezel + tick ring, Order/Reasoning/Action octants, Invariant Intelligence / Consequence Engineering poles, needle, six artefact glyphs, HDG/GS/ALT/TRK windows, standing ring + REGISTER marker, five layer labels, parchment (CCF) presentation. Canaries pin the new pure helpers + zero-literal gate.

---

## 3. In flight

| # | Item | Workstream | Notes |
|---|---|---|---|
| 32 | Bearing Instrument v1.0 atlas variant | C | Built + preview sent + **mounted in-app** (`components/composer/IRLDashboardTab.tsx`). Remaining follow-ons: fidelity polish toward the Breitling render (metal bezel, raised "ears" on the artefacts gauge), literal aviation numerics if wanted. |

---

## 4. Pending / backlog (ratification-gated where noted)

| # | Item | Workstream | Gate |
|---|---|---|---|
| 33 | **DCIR D3 — generic-vocabulary affordance derivations.** `generateAffordances` fires only on DCC dev-loop vocabulary; add derivations from generic event kinds (DocumentCreated / decisions / WorkflowAdvanced / NavigationOccurred) so affordances appear on aigentMe / Studio / IRL, capsule-contained, deduped vs DCC. Then render on ≥1 non-DCC surface. | B | Ratify before build (CFS-020 §87) |
| 34 | **DCIR D4 frontier** — `useDcirSeam` now adopted on 5 surfaces (the four + AssetDetailPanel). Remaining frontier: Aigent Z (most-integrated), then Marketa, then per-cartridge. | B | Each its own ratification |
| — | **Bearing Instrument** — mount in-app + fidelity polish (see #32). Use as the invariant nav primitive across Constitutional Atlas surfaces. | C | — |
| 35 | **Verify CDE env vars live** post-deploy — `env-check` shows GITHUB_TOKEN / LINEAR_API_KEY / REWARD_HUB_CANISTER_ID `[✓]`. Requires operator to set `REWARD_HUB_CANISTER_ID=lvo2w-jqaaa-aaaas-qc2wa-cai` on the dev Amplify app. | D | Operator |
| 36 | **DVN canister status** — `dfx canister --network ic status sp5ye-2qaaa-aaaao-qkqla-cai` (cycles / PEM / id-mismatch). **Seed ingests** — `inv.representation.121-129` + `inv.interaction.112-118` are `proposed` in the seed crystal; run the ingest to make them live (CFS docs are canon meanwhile). | D | Operator |
| — | DCIR D2 remaining snapshot fields harden against their organs (intent/goals/policies/persona/standing/…); Feedback Coordinator full design (component #12). | B | Ratify before build |
| — | Two Supabase migrations if not yet run: `dev_loop_sessions`, research objects table. | D | Operator |
| 47 | **CFS-025 Artifact Runtime (AR).** One runtime shepherding artifacts across three consequence tiers — disposable / operational / constitutional; constitutionality EARNED by promotion. **Ratified 2026-07-10.** Phase 0 (contract + canary 19/19), Phase 1 (skeleton: classify + tier router + profiles), Phase 2 (IRL `research` pilot: `artifact_published` DVN type + `/api/artifact/produce-research` + receipt-reconciliation adapter) all shipped. **Follow-up:** the 15-call-site ReceiptQube migration + retirement (incremental); anchoring registry `asset.published` is an opt-in (one-line flip) awaiting operator call. | F | Follow-ups only |
| 50 | **CFS-026 Constitutional Publishing Factory (CDS + CPS).** Three products: CCS (standards) · CDS (design system) · CPS (= the Artifact Runtime on document profiles). CDS v0.1 encoded as data + wired into native document production; delegate→AR convergence produces CCS-000 as the first factory output (seeded in the Homecoming workshop). **CFS-026 + CFS-027 RATIFIED 2026-07-12.** Plumbing shipped (plates as canonical assets · numbering registry · renderer map) + R1 plate renderer + artifact persistence (migration run). **R2/R3 (publication shell + derived PDF): SKIPPED per operator (2026-07-12)** — build on future request. IRL-0001 production + live drives: TBD (operator). | F | R2/R3 on request; drives TBD |
| 51 | **CP-008 candidate — the Constitutional Orientation compass (Bearing Instrument family).** Operator direction 2026-07-11: the compass in the v0.9 plate sheet is canonical imagery belonging to the Bearing Instrument (Canonical Asset 001). Decide at CFS-027 ratification: enters the plate set as CP-008, or stays the Bearing Instrument's own canonical rendering. Also from the v0.9 sheet: Hybrid-Intelligence-as-Venn (folded into CP-002 in v1.0 — likely stays derived). | F | Decide at CFS-027 ratification |
| 49 | **CFS-025 follow-up — ReceiptQube retirement (verify-in-place).** The receipt trail is unified (adapter double-write) and the raw-personaId T0 leak in `registry_receipts` is closed. Remaining: move the registry-ingestion READ path onto the unified `activity_receipts` table, then retire `registry_receipts` + drop the double-write. Touches the ingestion UI + money-adjacent paths — MUST be done against a running dev instance driving the ingestion UI, not speculatively. Also: the one-line opt-in to anchor registry `asset.published` (awaiting operator call). | F | Verify-in-place; operator-gated |
| 52 | **Consequential-artifacts plan — ALL 5 INCREMENTS SHIPPED 2026-07-12** (operator: "auto execute this whole plan"). (1) **Standing loop**: non-disposable production accrues standing to the PRODUCING delegate via canonical `accrueStanding` (delegated lane; CVS operational 2 / constitutional 5) with the trust-band ceiling (L2≥20 · L3≥50 · L4≥75 · L5≥100) surfaced on the Homecoming Test. (2) **Studio publish seam**: `POST /api/composition/publish` closes the composeArtifact PUBLISH SEAM — propose⇒operational record, publish⇒ONE anchored `artifact_published` receipt folded into provenance as a projection (engine untouched) + constitutional record. (3) **Business/AgentMe tiering**: `classifyBusinessArtifact` wired additively into create-artifact (gmail draft⇒disposable never persisted; doc/sheet/slides/calendar⇒operational records; constitutional structurally impossible on that path). (4) **Software pilot (D1-safe)**: `POST /api/artifact/produce-software` — the Implementation Pack IS the artifact body; operational tier, no receipt, executes/pushes/deploys NOTHING; `proposeDeployment` returns the documented pointer to the existing D1 route. (5) **Promotion UX**: promote route + Homecoming workshop chips lift a record operational→constitutional (receipt BEFORE lift; constitutional CVS accrues to the producing delegate). **Grant-gate decision RESOLVED 2026-07-12: option (c) — both, + admin accelerator.** Delegation route now dual-gated: grantor reputation (existing) AND, for L3+, the delegate's own server-resolved earned ceiling (`delegateStandingAllowsBand`; L1/L2 stay grantor-only as the bootstrap floor). Admins accelerate a delegate's standing via receipted `POST /api/homecoming/agent/standing` (+CVS 1–50 through the canonical accrual, `approval_granted` receipt first — the passportless-Polity-sponsorship shape) + a `+10` chip in the Homecoming workshop. | F | Done |
| 53 | **Linear lifecycle mirror — SHIPPED 2026-07-12 (operator-ratified).** One-way, best-effort projection of the production cycle into Linear (`services/linear/lifecycleMirror.ts`): issues keyed by a deterministic T2-safe `[AR:<hash12>]` marker on (delegate, profile, brief); phases map to portable Linear state TYPES (intent→backlog · pack→unstarted · produced→started · D1 proposed→started · published→completed); transition comments carry receipt ids/hash prefixes/standing. Wired into implementation-pack, deployment-proposal, produce-software, homecoming produce, promote, and composition publish. Soft-fails until the operator sets a write-scoped `LINEAR_API_KEY` + `LINEAR_TEAM_KEY` in Amplify (both allowlisted; the mirror lists available team keys in its warn when the team is unset). Linear is a MIRROR, never a source of truth — receipts stay canonical; only T2-safe content crosses the external boundary. | F | Operator: set env vars |
| 54 | **CFS-028 Capability Graph & Production Routing — RATIFIED + v1 SHIPPED 2026-07-12.** Producer (harness/model/delegate) × capability (AR profile) graph: contract types, seed graph (harnesses + delegates hand-seeded with stated reasons; model producers derived from the ModelQube registry), pure ranking core with the constitutional standing bar (`CONSTITUTIONAL_MIN_CEILING` = L3) applied to delegates, `GET /api/capability/producers` (admin-gated), routing strip on the Homecoming produce panel. `deployment-execution` edges seeded DORMANT until CFS-016 D2 ratification (operator direction: start with the Claude Code session as executor; server-side + other execution agents join as Producers earning the climb by standing). **Later, separately ratified:** receipt-learned fitness, real cost ingestion, live execution edges. | F | Done (follow-ons §4.5) |
| 55 | **Implementation Pack executed: video-article skill — SHIPPED 2026-07-13** (first live CFS-015 pack → harness → validation → receipts cycle). One skill, two corresponding artifacts: a 24-second video (2 × 12s segments — SkillVideoPlayer's provider cap makes 24s exactly two stitched clips) and a companion article drafted via `callSovereign('draft')` from the SAME invariant-grounded brief, so correspondence is structural. Composes the existing EXP-002 organs (`buildVideoInvariantBrief`, coherence engine, fixed player) — only the article half and the skill seam are new. Receipt plan closed: `artifact_created` on article (plan) + on video (the player's new additive `onCompleted` seam → mode:'video-complete'). Surface: metaMe IRL → "Video + Article" tab. **Pack-quality finding recorded**: the pack ran empty-canon (no invariant bindings, no areas-to-touch, no what-exists inventory) — honest about it (risk 59, low_confidence_knowledge) but all reconnaissance fell to the harness; feed the pack generator the CFS-028 capability graph + seed ingests (#36) so future packs draft areas-to-touch. **Follow-on**: converge the skill's article drafter with the EXISTING `skill:article_generation` (`/api/composer/article-draft` — route-inlined; extract to a shared service both consume). Also fix catalog drift: the catalog's `invokeEndpoint` says `/api/composer/article/generate` but the real route is `/api/composer/article-draft`. | F | Operator drive |
| 56 | **Workflow-gap fix: session findings now travel into pack generation — SHIPPED 2026-07-13.** Root cause (operator screenshots): the DCC's Context Pack (reuse signals), Gap Analysis (existing/missing, 60% reuse, Article Generation `use_directly`), and Consequence Canvas ("never duplicate capabilities") were populated but `ImplementationLayout.generate()` sent only `{ goal, domains }` — the inventory died in the session and the pack shipped "areas to touch: not drafted". Fix: `SessionFindings` threads Layout → route → `generateImplementationPack`; folds into the draft prompt with the anti-duplication mandate; seeds `areasToTouch` deterministically from session-named paths when the draft leaves it empty; echoes onto the pack (`pack.sessionFindings`) and renders as a "What exists vs what is needed" section in the pack markdown. **Regenerated pack verified by operator 2026-07-13**: areas-to-touch drafted from session paths, validation/receipt plans reorganised around the MISSING capabilities. **Delta executed same day**: `services/content/alignmentService.ts` (heuristic per-segment article⇄brief coverage; wired into the video-article plan + article receipt summary) + `services/rendering/optimization.ts` (structural render planning grounded in the real 12s/4-segment/2-3-clips-per-stitch constraints; 24s = one stitch pass) — both surfaced as chips on the Video + Article runner. Remaining pack weakness: empty-canon invariant bindings (seed ingests, #36). | F | Done |
| 57 | **CFS-029 Constitutional Capability Pipeline completion — ADOPTED + core SHIPPED 2026-07-13** (operator's "three things happening" review). (1) **CapabilityEvidence** re-homed from transport object to PERSISTED constitutional primitive (`services/constitutional/capabilityEvidence.ts` leaf + `capability_evidence` table, migration 20260713010000): pack generation persists fresh evidence and reads it back when none supplied — evidence outlives sessions. (2) **Constitutional Decision stage** before every pack draft: nine mechanisms + 'none' (capability exists — compose, build nothing); LLM via router + pure heuristic floor; recorded on the pack with rationale + alternatives; 'none' empties areasToTouch and mandates composition. (3) Renames: pack renders say Constitutional Validation / Constitutional Receipt; Capability Evidence section shows the persisted id. (4) **CS-001 case study** recorded: "Duplicate Capability as Constitutional Drift." `empty-canon` reclassified Constitutional Blindness (resolved same day via seed advancement + namespace widening). **§7 RATIFIED + SHIPPED 2026-07-13**: DCC Decision capsule (first-class ICE stage + /api/constitutional/decision; decision travels verbatim into pack generation) · evidence persistence receipted + ConstitutionalObject builder · freshness policy (7-day window, stale grounds loudly) · CS-001 registered as IRL-0002 (reserved). **Slug migration completed same day** (operator direction): `irl-cartridge` / `irl-dashboard` / `irl-research-copilot` with permanent legacy aliases + defensive DB migration. **Operator: run migrations 20260713010000 + 20260713020000 + 20260713030000.** | F | Migrations + drives |
| 58 | **Dogfood Run 001 — article convergence — SHIPPED 2026-07-13** (first end-to-end run of the completed pipeline on a real goal; D1 operating-history cycle #1). Evidence → Decision (`code`: extract + converge; `none` rejected — the drift was real) → execution: `services/composer/articleDraftService.ts` is the ONE drafting home (pure builders moved verbatim; the article call site migrated OpenAI-direct → `callSovereign`, one #45 site down); the route is a thin wrapper (response shape unchanged); `videoArticleSkill.draftArticleFromBrief` converged onto `draftCompanionMarkdown` (mandate + prompt byte-identical — correspondence contract untouched); catalog `invokeEndpoint` drift fixed. 8/8 seam drill + canary. Run record: `agentiq/updates/2026-07-13_dogfood-run-001-article-convergence.md`. **Canonized 2026-07-13 as CCE-006 — Constitutional Capability Convergence** (first Constitutional Computing Experiment; run renamed CVR-001; experiment taxonomy CCE/CIE/CAE/COE amended into CFS-019; Capability Integrity into CFS-028 §6; evidence split + Constitutional Ratification into CFS-029). **Operator: replay the cycle in the DCC for the receipted twin + propose the D1 deployment (cycle #1 evidence for D2, which explicitly recognizes CCE-006).** | F | Done |
| 59 | **Lab copilot AR/CPS observer wiring + stale-handoff fix — SHIPPED 2026-07-13** (operator report: copilot still handing off to EXP-004 after its run). Three fixes: (1) `/api/research/overview` now carries an `artifactProduction` block (recent AR records + Publication Register, T2-safe) and the IRL Research Copilot folds it into its ground context + renders an "Artifact production (observed)" panel — the observer informs the copilot of production state in its space. (2) Active-experiment default = the EARLIEST experiment below `published` (needs attention), never an arbitrary park. (3) Run-stage hand-off card now states honestly: an unpublished run does not exist constitutionally — publish from the runner tab and the hand-off clears. **Root cause of the stale EXP-004 hand-off is DATA-side: that run was never published to the canonical results store — operator: open the EXP-004 runner and hit Publish.** Canonical rule added to CLAUDE.md: all artifact-producing surfaces use AR/CPS + observer awareness. **Adoption inventory (each its own ratification): Studio Composer (largest gap — productions bypass AR records), Video+Article runner (receipts, no AR records), IRL experiment runners (canonical results, no AR bridge).** AR-native already: Homecoming workshop · Studio publish seam · business artifacts · delegate produce · software pilot. | F | Operator publish + adoption follow-ons |
| 60 | **DCC chain fixes from the CVR-002-candidate pack drive — SHIPPED 2026-07-13** (operator report: no auto-pass to Validate; returning to Implement loses the pack). Five fixes: (1) the generated pack is now SESSION state (`generatedPack` on DevLoopState, persisted with dev_loop_sessions) — the Implement capsule rehydrates it, never forces a regeneration; (2) **auto-pass to Validate**: pack generation satisfies the implementation gate and the loop advances + the Validate capsule mounts, no second click; (3) the Decide strip node, `getStageLabel` entry, and stage-index canary — all missed in the morning's stage insertion (esbuild parse gates don't type-check exhaustive Records; would have failed the Amplify tsc build) — completed; (4) **grounding domain-filter fallback**: a live canon with zero bindings meant the session's relatedCartridges domain filter matched no invariant rows — an empty filtered slice now retries unfiltered (fixes "_none resolved_" + empty preflight on session-driven packs); (5) canary walk updated. Regenerated pack verified by operator: real canon stamp, Constitutional Decision block (mechanism `automation`, three alternatives weighed), persisted evidence id `0bf56eb9`. | F | Done |
| 61 | **Live-drive round-2 fixes: observer reaches the operator's screen — SHIPPED 2026-07-14** (operator screenshots: copilot STILL stuck on EXP-004 after publish; lab hand-off button dead; runner blind to prior runs; DCC advances invisible; Implement still demands regeneration). Five root-caused fixes: (1) **derived-floor clamp in the copilot merge** — the loopExperiments working-object override let a stale session object drag a PUBLISHED experiment back below `published`; working lifecycles now clamp UP to the overview-derived floor (the same rule `overviewWithPersistedLifecycle` applies server-side). (2) **`codex:navigate-tab` listener inside the embed** — the only listener lived in the parent viewer window while tab components dispatch inside the embed IFRAME; CodexPanelDynamic (the embed's tab owner) now listens with the same visible-tab guard + legacy-slug resolution. (3) **EXP-004 runner observer-aware** — mounts with a "Canonical record (observed · AR/CPS)" panel from `/api/experiments/results` (published runs visible on return; refreshes after publish). Persisting UNpublished drill drafts stays a named follow-on. (4) **DCC advances are visible + complete**: `handleAdvanceStage` walks the gate-checked linear prefix PAST the viewed capsule's stage and mounts the destination capsule (gate failure mounts the BLOCKING stage's capsule instead of a dead button); `onPackGenerated` folds the pack's constitutional decision into the session and fast-forwards any parked session to Constitutional Validation (old guard only fired from stage === implementation); `ImplementationLayout` reads `session.generatedPack` EVERY render (initial-only useState missed hydration). Drill 11/11 incl. fork single-step (no runaway walk). (5) **remaining renamed slugs aliased** — `ccrl-experiment-lab/-charter/-protocols/-invariant-field/-invariant-registry` added to `LEGACY_TAB_SLUGS`. | F | Operator re-drive |
| 62 | **CVR-002 (candidate) — Studio Composer adopts AR/CPS + observer awareness — SHIPPED 2026-07-14** (operator: "Yes do this too"; executed evidence-first per the CFS-029 stage order by a delegated agent, verified by the session). `services/composer/studioArtifactTiering.ts` (pure classifier mirroring `businessArtifactTiering`: 9 named production kinds, unknown→disposable fail-safe, constitutional structurally impossible, whitelist-copy T2 body) + additive tiering on the four production routes (image generate: completed→operational multimedia; article draft: real-input-aware operational documentation; video invoke: submission-aware — submitted video doesn't exist yet, disposable; video stitch: the single-shot durable completion seam) + `GET /api/composer/artifact-production` observer route (spine-gated; same projection as research/overview). **Deliberate `none` decisions recorded**: video status poll (no idempotent upsert — would mint a record per poll) and bundle deploy (ExperienceQubes already canonical — double-record = CS-001 class). Canary + 20/20 drills (throwing-store stub proves disposable never touches storage; operational soft-fails). Run record: `agentiq/updates/2026-07-14_cvr-002-studio-composer-ar-adoption.md`. Honest limits: poll-completed videos unrecorded (needs idempotent seam), stitch re-runs can duplicate, Studio UI fold of the observer route deferred (13k-line surface). | F | Done (follow-ons named) |
| 63 | **aigentMe NBE observer-awareness + cockpit Active Work dedupe — SHIPPED 2026-07-14** (operator report on metame.live: after completing the daily brief's recommended actions — venture progress report, review block, claim standing — Move Forward re-recommended the SAME actions as the NBE, and the venture cockpit's Active Work showed the same actions repeated as IN_PROGRESS). The recommender now OBSERVES what its space produced (the AR/CPS rule applied to the NBE engine): (1) `selectNbeCandidates`/`selectTopNbeForCartridge` take `recentlyCompletedNames` — candidates whose label matches a recently completed intent name (exact-normalized or containment ≥10 chars, so LLM-contextualised titles still match their catalog parent) drop out of the pool, with a never-empty guard (all-done → unfiltered set). (2) `buildBrief` + `buildMoveForward` read the persona's completed intents (72h window, tolerant) and BOTH feed the selection filter AND fold a "Recently completed (observed) — do NOT re-recommend" block FIRST into the rerank's liveContext (survives the 600-char cap). (3) Venture cockpit: Active Work dedupes to the most recent row per (intent name, cartridge) — repeated acts no longer stack sibling rows — and Recommended filters BOTH sources (activation catalog + NBE fallback) by completed work with the same guard. Drill 10/10. Follow-on named: intents don't store their source NBE id (matching is name-based); packing the NBE id into the IntentQube extras would make suppression exact. **Delta same day (operator: "the cancelled completed clean up will be very helpful — pls add")**: `sweepSupersededIntents` — intent hygiene that cancels (1) SUPERSEDED live duplicates (an in_progress/awaiting_approval intent with a newer sibling of the same normalized name+cartridge — the newest row is the current state of that action) and (2) STALE live singletons older than 14 days (abandoned work that can never complete itself). Completed/failed/cancelled history is never touched; capped at 20 mutations per run so a backlog drains across a few loads; awaited in `/api/assistant/venture-progress` BEFORE the build so the cockpit's Active Work reflects the swept record in the same response. Drill 8/8 against the real bundled sweep (superseded twins, stale singleton, newest-kept, fresh-kept, history untouched, per-cartridge grouping). | F | Operator re-drive |
| 64 | **IRL-010 Constitutional Runtime Technical Specification v0.1 + IRL-010A Claims Traceability Matrix — AUTHORED 2026-07-14** (first external technical diligence on the IRL papers; Claude Code as *implementation witness* per operator/advisor direction — "inspect the current implementation, do not speculate"). IRL-010: formal definitions quoted from the ACTUAL stored representations (`InvariantRecord` verbatim, Law XII standing/reach, 12 edge types, composition laws, weakest-link manifests, T-tiers, consequence classes) · 14-subsystem architecture inventory with per-subsystem file anchors and ✅/◐/○ status · 10 algorithms as source-traceable pseudocode · experimental methods with the previously unpublished specifics (EXP-003 baseline = cold arm, 5 tasks/18-invariant collection, provider-reported tokens; EXP-001 judge protocol 4×15 + 0/1/2 contradiction scale) and honest n=1/internal-adjudication scoping · **falsification framework** (general statement + H0–H5 prediction/control/disconfirmation table; the reviewer's "prompt scaffold" alternative adopted as the H4 control) · implementation-status table naming what does NOT exist. IRL-010A: every IRL-000/001/002 claim classified I/D/P/F with anchors, ⚑ flags where paper phrasing outruns evidence (each with the precise supportable statement), and an honest register of unevidenced claims. Registered in the IRL foundation collection; run record in agentiq/updates. **Named priority controls (runnable on existing infra): EXP-001 ungrounded arm, EXP-004 scaffold-only arm, EXP-003 multi-run variance.** Possible extraction: Part VI → standalone IRL-003 for the paper series. **Delta 2026-07-14 (operator correction + ratification)**: (1) "identity continuity — planned" corrected to **personhood continuity — implemented** (Law XIII: identity is an optional derivative of individualization); IRL-010 §2.11 added as the formal definition with witnessed anchors. (2) The Law XIII **binding law seeded as `inv.constitutional.130`** (law-typed: "Personhood shall remain continuously individualizable without identity exposure…") alongside constituents 063–067; namespace-widening migration applied by operator; full 142-invariant crystal ingested; **family CANONIZED by operator ratification** (6/6 `canonical`/`document_verified`, verified in the operational store) — the first canonical-status invariants in the substrate, now grounding live reasoning via the canonical slice. DVN anchor of the canonization rides tracker #36. **Delta 2026-07-14b (discoverability → standing)**: operator insight — "it's hard for invariants to achieve standing if they can't be discovered." Confirmed mechanically (proposed invariants are invisible to `buildInvariantSlice` → never cited → never earn reach/standing), and the discovery↔composition gate distinction clarified (grounding injects statements; only `validateComposition` applies composition laws — so the 28 provisional-namespace invariants can be discoverable while their laws stay provisional). Recommendation refined to **advance all-but-`inv.constitutional.127`** (the one self-labeled hypothesis-standing entry). **Grounding-breadth measurement instrument shipped**: `scripts/benchmark-rediscovery.mjs --broad` adds a third arm grounded on the live domain-scoped crystal slice (mirrors buildInvariantSlice ranking) and reports the narrow→broad BREADTH DELTA — the extra rediscovery savings from a richer discoverable crystal. Syntax + summary-math verified on a fixture. Run record scaffold: `exp-003-rediscovery-savings/breadth-arm.md` (preconditions, run cmd, interpretation contract incl. the honest "zero-delta is equally publishable" branch, honest limits). Operator: advance the crystal, then run `--broad` for the measured number → candidate datum for the Austin evidence package + IRL-010A row 1.19. **Run 001 EXECUTED 2026-07-14 (operator, openai/gpt-4o-mini, temp 0, 5 tasks, crystal freshly advanced → 135 validated + 6 canonical, standing≈0):** grounding presence VALIDATED + replicated (narrow cut tokens −12.6%, grounded 65.8%→96.2%, contradictions 3→0; a same-day repeat gave −11.9%, 8→0). Raw breadth **NEGATIVE**: broad slice (24) used +14.7% MORE tokens than the curated 18 for +0.7pt grounded (already at ceiling). Honest reading: the freshly-advanced crystal ranks by confidence, not earned standing, so raw breadth injects less-relevant invariants — **curation beats un-earned breadth at t=0; the flywheel (does an EARNED crystal beat curation?) is the decisive Run-002 re-run after standing accrues.** Instrument honesty fix shipped: the summary's interpretation line was hardcoded-optimistic; now sign-aware (POSITIVE/NEGATIVE/NEUTRAL + grounded-share-fell guard), so the instrument reports the finding it measured. Result recorded in breadth-arm.md Run 001. A null/negative that demonstrates the lab publishes non-confirmatory results — strong for the evidence package. **Delta 2026-07-14c (advisor review of IRL-010 + results, operator-directed "address IRL-010, add IRL-011 as follow-on")**: IRL-010 sharpened — (1) **formal definition** added to §2.1 (`I = (S,E,C,V,P,L)` + Collection/Compose/Retrieve/Evaluate operator signatures) directly answering Austin's "what exactly is an invariant," with the full algebra forward-referenced to IRL-011; (2) **iQube proposition** formalized in §2.4 (`iQube = Intent + CuratedInvariants + ConstitutionalProvenance + Standing`; Retrieve = intent-guided constitutional curation, not RAG) — the operator's keystone insight "we're curating invariants, not information"; (3) **Law XIII chain refined** in §2.11 to `Personhood → Individualization → Standing → Authority → Consequence` with **Identity an off-chain projection** (explains anonymity+accountability coexisting); (4) EXP-003 breadth **reframed curation-dominates-accumulation** + `E=f(G,B,M)` (grounding/breadth/merit orthogonal), making Run 002 a true standing-vs-confidence-retrieval A/B. **IRL-011 (The Computational Model of Invariant Intelligence) opened as OUTLINE/commissioning note** (task #50) — 7-part structure + all formal seeds captured verbatim; proposes for operator ratification IRL Principle 004 (faithful instruments), the "organization before accumulation" emerging law, and EXP-003 → Reasoning Economics rename. Publication streams confirmed: Research (why) / Technical Spec (how) / Experimental Records (evidence). **Delta 2026-07-14d (all three ratified + IRL-011 AUTHORED)**: operator ratified (1) **IRL Principle 004** — instruments must faithfully report outcomes — seeded canonical `inv.epistemology.131` (normative commitment); (2) **emerging law** "knowledge quality scales through organization before accumulation" — seeded `inv.epistemology.132` as a GOVERNING PRINCIPLE with empirical confirmation PENDING EXP-003 Run 002 (deliberately NOT document-verified, preserving falsifiability); (3) **EXP-003 renamed Reasoning Economics** (machine id EXP-003 unchanged). Both invariants in seed (144 total) + appendix-a (same-commit Maintenance rule) — operator: ingest + canonize 131 (132 stays governing-hypothesis until Run 002). **IRL-011 (The Computational Model of Invariant Intelligence) authored v0.1** (outline superseded): 10 sections — formal definitions (invariant tuple, standing/reach axiom, iQube composite, weakest-link), computational primitives (typed operators + pre/post), composition algebra (legality, merit order, breadth-not-free proposition), runtime semantics (small-step intent→consequence + the curation flywheel fixpoint), coherence calculus, complexity + reasoning economics (`E=f(G,B,M)`, minimum-sufficient-substrate `K*`), relationship to statistical inference (vs RAG/ICL/constrained decoding), **model-level falsification (F1–F5, each a runnable experiment)**, ratified principles carried, honest limits. Every claim labelled implemented/partial/proposed-formalism. IRL-010A gains rows 1.21 (iQube curation proposition) + 1.22 (Principle 004). **Delta 2026-07-14e (v0.2 + shareability + flywheel redesign)**: (1) **IRL-011 v0.2** — added inference rules (§3.6), algebraic structure (§3.5: 5 properties + proof sketches — confidence min-semilattice, breadth-cost monotonicity, idempotence-up-to-subsumption, partial commutativity, normative force-preservation), `K*` elevated to centrepiece (§6.3: open problems P1–P5, Bellman/PageRank framing), convergence conjecture C1 + **versioned-crystal + receipted-standing + H1/H2 separation + living-constitution remark** (§4.2–4.5), §7A economics seam (capital/circulation/production/price/market-clearing/anti-inflation), falsifiers extended F6/F7. (2) **Shareability** — all three IRL reports de-personalized (no Austin/reviewer name; substance kept as "external review"); the v0.2 update record + draft note genericized + renamed (reviewer correspondence stays in the operator's direct channel). (3) **"EXP-003 Run 002" DEFERRED + redesigned → EXP-006 Constitutional Knowledge Evolution** (operator + advisor): a standing A/B under uniform standing (=f(timesValidated)=0) is a null-by-construction (recorded per Principle 004); earning standing IS the flywheel → the institute's first LONGITUDINAL series. Charter authored: 006A Accrual/H1 (receipted) → freeze `Crystal v1` → 006B Retrieval/H2/P2/F7 → 006C Φ-convergence/C1/F6 → 006D Transfer/P3 → 006E Version comparison; design invariants freeze-before-measure + receipted standing + independent H1/H2. Numbering: 005 held by Provider Choice → 006 (operator confirms designation). Deferral deliberate — stronger experiment > faster weaker answer. | F | Operator: ingest 131/132; EXP-006 = flagship longitudinal build when resumed |
| 65 | **CVR-003 (candidate) — runArtifact invariant grounding + cited-invariant recording — SHIPPED 2026-07-14** (operator: 'treat "runArtifact invariant grounding + cited-invariant recording" as its own CVR-style increment — lets build it now', after the AR/CPS audit found the composition engine grounded but the runtime blind). The Artifact Runtime is now invariant-aware: (a) `resolveGrounding` — composition-supplied `grounded.invariantIds` win (compose-never-fork), else a live profile-scoped `buildInvariantSlice` (limit 8; research→constitutional/epistemology/reasoning, software→constitutional/engineering, white-paper→constitutional/narrative/epistemology, multimedia→constitutional/style/narrative; default constitutional/engineering), any failure degrades to `none` — grounding informs, never gates; (b) cited ids ride the result (`groundingOf`, the `_evidence`-pattern additive extra), the ConstitutionalObject (payload `groundedInvariantIds`/`groundingSource` + `authority.governingInvariants: ['CFS-025', ...ids]`), and `artifact_records.cited_invariant_ids` (migration 20260714000000, operator-run; pre-migration grounded saves soft-fail); (c) operational + constitutional runs CITE via `citeInvariants` → Reach only (Law XII — Standing stays EXP-006A's receipted design); disposable never grounds, never cites; (d) softwarePilot + homecoming produce thread `citedInvariantIds`; research-overview + composer artifact-production observers project `groundedInvariants` counts. Validation: 6/6 parse gates; 30/30 stub-bundle drill (stub shapes verified against the real grounding module). Honest limits: tiering seams (studio/business) don't ground yet; live fallback is scope-ranked not content-matched; possible double-citation when composition already cited. Run record `updates/2026-07-14_cvr-003-runartifact-invariant-grounding.md`. | F | Done |
| 66 | **DCC Implementation Dispatch — the platform drives implementation — SHIPPED 2026-07-14** (operator: "no button or clear affordance that progresses this to implement — just copy pack… should now be at the stage where we can drive implementation, build and deployment within the platform"). The copy-paste break is closed: (a) `.github/workflows/claude-implement.yml` — `repository_dispatch` receiver running `anthropics/claude-code-action@v1` against the pack (materialized injection-safe via `toJSON()`→file) on an `aigentz/pack-*` branch (refused otherwise — `claude/**` would auto-merge to dev and collapse the D1 gate), opening a PR to dev, never merging; (b) `POST /api/dev-command-center/implement` — admin-gated, deterministic `dispatchBranchFor` (`aigentz/pack-<slug>-<sha8>`), 55k payload ceiling (honest 413, no silent truncation), best-effort `implementation_dispatched` receipt (new DVN-anchorable action type per the permitted-addition rule) — provenance chain pack → dispatch → PR → human merge; (c) `ImplementationLayout` gains the primary "Dispatch to Claude" affordance (single-shot personaFetch — experimentStep's retry could double-fire CI) + D1 propose box suggests the dispatched branch. D1 preserved: initiation is platform-driven + receipted; execution stays human at the PR merge. Validation: 4/4 parse gates, YAML clean, 7/7 branch-minting drill. Operator setup: `ANTHROPIC_API_KEY` repo secret + workflow onto main (repository_dispatch triggers only from the default branch). Run record `updates/2026-07-14_dcc-implementation-dispatch.md`. | F | Done |
| 67 | **In-app PR merge — the D1 human gate exercised in the platform — SHIPPED 2026-07-14** (operator: "Can I merge the PR from within the GitHub viewport… as opposed to having to go to github?"). `POST /api/dev-command-center/github/merge` (admin-gated; open + dev-base PRs only; descriptive merge-commit title per the CLAUDE.md rule; GitHub branch protection/checks still enforce; `deployment_authorized` receipt — merging dev IS authorizing the Amplify deploy) + `GitHubLayout` confirm-then-merge affordance on dev-base PR rows (`head → base` shown; two-click arming; sha + receipt in the result note; eyebrow "read + merge gate"). `_lib/github.ts` stays read-only — the write is route-inlined; `ghOpenPulls` gains `baseRef`. D1: the gate moved surfaces, not authority. Loop now fully in-platform: intent → pack → dispatch → CI implement → PR → in-app merge → deploy. 4/4 parse gates. Addendum in `updates/2026-07-14_dcc-implementation-dispatch.md`. | F | Done |
| 68 | **Merge validation gate + receipted override — validated code deploys — SHIPPED 2026-07-14** (operator-ratified: "we want validated code deployed" + "admin gated override… with a warning and dvn receipt"). Found + closed the missing receipt half: the DCC never wrote `constitutional_validation_recorded` (report lived in session state only) — `handleApproveProposal` now fires it on validation_report approval with verdict/counts/**packId**; validation-record route tags `pack=<id>`. Gate: `aigentz/pack-*` PRs merge only against the admin's own `verdict=pass pack=<match>` receipt (slug parity with `dispatchBranchFor`, drill-pinned); partial/fail blocked; non-pack PRs ungated; blocked → 409 + instructions. Override: reason ≥10 chars → merge + **`validation_override_granted`** receipt (new DVN-anchorable type) naming PR/branch/sha/reason + ⚠ unvalidated-deploy warning; GitHub capsule arms a reason-required override form on the blocked row. 6/6 parse gates, 12/12 drill. Limits: summary-tag correlation (payload field is the long-term home); persona-scoped receipt scan; pre-gate validations untagged. Addendum 2 in `updates/2026-07-14_dcc-implementation-dispatch.md`. | F | Done |
| 70 | **CFS-035 The Invariant Engine — CHARTERED 2026-07-18 (operator × Aletheon × Claude convergence).** Governing law: every consequential decision is a projection of the constitutional field, not an isolated heuristic. Four forms of embedded compressed reasoning (magic numbers · thresholds · branches · ordering) migrate to Constitutional Projection. Three levels (Registry exists / Engine = ~80% composition of existing façades + Field Snapshot + `resolvePolicy` / OS = the rollout). Four faces (Reasoning · Constitutional Projection · Experience · Evolution) over one Field Snapshot. Formal Invariant Decision Node schema (inputs/projection/outputs/receipts/metrics/standingEffects/lens). Lenses map 1:1 to the 5 OperatorArchetype pathways. Three generations (Grounded Reasoning → Constitutional Projection → Emergent). **Phase 0 pilot: discovery ranking (`scoreCapsule`) in shadow mode.** Field Extractor (perception) = Gen 3, deferred. Rollout reuses the DCIR seam + shadow-mode-first (CFS-017) + per-surface canary + this tracker. **Operator authorized full execution 2026-07-18.** | G | Phase 0 in build; shadow→authoritative flips operator-gated |
| 69 | **Dedicated validation runner + Deploy-Auth-hosted merge + descriptive auto-merge — SHIPPED 2026-07-14.** (a) Validate chat turns die at Amplify's hard ~30s SSR ceiling (`maxDuration` not honored — amplify-hosting#3475/#3223); the mega-prompt + longest generation can't fit. Fix: `POST /api/dev-command-center/validate` — one compact invariant-routed inference (`callSovereign('validation')`, ModelQube + sovereign ladder), canvas + 6k-bounded brief, honest unresolved-when-silent, returns a standard `validation_report` proposal into the EXISTING approve→receipt→merge-gate path; Validate capsule gains "Run validation". (b) Deployment Authorization capsule now hosts the pack-PR merge (operator: the final Deploy Auth stage is where the PR is actually merged/deployed) — lists open `aigentz/pack-*` PRs (session's pack preferred by slug), same merge route/gate/override as the GitHub capsule. (c) `merge-claude-to-dev.yml` fallback merge now titles itself branch + head subject — the forbidden `--no-edit` generic descriptor is gone from the Amplify deploy history. Canonical sequence: pack → dispatch → validate (approve report) → [remediate] → Deploy Auth: authorize + merge → Amplify deploys. Follow-on: validate against the PR's actual diff, not just the brief. | F | Done |
| 70 | **Dedicated remediation runner + guide-only auto-turns — SHIPPED 2026-07-14** (operator stranded at Remediation live: validation runner worked, report approved, loop advanced — then the auto-fired "produce the remediation_plan" chat turn died at the same Amplify ~30s ceiling; no button, no way to progress). Same cure as validation: `POST /api/dev-command-center/remediate` — one compact invariant-routed inference (`callSovereign('analysis')`), remedy + captured lesson per failed/partial/unresolved item, `revalidationRequired` defaults true (safe constitutional default), returns a standard `remediation_plan` proposal into the existing approve→record→re-validate path; Remediation capsule gains "Generate remediation plan" (sends everything not satisfied, not just the fork's high/critical). Auto-turns for the two heavy stages are now GUIDE-ONLY (2-3 sentences, explicitly no fence — the capsule button produces it), so the observed transition narrates reliably instead of dying. 3/3 parse gates. | F | Done |
| 71 | **The correction arc — remediation becomes code, validation judges the PR — SHIPPED 2026-07-15** (operator: "how would it resolve it if it doesn't implement and go back and write the code and redeploy for validation? … caught in a loop around remediation that is observing without correcting"). Confirmed live: PR #89 was already merged (operator, 13:50Z) and no remediation dispatch existed — the loop specified remedies but never implemented them, so re-validation re-failed the same build. Three-part close: (a) `claude-implement.yml` gains re-dispatch continuity — an existing `aigentz/pack-*` branch is checked out and AMENDED (never recreated from dev), and Claude updates the open PR (or opens a fresh one if the previous merged) instead of failing on `gh pr create`; (b) Remediation capsule gains **"Dispatch remedies to Claude"** — the approved plan travels as a remediation brief through the same dispatch route/branch (receipted `implementation_dispatched`); (c) the validation runner is **PR-aware** — given the pack id it folds the dispatched PR's body + changed-file list into the summary, so re-validation judges the ACTUAL amended work, not the stale brief. Canonical closed loop: Implement (pack + dispatch) → Validate → Remediate (plan → dispatch remedies) → CI amends the PR → re-Validate (PR-aware) → Deploy Auth (merge) → Amplify deploys. 3/3 parse gates + YAML. Operator setup: sync the updated workflow to main (repository_dispatch runs from the default branch). | F | Done |
| 72 | **Correction-arc first live run + receipt-constraint fix + flow-through UX + fast validation routing — SHIPPED 2026-07-15.** Live evidence: remediation dispatch run 7 SUCCEEDED → **PR #90** ("Video+Article remediation: Studio service integration + marketer/creator UX", both remedies additive, 32 tests) opened on the SAME pack branch — the closed loop works end-to-end; run 8 (double-click) collided harmlessly. Fixes shipped: (a) **migration 20260715000000** — the activity_receipts action_type CHECK had drifted since 2026-07-03; every dev-loop receipt since (incl. the merge gate's `constitutional_validation_recorded`) silently failed; rebuilt with the complete union (operator ran it; receipts now persist — dispatch receipt 994b09e3 confirmed); (b) chat prompt slim — `aigentMe` (empty-scaffold) KB search skipped, reclaiming up to 15s of the ~30s ceiling on every DCC/metaMe turn; (c) validate-runner 504 fix — validation stage default rerouted sonnet→haiku (mechanical checking; ModelQube/env override available) + maxTokens 2400→2000; (d) flow-through UX (operator direction): successful remedies dispatch pulses the Validate chip, deep-links into the Validate capsule, and fires a guide-only left-pane turn narrating the hand-off (CI ETA → Run validation → approve → Deploy Auth merge). 4/4 parse gates. | F | Done |
| 73 | **First fully-closed constitutional dev loop, live — PR #90 merged 2026-07-15** + stage→capsule reconciliation safety net. Operator drove the complete arc end to end: pack → dispatch (PR #89, merged) → validate (fail) → remediate (plan generated + approved) → dispatch remedies → CI amended the SAME pack branch → **PR #90** ("Video+Article remediation: Studio service integration + marketer/creator UX", 32 tests, additive) → re-validate → **merged to dev by the operator** — the loop this session built end to end actually shipped code. Gap found in the same drive: "when validation clears, Deploy capsule is not auto-opened." Fix: a decoupled `useEffect` in `DevCommandCenterTab` watching `session.stage` transitions — independent of the click-handler flow-through in `handleApproveProposal`/`handleAdvanceStage`, it reconciles the active capsule (+ re-pulses its chip) to whatever stage the session actually reaches, so the pane follows the loop even if a future stage-advancing path forgets the inline call. 1/1 parse gate. | F | Done |
| 74 | **Linear mirror: silent-failure fix + full dev-loop lifecycle wiring — SHIPPED 2026-07-15** (operator: "Linear tool is not showing any of the project work… Tool is working but no current project tasks being picked up"). Root cause: the pack-generation route already called `mirrorLifecycleToLinear` (phase `pack_generated`), but (a) its `linear` result was silently dropped by `ImplementationLayout` — if `LINEAR_TEAM_KEY` were unset or wrong, the mirror would soft-fail with zero operator-visible signal, breaking the honest-degradation convention every other CDE tool follows; (b) the dispatch and merge stages never called the mirror at all, so a shipped/merged pack's issue (if created) froze at "Todo" forever. Fixed: keying switched from free-text `goal` to **`packSlug(packId)`** (stable across a remediation redispatch, whose goal text differs — drill-confirmed 3-stage convergence); `implement/route.ts` now mirrors phase `artifact_produced` on successful dispatch; `github/merge/route.ts` now mirrors phase `published` (or `deployment_proposed` on an override) on successful merge of a pack PR; all three routes return `linear` in their JSON, and `ImplementationLayout`/`GitHubLayout`/`DeploymentAuthorizationLayout` now render it — success or failure, never silent. Canonical lifecycle: pack generated (Todo) → dispatched (In Progress) → merged (Done), one Linear issue per pack, tracked end to end. 6/6 parse gates, 3/3 convergence drill. Operator: if the note reads "Linear not tracking this pack: LINEAR_TEAM_KEY not set — available: …", set `LINEAR_TEAM_KEY` in Amplify to one of the listed team keys. | F | Done |
| 75 | **CFS-030 Constitutional Reconciliation + CCE-007 canonization — DRAFTED 2026-07-15, awaiting ratification.** Alethean's review of the dev-cycle-through-remediation drive proposed formalizing "Constitutional Reconciliation" as a primitive and canonizing today's cycle as an experiment. Numbering correction made before drafting (CS-001-class near-miss avoided): Alethean's proposed CFS-028 "Capability Evidence" + CFS-029 "Constitutional Decision" ALREADY EXIST under those exact numbers — CFS-029 (ratified 2026-07-13) already covers both; CFS-028 is already a different spec (Capability Graph Production Routing). "CCE-005" collides with the reserved slot (EXP-005 Provider Choice); the article-convergence dogfood run is already canonized as CCE-006 with near-identical framing to Alethean's independent wording. CFS-030 (genuinely free) drafted for the validate→remediate→dispatch-remedies→CI-amends→revalidate loop built 2026-07-14/15 (branch-continuity in `claude-implement.yml`, the Remediation capsule's dispatch button, PR-aware validation) — §3 names the generalization beyond code (registry/docs/ontology/schemas/VentureQubes) as unratified design, not built. CCE-007 drafted as a distinct SECOND experiment from CCE-006 (video/article skill's PR#89→validate-fail→remediate→PR#90→revalidate→merge cycle, driven by platform infrastructure rather than session narration), cross-referencing CCE-006, with an honest-limits section (N=1, no independent adversarial review of the remedy quality, no fresh execution of the amended code). Both files + collections.json registration pushed as candidates; ratification pending operator review. | F | Done |
| 76 | **CFS-030 + CCE-007 RATIFIED; three doctrinal amendments + CHRYSALIS_HISTORY.md created — 2026-07-15** (operator: "your corrections are completely good and stand accordingly… some nuggets here as well that might also be worth canonizing," relaying Alethean's second review). CFS-030 (Constitutional Reconciliation) and CCE-007 flipped from candidate to ratified/canonized. Triaged Alethean's second round the same way as the first — most points already canonical (CFS-029 pipeline; CFS-022's "constitutional operating environment, not IDE" already ratified 2026-07-09, near-verbatim; CFS-019's recursive mandate). Three genuine new nuggets recorded as amendments: (1) **Orchestration vs. Sovereignty** (CFS-018 amendment) — CCE-006/007 demonstrate constitutional ORCHESTRATION (memory, reasoning, governance, routing, evidence going platform-native); the sovereignty bundle's dimensions (`inv.sovereignty.10x`) are unmoved — inference still runs on frontier providers; naming this precisely prevents overclaiming. (2) **Constitutional Coherence — six recurring questions** (CFS-022 amendment) — what-exists / what-authority / what-capability / what-consequence / what-evidence / what-standing, mapped to the eight-facet object model + this session's own build as worked examples; descriptive, no new enforcement. (3) **The Lab↔Platform feedback loop, made explicit** (CFS-019 amendment) — the return path (Platform → Research Lab) CCE-006/007 make concrete for the first time, with an honest caveat that the loop is not yet automatic (an operator/witness still corrects numbering before ratification — this exact triage is the proof). Created `CHRYSALIS_HISTORY.md` (new document type, distinct from this tracker: narrative WHY, not engineering WHAT) with its first entry, Milestone 001, adapting Alethean's proposed historical paragraph with full honest-limits carried forward. All five files registered in `codexes/packs/irl/collections.json`. | F | Done |
| 77 | **CFS-031 The Constitutional Cybernetic Loop — RATIFIED 2026-07-15** (operator: "Some points from Alethean and I for reference and ratification," a macro synthesis following CCE-006/007). New foundational spec (next free number after CFS-030) formalizing: (1) **the deepened Constitutional Computing definition** — a cybernetic system where every action produces evidence improving both code and constitution, via a two-rate model (fast loop = code, intentionally high-variance; slow loop = constitution, intentionally low-variance); (2) **Standing as the membrane** carrying evidence from fast to slow loop (Action→Evidence→Standing→Confidence→Invariant Candidate→Ratification→Constitution) — naming a shape already scattered across delegate/invariant/object-model standing, not a new mechanism; (3) **Reconstitution** — the convergence half of CFS-030's cycle, the Chrysalis metaphor's stronger reading (continuous metamorphosis, no final form, not episodic versioning); (4) a **macro market↔research cybernetic loop** (Founder Office → Horizen → Verification → Research → Platform → Market) ratified explicitly as ARCHITECTURAL VISION, not delivered — Horizen has zero prior codebase footprint, Founder Office/Operation Leap remain deferred per CFS-015/023, checked before drafting to avoid the CS-001 defect class at the vision scale; (5) three hypothesis sources (market/constitutional/community-led) converging on the existing CFS-019 pipeline — only constitutional-led is exercised; (6) **Signals vs. Hypotheses** — vocabulary only, explicitly not yet a seeded invariant. Glossary amended: deepened Constitutional Computing entry + new entries (Reconstitution, Constitutional Coherence — merging last session's structural six-questions reading with this session's dynamic reading, Standing — previously undefined despite heavy use, Signal, Hypothesis). CHRYSALIS_HISTORY.md gained Milestone 002. Honest-limits sections in both the spec and the history entry are explicit about what's proven vs. envisioned. | F | Done |
| 78 | **CRP-003 Financial Services: the first Constitutional Capability Domain — CHARTERED 2026-07-15** (operator: "for your observation and capture," relaying an Alethean exchange narrowing a Founder Office Financial Services Division). Verified against the codebase first: **Horizen has zero prior footprint** (no spec/service/route — flagged prominently, same discipline as CFS-031); **Constitutional Commerce infrastructure already exists and is already honestly scored** — IRL-010A §3.6 already marks Q¢ pricing/multi-rail payments/KNYT commerce Implemented(early), *constitutional* (standing-mediated) commerce Proposed, ⚑-flagged — this charter reuses that classification rather than re-asserting it. Chartered five domains, each mapped to ONE constitutional primitive rather than a generic financial-workflow category (deliberately avoiding "pick the three biggest categories"): Treasury Operations (Bounded Delegation), Trading Orchestration (Standing), Financial Intelligence (Evidence), Constitutional Financial Integrity (the cross-cutting invariant "every constitutional transaction carries its own authority/resources/verification/settlement" — CFI-001), and Constitutional Commerce repositioned from "a payment protocol" to "the constitutional integrity layer for economic interactions" (settlement becomes a replaceable adapter). ALL candidate invariants (F-001–F-203, CFI-001, CFI-Q1–Q4) explicitly marked UNSEEDED vocabulary — a future seed-and-ratify pass must translate them into the crystal's own `inv.finance.<n>` numbering, never keep the "F-nnn" shorthand as a parallel scheme. Horizen pilot (3 proposed experiment series) marked unscheduled. New CFS-019 amendment formalizes **"Constitutional Capability Domain"** as a third IRL organizing category (alongside CRP/CFS) — a unit with three simultaneous outputs (Scientific/Platform/Commercial); Financial Services is its first named instance. "The Blocksee outcome" referenced by the operator is now recorded as operator-attested context (2026-07-16 clarification): an earlier conditional-commerce pilot planned with a vendor consortium that fell through, the historical reason for the charter's vendor-neutral commitment — same operator-attested status as IRL-010A row 3.8, not code-witnessed. | F | Done |
| 48 | **CFS-024 Delegation consolidation — SHIPPED 2026-07-10.** Persona-first Delegation tab + persisted per-persona assignments (`persona_agent_assignments`, many delegates / one aigentMe / reassignable) + Wallet reflection (aigentMe star single-sourced from the resolver). Cross-persona aigentMe-creation fixed. Migration run. Follow-up: filter agent-personas out of the "Delegating as" selector (needs a reliable agent-persona marker). | F | Done (1 minor follow-up) |
| 79 | **Invariant Field Theory (CFS-019 amendment) + CRP-003 domain generalization + IRL Principle 005 — RATIFIED 2026-07-15** (operator + Alethean, "further refinements for ratification where appropriate"). Verified before drafting: `IRL-011` has zero prior "field" terminology, but CFS-019 §1's central hypothesis has named "Invariant Fields" since the charter's founding and Phase E already shipped an **Invariant Field Explorer** (2026-07-07) — so this amendment names the empirical RESEARCH PROGRAMME the founding hypothesis always implied, it does not invent new vocabulary or duplicate the Explorer instrument. Four-part programme: domain invariants → cross-domain/shared invariants → field strength → interference, with EXP-003's breadth-arm result (−14.7% breadth delta, "curation dominates accumulation") reinterpreted — explicitly flagged as a reinterpretation, not a new finding — as founding evidence of interference. CRP-003 amended same day: Domain 1 "Treasury & Portfolio Operations" → **Investment Operations**, Domain 2 "Trading & Execution Orchestration" → **Market Operations** (candidate invariants F-001–F-103 unchanged, renamed for capability-generic framing), explicit scope-item lists added to all five domains per Alethean's breakdown, and §7's Horizen pilot reframed from three per-domain questions to six field-theory measurements (domain invariants, shared invariants, field strength, interference, coherence, Time-to-Value) — Horizen's zero-footprint flag carried forward unchanged. New **IRL Principle 005** seeded (`inv.epistemology.133`, `proposed`, appendix-a + seed.json): "the value of an invariant lies not in how it was discovered, but in its stability across contexts and its ability to organize future reasoning" — the operator's "the origin of the invariance is not what is important, it is the invariance of the invariance," Alethean's rephrasing ratified as the canonical statement. Distinguished explicitly from Principle 004 (131, instrument honesty) and from 132 (organization scales knowledge quality) before seeding, to avoid restating either under a new number. Answers the critique (attributed to Austin's Claude) that invariant extraction/reuse differs from prompt engineering or RAG because it commits to a validated, reusable claim rather than retrieving context. | F | Done |
| 80 | **Constitutional Agreement primitive (CFI-002) + the Constitutional Agent Execution Pilot (six workstreams) — RATIFIED 2026-07-16** (operator: "in prep for next series of pilots," relaying Alethean's stack decomposition of Constitutional Commerce by primitive rather than vendor). Assessment: the platform already holds ~90-95% of the stack (Passport, Standing, Bounded Delegation, Agent orchestration, Verification, USDC settlement, QriptoCENT constitutional fees — all native, no new lift); the one genuinely missing primitive is **Agreement** — request→negotiate→accept→authorize→delegate→execute, an explicit attributable record binding intent to delegated authority before execution. New candidate invariant **CFI-002 — Constitutional Agreement Required** added to CRP-003 Domain 4, provider-agnostic by construction (`x409` is the pilot's first candidate provider, not the definition — same "primitives invariant, providers replaceable" pattern as settlement/CFI-001). CRP-003 §7 renamed the **Constitutional Agent Execution Pilot** and restructured into six workstreams: Sovereign Identity, Constitutional Agreement, Agent Discovery & Orchestration (Horizen Pulse), Constitutional Execution, Constitutional Commerce, and **Constitutional Memory** (carrying forward the full prior-proposal scope: transaction/execution receipts, AutoDrive positioned as one implementation of a Constitutional Evidence Store, TEEs and Sui Walrus named as candidate secure-execution/evidence-preservation technologies, deliberately uncommitted between them). Verification catch before drafting: re-grepped "Horizen" and found the name already appears as KNYT campaign Wave-1 partner #14 (`KNYT_PARTNER_AND_INVESTOR_ACTIVATION_ADDENDUM.md`) — corrected the prior CFS-031/CRP-003 "zero prior footprint" claim to "no technical footprint," and explicitly left open, unresolved, whether the campaign partner and the pilot's candidate registry partner are the same organization (CLAUDE.md's no-guessing rule — flagged, not assumed either way). Distinction made before naming anything: **Transaction Reconstitution** (per-transaction receipt-trail replay/audit property, new) is NOT the same as CFS-031 §3's existing **Reconstitution** (system-level code/constitution convergence) — the pilot's final loop step ("evidence → Standing → invariant research → platform") is the EXISTING Reconstitution mechanism given a concrete entry point, not a new "Platform Reconstitution" term (avoided minting a redundant name for the same concept). Companion amendment: CFS-018 gains "Constitutional primitives are invariant; providers are replaceable" as a named cross-cutting rule, tabulating the pattern across inference/identity/settlement/agreement/evidence-storage. Glossary gained Transaction Reconstitution + Constitutional Agreement entries, placed beside Reconstitution for the contrast. All candidate invariants remain unseeded. | F | Done |
| 81 | **WP5 Invariant Morphogenesis + Convergence Log + Invariant Lineage — RATIFIED 2026-07-16** (Aletheon's reconciliation of the operator's DNA/biology dialogue against standing doctrine, "discover, reconcile, then extend... becoming a constitutional method in itself"). Major verification finding, caught before drafting: most of the dialogue's "new" insights were ALREADY ratified 2026-07-06 — CFS-002 §2a (inv.81/84/85) already defines an Invariant Field as "a coherent domain of interacting invariants... a role, not a container," already states fields may be "natural (discovered) or constitutional (ratified)," and its systems table already carries the exact DNA row the new dialogue re-derives independently ("DNA does not create an organism; it defines the fields whose composition IS the organism"); CFS-013/014/Law XV already govern HOW invariants compose. Corrected CFS-019's Field Theory amendment (2026-07-15) to cite inv.81/84/85, which its first draft missed. Two genuinely new naming collisions caught: "Behavioral Invariant" (DCIR's narrow observed-pattern term) and "Representation Invariant" (CFS-021's media-identity family) both collide with the dialogue's proposed genotype/phenotype split — Aletheon's counter-proposal (Generative/Expressed) explicitly NOT ratified this round, deferred pending further naming iteration ("I'd rather spend another week finding the right names than spend the next five years living with slightly wrong ones"). What IS genuinely new and ratified: **WP5 — Invariant Morphogenesis** added to CRP-002's ladder (WP0–WP4 unchanged, no split of WP2/WP3 despite an earlier mislabelled framing) — studies whether invariant composition can generate a genuinely NEW invariant, not just a new output/experience (distinct from Law XV, which already covers experience-level emergence); human/machine/hybrid morphogenesis as a research vector; Constitutional Capital (already named in `invariant-intelligence.md`'s founding-programmes list, not invented for this dialogue) as WP5's first candidate case study; WP6 — Invariant Ecology named for much later, explicitly not chartered. New `CONVERGENCE_LOG.md` (registered in collections.json) records independent rediscoveries as evidence-not-proof, per Aletheon: "science values independent convergence." Glossary gains **Invariant Lineage** (the traceable derivation graph spanning CIRS mutations, KnowledgeQube versions, WP5 candidates, and constitutional amendments — named, not yet built). CFS-019 gains a new amendment naming "discover→reconcile→extend" as a constitutional method (sibling to, not a duplicate of, the existing "discipline of distinctions") and the three-layer research discipline (empirical / computational theory / philosophical) as guidance, not a law. No invariant seeded; `types/invariantIntelligence.ts`'s workstream constant not yet updated (named code follow-on). | F | Done |
| 82 | **Clarification — Invariant Fields extend Invariants, never replace them — RATIFIED 2026-07-16** (operator: "Invariant Fields do not replace Invariants, they are more an extension of them. Invariants and their fields both remain canonical."). A precise, small correction against a real risk: the last several rounds' heavy emphasis on "fields" (CFS-019 Field Theory, WP5 Morphogenesis, CRP-003's domain reframe) could be misread as fields superseding invariants as the primary canonical unit. CFS-002 §2a already implied the non-replacement relationship structurally ("three axes, not one hierarchy": Invariant / Invariant Collection / Invariant Field / InvariantQube are each distinct, a field is "a role, not a container") but never stated the non-replacement rule explicitly. Made explicit in three places: CFS-002 §2a's Definition — Invariant Field section gains a one-paragraph clarification; a new **Invariant Field** glossary entry states it as the entry's second sentence; CFS-019's Field Theory amendment gains a clarifying paragraph before its central research question. No mechanism changed — a documentation-only correction, closing an ambiguity before it could be misread by a future agent or reader. | F | Done |
| 83 | **CFS-032 — The Constitutional Development Router — CHARTERED 2026-07-16** (operator: "I think we're now ready to close out chrysalis 2.0's last evolutionary set of features," relaying Aletheon's Phase 2B proposal). The operator's own five sequential "sovereignty milestones" (ChatGPT→Claude Code→Anthropic APIs→multi-frontier→open-weight→sovereign agents) reconciled, per the operator's own mid-dialogue correction, into ONE routing capability rather than five projects. Major verification pass before drafting: (1) confirmed CFS-015 already named Model Router/Provider Sovereignty/Sovereign Survivability as Chrysalis 2.0 objectives at founding — validates Aletheon's "Phase 2B, not 2.5" framing; (2) confirmed CFS-023's four Homecoming workstreams already match Aletheon's sequencing claim, no correction needed; (3) caught that Aletheon's proposed new "Implementation Strategy" stage DUPLICATES the already-ratified CFS-029 §4 "Constitutional Decision" stage (same vocabulary: none/code/configuration/registry/prompt/policy/schema/knowledge/automation/documentation) — no new stage added for this, only "Development Routing" (fires when mechanism=code) is genuinely new; (4) confirmed "Constitutional Acceptance" (between Validation and Receipt) has zero prior codebase footprint — genuinely new, charters the distinction between "did it work" and "does this become part of the platform's constitutional/reusable state," no mechanism specified yet; (5) **directly answered the operator's question about post-deployment Standing** by grepping actual call sites of `actionType: 'standing_accrued'` — confirmed it fires ONLY from `services/crm/standingAccrualService.ts` (CRM/venture domain), never from any DCC deployment/merge/validation receipt — Standing does NOT currently accrue after a capability ships, a real code gap now named (§5), not fixed. CFS-032 charters the three-axis distinction that resolves the operator's own expressed uncertainty ("I don't know that we've conflating two things") — reasoning/inference routing (existing ModelQube router) ≠ development/code-writing routing (this spec, unbuilt beyond Claude Code) ≠ deployment execution authority (CFS-016 D1/D2/D3, completely unchanged). Cites `inv.engineering.031` (already ratified 2026-07-09: "reasoning is the platform's, inference is a swappable object") as the SAME invariant Aletheon's closing insight restates for the development axis — recorded as Convergence Log Entry 003, not a fresh discovery. Companion amendments: CFS-018's "primitives invariant, providers replaceable" table gains a Development row; CFS-015 gains a Phase 2B cross-reference; glossary gains Constitutional Development Router, Constitutional Acceptance (with an explicit note that "Development Router" is NOT the same as the deliberately-retired "Constitutional Development Pipeline" name). All routing dimensions and executor adapters remain named, not built. | F | Done |
| 84 | **Constitutional Acceptance repositioned + defined as the registry write — RATIFIED 2026-07-16, same day as CFS-032** (operator: "the constitutional act here would be recording the capability and its associated metadata in the registry as a new constitutional asset"). Two same-day corrections to the fresh charter: (1) confirmed no "Implementation Strategy" stage was added — CFS-032 §3 already records it as a duplicate of CFS-029 §4's Constitutional Decision, per operator confirmation; (2) Acceptance MOVED from between-Validation-and-Receipt (Aletheon's first sketch) to immediately FOLLOWING Deployment + its receipt, and DEFINED as the act of recording the shipped capability + metadata (pack id, PR, validation/deployment receipts, governing invariants, reuse disposition) in the registry as a new constitutional asset (`ConstitutionalObject`). Rationale: the constitutional state should only contain capabilities that actually shipped; composes with the §5 Standing sequence (Acceptance records THAT the asset exists; Operational Validation evidences it WORKS; Standing accrues from the latter — declared→verified→accrued preserved). Verified against code before agreeing: Gap Analysis today reads `capability_evidence` (per-goal evidence rows) — NO durable catalog of shipped capabilities exists anywhere; the Canonical Asset Registry (`services/composition/canonicalAssets.ts`, CFS-022a) has the right `ConstitutionalObject` shape and is the extend-don't-duplicate default home, but has only ever held representation/composition assets (Bearing Instrument, metaVitruvian, Plates), never a shipped code capability. Mechanism remains unbuilt — CFS-032 §4/§9 and the glossary entry updated to the corrected definition and placement. | F | Done |
| 85 | **Registry Registration ≡ Constitutional Acceptance (one event) + Standing's registration precondition — RATIFIED 2026-07-16, third same-day refinement of CFS-032** (Aletheon confirming alignment with rows 83-84 and strengthening the identity: "I don't think they're separate. The constitutional acceptance event is literally: the capability is admitted into the constitutional registry. That's the constitutional ceremony. Everything before that is engineering. Everything after that is constitutional memory."). CFS-032 §4 now records the identity explicitly (no separate acceptance ceremony; the registry write IS the event) plus its two consequences: (1) **the Registry, not the receipt, closes the self-improvement loop** — only registration makes a shipped capability discoverable by the next Gap Analysis, which is what makes the pipeline constitutionally evolutionary rather than merely well-documented; (2) **the Registry's role sharpened**: the constitutional ledger of capability, not an asset catalogue — registration is the capability-level equivalent of constitutional ratification (the same proposal→ratification→canonical shape Law XI gives invariants). §5's Standing sequence refined to compose Aletheon's registration precondition with the existing operational-evidence trigger: **registration is the eligibility gate** (only a capability admitted to the constitutional order can accrue Standing at all), **operational evidence is the accrual trigger** — registered-but-unverified accrues nothing yet; verified-but-unregistered accrues nothing ever (declared→verified→accrued preserved, now with "entered the constitutional order" as the explicit middle term). §2 diagram updated (Registry Registration ≡ Constitutional Acceptance as one node; Knowledge → future Gap Analysis closes through the Registry). Also confirmed: Aletheon's revised pipeline sketch matches CFS-029/CFS-032 as already drafted — Constitutional Decision stays the CFS-029 stage (no "Implementation Strategy" duplicate), Development Routing remains the only new pre-implementation stage. All mechanisms remain unbuilt; doctrine-only refinement. | F | Done |
| 86 | **Constitutional Acceptance + Standing wiring BUILT — the loop closes in code — SHIPPED 2026-07-16** (operator: "Let's build them and close the loop"). The two mechanisms CFS-032 §4/§5 chartered are now real: (1) **Registry Registration ≡ Constitutional Acceptance** — `capability_registry` table (migration `20260716000000`, RLS service-role-only, activity_receipts CHECK rebuilt with `capability_registered` + `capability_operationally_validated`, both added to ANCHORABLE_ACTION_TYPES per the permitted rule); `services/constitutional/capabilityRegistry.ts` with pure `buildCapabilityObject` (new `'capability'` kind on `ConstitutionalObjectKind`, canonicalAssets object shape mirrored, T2-safe by construction — `findForbiddenObjectKey` clean); `registerCapability` is an idempotent, receipted ceremony (re-registering returns the existing row, no duplicate receipt — acceptance is one-time); admin-gated route `/api/constitutional/capability-registry` (GET ledger / POST register / POST operational-validation). (2) **Standing accrual per §5's two-condition composition** — `recordOperationalValidation` REFUSES with an honest 409 when the capability is not registered (the eligibility gate — verified-but-unregistered accrues nothing ever), requires evidence ≥10 chars ("Standing accrues from verified contribution, not from a click"), accrues +0.1 capped at 0.55: **accrual reaches `validated` band but can never mint `canonical`** — that requires a Law XI ratification ceremony (policy bug caught during build: the first-draft constants 0.3/0.7 would have entered at `validated` and accrued to `canonical`, contradicting the doctrine — corrected against the actual `standingBandFor` boundaries before shipping). (3) **The loop-closing read** — `buildStageGroundData` now folds `registeredCapabilityBlock()` into gap-analysis + context-assembly ground data, so a constitutionally accepted capability is DISCOVERABLE by the next capability request's Gap Analysis — the Registry, not the receipt, closes the loop, now in running code. (4) **UI** — the Deployment Authorization capsule gains the Constitutional Acceptance section: accept-into-registry (seeded from the session's pack slug + intent goal), the live capability ledger (standing/band/validation count per row), and per-capability Record-operational-validation with an evidence field. Verified: 7/7 esbuild parse gates + an executed 8/8 smoke run (experimental floor, provenance carriage, determinism, T2-safety, accrual sequence + cap band). Operator setup: run migration 20260716000000 (SQL provided) — until applied, writes soft-fail with an honest note, never silently. Named follow-ons: the Development Router itself (no second executor adapter yet) and automated operational-validation triggers (Chrysalis-Test-driven). | F | Done |
| 87 | **EXP-010 Representation Gauntlet chartered + EXP-006 numbering collision corrected — 2026-07-16** (operator: "fold into the lab as appropriate," relaying the three-way exchange with the external reviewer's agent and Aletheon). **Collision caught during the fold**: "EXP-006" was doubly assigned — CRP-002's *Intent → Invariant Projection Fidelity* (2026-07-09, code-registered, Stage A built) AND the longitudinal *Constitutional Knowledge Evolution* series (2026-07-14, whose designation note checked 005 but missed 006). Per no-number-reuse + first-assigned-wins, the DESIGN-status longitudinal series renumbered to **EXP-009** (directory git-mv'd, README designation history records the full collision + the external-correspondence mapping "EXP-006A/B" → EXP-9A/9B; IRL-011 §9A, CFS-022, breadth-arm, collections.json updated; tracker rows 64–65 left append-only as history). **EXP-010 — the Representation Gauntlet** chartered (DESIGN; joint protocol pending external sign-off; nothing run): the Claim A/Claim B decomposition (content vs substrate — only B is distinctively the institute's, and the reviewer's Arm C misread corrected for the record: origin was never the variable, per Principle 005); Aletheon's representation reframe adopted — an invariant is a computational object, not a sentence ("hydrogen atom vs the word Hydrogen"), so the flattening control SPLITS into Arm C (decomposition preserved, machinery removed) vs Arm D (expert prompt, no decomposition): B-vs-C isolates runtime semantics, C-vs-D isolates decomposition; Mutation probe reframed as LOCALITY (propagation is by-construction; the question is change-one-without-reconstructing-the-rest); pre-committed interpretation for every branch incl. the null soft-landing (knowledge curation, commercially real). Two methodological adoptions recorded as IRL METHOD guidance, not seeded invariants: **the by-construction guard** (a property test counts only if not guaranteed by construction or causally productive of capability an equal-cost baseline lacks — Principle 004 applied to experiment design; composition challenge is the one passing property test, Phase 3, scoped to two collections) and **the language rule** ("empirically stable under repeated validation," never "provably invariant" — standing is accumulated LLM-judge validation, not proof). The reviewer's closing capability probe answered against the code, honestly: slice export substantially supported (`buildInvariantSlice` path) but no first-class verbatim-export command; judge config partially external (EXP-001 harness takes provider/model, rubric still in-script) — both named as unbuilt Phase 1 prerequisites. Phased sequence recorded: P1 static gauntlet (n≥20/arm, hashed pre-registration, joint judge spec) → P2 EXP-9A/9B → P3 composition challenge + externally-scored forecasting benchmark; finance as host domain (CRP-003), never validation. Posture shift named: from proving invariants work to discovering why they work. | F | Done |
| 88 | **EXP-010 Phase 1 prerequisites BUILT — verbatim slice export + external judge config — SHIPPED 2026-07-16** (operator: "Can AgentiQ platform accommodate an externally specified judge configuration that can export grounding slices verbatim from Arm C per Austin and his agents request?" — the answer is now an unqualified yes, in code). (1) **`scripts/export-grounding-slice.mjs`** (new instrument, benchmark-rediscovery sibling): exports the exact invariant statements verbatim — one marker-prefixed statement per line, decomposition preserved (Arm C's shape by construction) — as a text file + a manifest carrying the sha256 pre-registration commitment, full generation parameters, per-invariant provenance (seed id/standing/confidence/reach/status), and a token estimate for the equal-budget discipline; `--collection exp001` (fixed instrument, config order) and `--broad` (live domain slice, standing→confidence→reach — the benchmark's broad query verbatim); deliberately never generates Arm D (human-authored by design). (2) **`--judge-config <path>` on `scripts/evaluate-exp001.mjs`**: an external JSON artifact specifying provider/model/rubric-prompt overrides (answerSystem, judgeQuestionSystem, judgeCoherenceSystem); the config's sha256 prints at run start AND rides the results JSON (`judgeConfig: {path, sha256, rubricOverrides}`); the pre-registered config is authoritative — a conflicting `--provider` flag is a hard error, never silently resolved; same format is the contract EXP-010's future Phase 1 harness consumes (extend-don't-duplicate). Verified: `node --check` both scripts + live stub-bundle drills (config load + sha256 banner + override listing; the provider-conflict guard fires with the exact honest message; exporter usage line; missing-env honesty). EXP-010 charter §7 + honest limits updated from "unbuilt prerequisites" to built. What remains unbuilt: EXP-010's own Phase 1 runner (awaits the jointly signed protocol). | F | Done |
| 89 | **CFS-033 Constitutional Evaluation — the Experimental Operating System — CHARTERED 2026-07-16** (Aletheon's generalization of the external reviewer's capability probe: "if you build it as a first-class capability of the AgentiQ Runtime, it becomes scientific infrastructure"). Ratifies the spec-level principle **"evaluation is a pluggable, receipted, versioned component of every experiment"** (deliberately unseeded as a crystal invariant) and adopts the naming rule: the capability is Constitutional Evaluation, never "externally specified judge configuration" (which names one consumer, not the capability). Verified-what-exists table before drafting: the platform already holds real fragments of Aletheon's Evaluation Configuration tree — externalized task banks (`exp001-config.json`), the verbatim slice exporter + external judge-config artifact (row 88, components 3 and 5, built hours before the charter — the abstraction named from working code, not speculation), hash-committed `experiment_results`, `experiment_result_published` DVN receipts, the Independence Protocol's cross-provider role routing, the versioned-crystal discipline (EXP-009), and `replicated` already lifecycle-defined as ≥2 distinct providers. Chartered as VISION, honestly: the full `EvaluationConfiguration` object, **Research Packages** (fully exportable/replayable experiments — the versioned-crystal discipline extended from knowledge substrate to entire apparatus; vs today's "we prompted GPT-5, good luck reproducing that"), **multi-judge panels + the judging≠adjudication architectural split** (judges score, adjudication resolves disagreement — the seam IRL-010's "internal adjudication" honesty note and EXP-001's human-final-authority rubric always implied), and **distributed replication** (export→import→run→submit-receipt→aggregate; "distinct providers" extended to distinct PARTIES — explicitly the furthest-out product idea, "Invariant Intelligence Research Kit"). Build order deliberately unspecified; EXP-010 Phase 1 does not wait for any of it (it needs only the two built instruments). Glossary gains Constitutional Evaluation. Any implementation forking a parallel receipt/versioning path violates the spec. | F | Done |
| 90 | **IRL OS — the public-facing research cartridge — SHIPPED 2026-07-16** (operator: "create a new public facing cartridge for this like AgentiQ OS is the open public facing version of AgentiQ"; name confirmed **IRL OS**, all slugs matching). New `IRL_OS_CARTRIDGE` in `data/codex-configs.ts` (id `irl-os-cartridge`, slug `irl-os`, `permissions.view: ['*']`, owner `system`), registered in `CODEX_DEFINITIONS` — a standalone cartridge with **its own embeddable URL** (`/triad/embed/codex/irl-os`), surfaceable in the runtime and embeddable elsewhere. **v1 content-only, 9 tabs, all public** (no `adminOnly` anywhere): Charter, Layers I–III, **Constitutional Evaluation** (NEW — the CFS-033 front door for external researchers, no internal counterpart), Protocols & Articles (`col_experiments`, EXP-001…010), Glossary & Ontology, Records & Findings, Research Programmes — every tab an `AgentiqCartridgeTab` consuming the SAME `irl` pack as the internal cartridge (single source of truth; `irl` already in the packRegistry skip list — zero auto-duplicate risk, no packRegistry change). metaMe `QuickLinksCard` gains the IRL OS entry (runtime surfacing). **Deliberately excluded**: `InvariantExperimentLab` (admin, credit-spending — never public); the four interactive-but-public instruments (Dashboard, Research Copilot, Invariant Field Explorer, Invariant Registry) deferred to a named follow-on gated on an anonymous-read API audit of each. CFS-033 gains §8 (UI surface decision + build record). Naming note: "IRL OS" is the cartridge/product name — CFS-019's pending external-banner decision (Aletheon's "Invariant Intelligence Research Institute") remains pending, deliberately unresolved by this increment. Verified: parse gates + no slug/id collisions (`irl-os` unique across CODEX_DEFINITIONS, LEGACY_CODEX_SLUGS, pack dirs). | F | Done |
| 91 | **EXP-010 Phase 1 FROZEN + the structural/constitutional disentanglement — RATIFIED 2026-07-17** (the final alignment round with the external reviewer; operator: "the first principle science here is structural invariants, not constitutional ones... the runtime is more of a convenience to this than a first principle participant"). **Four adoptions, frozen into EXP-010 §7A**: (1) the protocol freeze — four arms + mutation probe + generative sufficiency probe, nothing further admitted to Phase 1, every new hypothesis → the Phase 2+ parking lot; (2) the **generative sufficiency probe** — reconstructability becomes a scored sub-task (C must beat D on held-out DERIVATION tasks specifically, not recall; C≈D on derivation = unsupported at current scale, publishable); (3) the **token accounting rule** (final rendered prompt, target-model tokenizer); (4) the **mechanistic difference enumeration — ANSWERED from the code, not deferred**, with a **pre-registered prediction stated before any data: B ≈ C by construction on single-call recall tasks with a fixed matched slice** (serialization identical — the exporter mirrors the runtime slice marker-for-marker; the within-call difference localizes entirely to selection-under-intent; post-call citation is lifecycle and cannot affect the in-call answer) — converting the reviewer's anticipated "escape hatch" into a falsifiable prediction, plus Aletheon's **Mechanistic Trace** artifact (corpus→selection→composition→serialization→model→evaluation→receipts, starting at the corpus). **EXP-010 §7B names the real fault line**: two hypotheses were hiding in one design — Hypothesis B/Execution (the reviewer's: does the runtime add within-call value? — Phase 1, frozen) vs **Hypothesis A/Structure (the Institute's PRIMARY claim: does invariant organization change the computational properties of reasoning vs raw experience?** — deliberately NOT in Phase 1, parked to Phase 2 under identical disciplines, EXP-nnn assigned only when chartered). Four-stage decomposition recorded: Discovery→Representation→Execution→Evolution (Phase 1 = Execution; EXP-009 = Evolution). **Verification finding that anchors the reply to the reviewer**: the structural/constitutional separation is NOT new doctrine — `inv.reasoning.085` (2026-07-06, natural/discovered vs constitutional/ratified fields), CFS-021's three families, and CFS-019 §2's Layer I vs II/III predate the exchange by ten days; the ontology already separated the layers, the experiments and prose had conflated them (Convergence Log Entry 004 — the separation cannot be dismissed as an escape hatch invented under adversarial pressure). Axis-precision caveat recorded and binding: operator's structural/constitutional = FUNCTION axis; inv.085's natural/constitutional = ORIGIN axis; mapped through CFS-019 layers, never silently equated. CFS-019 gains the "Structural before Constitutional" amendment (operator's articulation + Aletheon's one-sentence synthesis adopted as the external framing sentence; Programme A/B redraw recorded as a lens, NOT adopted; future-architectures argument deliberately kept OUT of Phase 1). Pre-committed interpretation of the reviewer's "repricing" scenario: B≈C within-call is the two-layer doctrine stating itself in data, not a retreat. | F | Done |
| 92 | **IRL OS anonymous-read audit + first public instrument + naming closure — SHIPPED 2026-07-17** (operator: "lets run this now" on the four-instrument follow-on; "IRL OS is the name, close down Invariant Intelligence Research Institute"). **Audit run against every route the four interactive instruments call**, verified in code: ALL 401 anonymous (each resolves `getActivePersona`); two additionally 403 non-admin (`chrysalis-test`, `research/objects`). Verdicts: **Invariant Registry (Browse) — ELIGIBLE, SHIPPED**; **Invariant Field Explorer — eligible, deferred** (needs a shared-read extraction of the 415-line spine-gated field route); **Dashboard — partial, deferred** (needs public results+overview projections + the admin chrysalis-test source dropped); **Research Copilot — NOT public-eligible, permanently internal** (live LLM chat + provider credits + admin writes, same class as the Experiment Lab). **Built the public Invariant Registry**: new read-only route `/api/public/irl/invariants` (reuses `listInvariants`, capped at 250, T2-safe, NO persona — a NEW public surface, the internal spine-gated `/api/invariants` untouched, so no access gate weakened per CLAUDE.md PARAMOUNT; the data is published canon already in appendix-a) + `publicMode` prop on `InvariantRegistryTab` (Browse-only, plain `fetch` at the public route, Overview/Ontology/Graph toggles + detail modal disabled since they hit gated routes without public projections; the internal tab with publicMode omitted is unchanged) + the tab wired into IRL OS's knowledge group (`props: { publicMode: true }`). First live interactive instrument in the public cartridge — an anonymous visitor can browse the real substrate. **Naming closed**: corrected the erroneous CFS-033 §8 "external banner still pending" note — the institution was named metaMe IRL on 2026-07-09 (already superseding "Invariant Intelligence Research Institute"); that candidate is now explicitly retired (CFS-019 naming note + CFS-033 §8/§8A). Settled: institution = metaMe IRL, public edition = IRL OS. Verified: 3/3 esbuild parse gates; new tab id/slug unique. | F | Done |
| 94 | **Polity Papers → invariant register canonization — BACKLOG (operator-flagged 2026-07-18).** The operational-vs-absolute truth register just canonized as `inv.epistemology.149` is the Polity Papers' **FACT — a "Fundamentally Accurate Current Truth"** (fundamentally accurate + current/operational, never absolute; operator-attributed). The Polity Papers are **DVN-receipted in the Qriptopian Cartridge** but not yet ingested as text into THIS register — the full phrase "Fundamentally Accurate Current Truth" does not yet appear anywhere in the repo (verified). Future exercise: walk the Polity Papers series and canonize their content (the FACT framing + the papers' governing claims) into the invariant crystal, seed-and-ratify per Law XI, reconciling against existing canon (CS-001 discipline — reaffirm, don't duplicate) and citing the DVN receipts as provenance. Related prior backlog (INGEST workflow, distinct from this REGISTER-canonization angle): `agentiq/updates/2026-06-22_polity-papers-ingest-workflow-backlog.md` + `2026-06-17_polity-paper-series-ingest-backlog.md`. Not started. | F | Backlog — operator to schedule |
| 93 | **Public Invariant Field Explorer — the shared-read extraction — SHIPPED 2026-07-17** (operator: "do the Field Explorer extraction next"). The 415-line spine-gated `/api/research/invariant-field` route was refactored into a shared, persona-free module **`services/research/invariantFieldQuery.ts`** (`queryInvariantField` for GET neighbourhood/overview, `projectInvariantFieldCounterfactual` for the pure what-if POST — both reuse the same store readers + `forecastConsequences` + `projectCounterfactual`, write nothing, emit no personaId). The gated route now delegates to it (**415 → 57 lines**, persona gate byte-identical) and a NEW public route **`/api/public/irl/invariant-field`** calls the SAME module with no gate — Extend-Don't-Duplicate, no gate weakened, the two routes can never drift. `publicMode` on `InvariantFieldExplorerTab` points all three fetches (picker + overview/neighbourhood GET + counterfactual POST) at the token-free public routes; the internal tab is unchanged. Both GET and POST are safe public: read-only, pure graph traversal + arithmetic, NO LLM/provider calls (no credit-spend vector). Wired into IRL OS as a new "Consequence Engineering" group tab (`props: { publicMode: true }`). **Two live interactive instruments now public** (Registry Browse + Field Explorer). Remaining: Dashboard (partial, needs public results/overview + chrysalis-test dropped) and Research Copilot (permanently internal — LLM chat + credits + admin writes). CFS-033 §8A updated. Verified: 4/4 esbuild parse gates; the internal gated route's logic is byte-for-byte the same behaviour (same shared module) — a genuine codebase improvement, not just an addition. | F | Done |
| 98 | **CFSP — Constitutional Financial Services Programme (Horizen Pilot Series 001) — PRD RECEIVED 2026-07-18, implementation programme opening.** Operator-supplied PRD v1.0 (MetaProof Internal): Financial Services as the **first Founder Office Capability Suite**, delivered via Pilot Series 001 with Horizen, provider-agnostic by construction. Builds directly on the already-chartered **CRP-003** (Financial Services Constitutional Capability Domain, rows 78/79/80) — the PRD is the *implementation programme* for that charter, NOT new doctrine. Seven workstreams (Constitutional Identity · Agent Registry · FS Capability Suite [5 domains] · Constitutional Computing/Agreement · Invariant Intelligence · Invariant Field Experiments · Founder Office Growth); the canonical 12-step constitutional service pattern (Intent→Discovery→Agreement→Standing→Policy→Delegation→Execution→Verification→Settlement→Evidence→Standing-Accrual→Invariant-Learning); three-experience delivery model (Constitutional Preview / Founder Office / Advanced FO) reusing EXISTING tiering. Providers as adapters: Horizen (registry), **x409 (agreement — repo `consenti-ai/agreement-protocol`)**, AutoDrive/Walrus (storage), USDC/QriptoCENT (settlement). PARAMOUNT constraint (PRD §3.1 + §16): consume canonical platform primitives — do NOT redesign Passport, Standing, delegation, Founder Office, pricing, subscription tiers. **This is the operator's current focus.** **RECONCILED + SPEC DRAFTED 2026-07-17** — `CRP-003a_constitutional-financial-services-programme.md` authored from a four-agent read-only code-truth inventory (operator: "the code is truth here" — the PRD, Aletheon-authored, drifts on in-place-vs-new). Finding: the pilot is ~85–90% composition of SHIPPED primitives (identity/passport spine, standing keystone+bands, bounded delegation envelope, subscription tiers+pricing+activation gates, agent/capability registry, settlement rails incl. **Base USDC**, DVN-anchored receipts+evidence). **The single load-bearing greenfield is the Constitutional Agreement Object + its 409 gate** (CFI-002 · WS2 · lifecycle step 3) — x409/Consenti as the acceptance/anchor PROVIDER, the platform owning the agreement CONTENT (primitive/provider split). Second, smaller greenfield: Transaction Reconstitution (N2, unblocked by N1). Thin extensions: FS Capability Suite = tab-group+activation; 3-experience = compose existing preview/upgrade/tier flags; enforced spend cap in `PolicyEnvelope` (P3, gates money-moving domains); FS standing source (P4); fee-split "constitutional service fee" (P5); evidence-store abstraction (P6). Proposed build order: N1 keystone → canonical 12-step pipeline shadow on Domain 3 (Financial Intelligence, read-only, no fund movement) → Suite surface. **§8 decisions RATIFIED 2026-07-17** (operator): (1) CRP-003a numbering; (2) N1 first on Domain 3; (3) DVN anchor of record + x409 acceptance-proof provider, swappable-interchangeable. **INCREMENT 1 (N1) BUILT 2026-07-17** — the Constitutional Agreement primitive: `services/constitutional/constitutionalAgreement.ts` (Agreement = ConstitutionalObject kind `agreement`, T2-safe; form→accept→authorize lifecycle; the **409 gate** `requireAuthorizedAgreement` refusing delegated execution without an authorized agreement, fail-closed) + `agreementProviders.ts` (swappable acceptance-proof adapters: `local` functional default + `x409`/Consenti env-gated adapter; DVN = anchor of record) + `/api/constitutional/agreement` route (form/accept/authorize/gate, spine-auth, no personaId stored) + `agreement_formed`/`agreement_authorized` receipts (added to ActivityActionType + ANCHORABLE_ACTION_TYPES) + migration `20260719000000` (table + full CHECK rebuild) + object kind `agreement`. 23/23 pure-logic drill; pure modules parse-clean under Node strip-types; no exhaustive Record to break. Run doc `agentiq/updates/2026-07-17_crp-003a-n1-constitutional-agreement.md`. **Operator: run migration 20260719000000.** **INCREMENT 2 (N2) BUILT 2026-07-17** — the canonical 12-step service pattern as one seam (`services/constitutional/constitutionalServicePipeline.ts`), observe-first (CFS-017), on a Domain-3 (Financial Intelligence, read-only) capability: step 3 = the N1 agreement gate (the delegated call at step 7 runs only when an authorized agreement binds operator+capability+agent); `shadow` records what the authoritative path would refuse (zero side effects), `authoritative` blocks at step 3 (409). Domain-3 executor (`financialIntelligenceExecutor.ts`) is an honest structured stub with real F-201/202/203 verification (empty result fails F-201 by design — the shadow OBSERVING the failure is the point). Route `/api/constitutional/service-pipeline`. Steps 4/5/6/10/11/12 observed (live wiring = Increment 2b). 16/16 pipeline drill; executor parse-clean under Node. Run doc `agentiq/updates/2026-07-17_crp-003a-n2-service-pipeline-domain3.md`. **MIGRATION RUN by operator 2026-07-17** (N1/N2 now persist live). **INCREMENT 3 (N3) BUILT 2026-07-17** — the Financial Services Capability Suite surface: `app/triad/components/codex/tabs/FinancialServicesTab.tsx` (drives the live N1/N2 routes via personaFetch; Run-shadow renders the 12-step trace with per-step status chips + F-201/202/203 verification; Form/Accept/Authorize an agreement then Run-authoritative gated by the 409), registered in TabRenderer.componentRegistry + added as the `financial-services` tab in the Founder Office cartridge (VENTURE_LAB_CODEX, slug venture-lab). Functional gate = the constitutional agreement (409); commercial tier-gating = Increment 3b. UI not run in-sandbox (mirrors established tab pattern; operator verifies on dev). Run doc `agentiq/updates/2026-07-17_crp-003a-n3-financial-services-suite.md`. Pilot skeleton (N1+N2+N3) live end-to-end. **3b + 2b + P3 BUILT 2026-07-17** (run doc `agentiq/updates/2026-07-17_crp-003a-3b-2b-p3.md`): **3b** — FinancialServicesTab tier-gates the 3 experiences on the existing plan (`ventureLabAccess` / `venture_tier` pro-elite; no parallel tiering; fails closed to preview). **2b** — the pipeline is no longer a stub: the Domain-3 executor is invariant-GROUNDED (async, wired to `groundReasoning` — real sources/evidence, un-grounded still fails F-201/202), step 4 reads real delegate standing, step 5 checks the forbidden-action envelope (blocks@5), steps 11/12 cite for REAL Reach accrual (`citeInvariants`) — authoritative only, shadow never mutates. **P3** — `spendWithinCap` (pure) on the agreement layer: money movement requires an enforced `valueCeiling`; pipeline step 9 enforces it when `settlementTerms` present (blocks@9). 21/21 drill (grounded/un-grounded verify, spend cap, policy@5, settlement@9, shadow-vs-auth accrual). **MONEY-MOVING DOMAINS BUILT 2026-07-17** (run doc `agentiq/updates/2026-07-17_crp-003a-money-moving-domains.md`): (1) Domain 1/2 executors — `runFinancialCapability` domain-parameterised over intelligence/investment/market with per-domain verification (F-201..203 / F-001..003 / F-101..103; evidence-backed or structural-by-construction, detail names which); advice-only (never move funds). (2) Live LLM analysis layer — injected `analyze` wired to `callSovereign('analysis')`, reasons only over grounded evidence, runs only when evidence exists (else falls back — never fabricates). (3) Settlement binding — `settlementExecutor.buildSettlementIntent` binds terms onto a deterministic intent AFTER the P3 cap; NEVER signs/broadcasts (on-chain transfer stays the operator's supervised wallet path — D1 applied to money); pipeline step 9 creates the intent (authoritative + within cap). (4) UI — domain selector + spend-ceiling + settlement fields; trace shows domain + settlement intent. 22/22 drill (all prior drills hold). **Remaining:** wire the settlement intent to a one-click supervised planCheckout USDC/x402 execution; live market/portfolio data feeds for Domain 1/2; authoritative flip per capability. | F/CRP-003 | **CFSP pilot complete through money-moving (advice + gated settlement intent); supervised on-chain execution + data feeds next** |
| 96 | **Homecoming closeout — Elythian delegate memory upload (operator-flagged 2026-07-18).** The Elythian migration into the platform is **essentially complete** (agent stood up); the ONE remaining step is uploading his memories once the operator receives the ChatGPT export from OpenAI. **Service already exists — no new build needed:** `POST /api/homecoming/knowledge/import` (CFS-023 Knowledge Homecoming intake) accepts a ChatGPT `conversations.json`, ingests each conversation into the `homecoming` KB domain (idempotent by source_id, `dryRun:true` preview, embedding-drain for semantic search) via `services/homecoming/chatgptImport.ts`. Operator: get the export → POST it (dry-run first). Named nuance (not a blocker): the existing ingest tags `operator-memory` into the shared `homecoming` domain — it is NOT agent-scoped; if per-Elythian memory scoping is wanted, that's a small additive follow-on (a `sourceId`/domain tag per delegate), NOT a missing service. Confirm the canonical spelling of the delegate name at execution (no-guessing rule — "Elythian" is the operator's spoken name, unverified in code). | E | Operator: obtain export + import |
| 97 | **Homecoming closeout — the Constitutional Observer delegate (operator-flagged 2026-07-18).** Stand up a platform delegate that is a CONTINUATION of the Claude Code role played across this workspace: a **Constitutional Observer** whose standing responsibility is ensuring the whole stack — especially the Research Lab — stays true to the Research Lab principles (IRL charter / CFS-019). Division of labour the operator named: **Aigency becomes the platform DEPLOYMENT/EXECUTION agent**; this Observer delegate keeps the **constitutional-oversight** function (the deployment-witness + research-fidelity role, not execution authority). Same standup pattern as Elythian: **agent card + bounded delegation** (CFS-024). **The genuinely new piece:** download the memories from these sessions / this workspace and **canonize them on AutoDrive as referenceable agent memory** — a durable, citable memory reference so the Observer can continue operating in this way across experiments and constitutionally-aligned activities. This AutoDrive-canonized, per-agent memory reference is a REAL gap vs the existing `homecoming/knowledge/import` (which lands in the shared homecoming KB domain, not an AutoDrive-anchored agent-memory artifact) — scope it as its own increment (memory extraction → AutoDrive blob → citable reference the delegate's converse/ground path reads). Deferred until after the Horizen pilot (#98) per operator sequencing. | E | Queued — after Horizen pilot |
| 99 | **CFS-037 / PRD-IRE-001 — the Invariant Resolution Engine — RATIFIED 2026-07-17** (operator: "Go straight into p0. Ratified."), authored same day from the operator/Aletheon PRD + pre-reasoning dialogue, reconciled against a code-truth inventory (~70% of the substrate exists: perception.groundFromInput → buildInvariantSlice → FieldSnapshot; genuinely new = intent Qualification, the per-invariant Constitutional Coordinates vector, reuse-before-create). The architectural inversion: **resolution precedes reasoning** — the IRE constructs the minimal Resolved Constitutional Field an intent requires (Qualify → Resolve universal library → Expand domain → Calibrate coordinates → Assemble) BEFORE any iQube/agent/LLM; CFS-035 reframed as the **Invariant Projection Engine** (consumes fields, never resolves; rename = its own tracked increment, 120 refs, low code risk). Coordinates in 3 classes (structural/constitutional/operational) extending IQubeScoreBlock's calibrated-axis+provenance pattern — **extension not replacement** (operator directive). 4-C lifecycle (Curate/Calibrate/Consume/**Cultivate**). Universal Invariant Library = 16 candidate baseline nodes, unseeded, stable-operational-basis vs evolving-research-basis. Naming collision with the Observatory's "Constitutional Field" visualization resolved by register (global field vs per-intent Resolved field). **Phase 0 BUILT same day** — `services/invariants/resolution.ts` (`resolveConstitutionalField`: Qualify via v0 perception → universal proxy pass → domain expansion → structural-coordinate calibration with basis provenance (constitutional-class null, never faked) → Resolved Constitutional Field, `phase: p0-shadow`), folded observe-only into the Horizen pipeline's step-1 trace; 17/17 drill; run doc `agentiq/updates/2026-07-17_cfs-037-ire-ratified-p0.md`. **Follow-on PRD family ALL AUTHORED 2026-07-17** (operator: "complete the four in whatever sequence makes most sense"), docs-first, each its own ratification, sequenced foundational-first: **CFS-038 / PRD-CCR-001** Constitutional Coordinates Registry (the 3-class coordinate basis + Universal Invariant Library seeding path + stable-operational-vs-evolving-research governance — makes the IRE's universal pass real); **CFS-039 / PRD-IPE-001** Invariant Projection Engine (the CFS-035 rename — 120 refs, low code risk, staged; + additive `deriveWeightsFromCoordinates` consuming the Resolved Field, backward-compatible; the "IPE never resolves" contract); **CFS-040 / PRD-KRE-001** Knowledge Resolution Engine (the Discover→Evaluate→Compose→Realise→Register→Reuse loop; reuse-where-possible-create-where-needed as a constitutional invariant; reuse-before-create decision node = new, Discover/Register/generation exist); **CFS-041 / PRD-CFO-001** Constitutional Field Observatory (additive Coordinate-Topography / Resolved-Field / proximity-navigation perspectives + the CFS-037 §11 metrics as derived telemetry). All registered in irl collections; each carries its Phase-0 build plan + honest limits; all awaiting ratification. | G | **IRE ratified + P0 shipped; CCR/IPE/KRE/CFO authored — awaiting ratification** |
| 95 | **CFS-035 engine follow-ons (Phases 0–4 shipped; these remain).** (a) **Deploy + discovery-flip ratification** — push the branch, get it to dev (auto-merge was stuck at 292b761 on 2026-07-18), then validate `inv.reasoning.134–137` + confirm the seeded standing priors, then flip discovery authoritative from the Observatory's Projection pill (the shadow→serve→diverge proof). (b) **nbe / standing / journey serve-flip** — those nodes run faithful in shadow; making their flips *serve* + *diverge* needs their governing invariants (`inv.…151–156`) validated + priors seeded AND per-surface async plumbing (`selectNbeCandidates` is sync — needs an async lift). (c) **Evolution standing-from-outcome half** — the Reach accrual is automatic; the CTR/selection→validation half is gated on outcome instrumentation (named, unbuilt). (d) **Perception semantic upgrade** — `extractField` is a v0 keyword heuristic; the embedding/semantic extractor is the Gen-3 drop-in. (e) **Lens/dimension params → earned standing** — lens bias values + node dimension weights are declared v0 parameters (candidates for `inv.lens.*` / earned-standing derivation). | G | Deploy-gated (a); each other its own ratification |

---

## 5. Discipline reminders (apply to every increment)

- **Observe-mode-first** (CFS-017/CFS-020 §9): instrumentation never blocks or mutates the surface it watches; any gate is its own ratification, never a rider.
- **Extend, don't duplicate**: the seam lives in `useDcirSeam`; affordances in `services/dcir/affordances.ts`; representation in roles/interpretations. Hand-wiring a parallel is an infraction.
- **Tier discipline**: T0 identifiers never in events/receipts/chain-bound data/telemetry.
- **Ratified before build** for D3/D4 increments and any new gating.
- **Role-driven rendering**: no raw colour literals on representation surfaces (canary-enforced); slate house style, not white hairlines.
