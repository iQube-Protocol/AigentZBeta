# Operation Chrysalis — Workstream Tracker

**Living consolidated plan. Single source of truth for what is done, in flight, and pending across the one Chrysalis 2.0 program.** Update as increments land. Last updated 2026-07-12.

**Program of record: `CFS-022` — Operation Chrysalis 2.0: The Constitutional Operating Environment.** That doc is the *why* and the reframe (COE, not IDE) + the five-pillar architecture + the gap assessment + the P0–P4 roadmap; this tracker is the *what-next*. The program folds IRL / DCIR / Atlas / CDE as workstreams (specs unchanged; roadmap consolidated).

Companion / folded specs (authoritative detail lives there):
- `CFS-022` COE program of record · `CFS-015` Chrysalis PRD (mission expanded by CFS-022) · `CFS-016` deployment
- `CFS-019` IRL charter (Research Lab workstream)
- `CFS-020` DCIR charter (interaction substrate workstream)
- `CFS-021` Constitutional Civic Futurism (representation + Bearing Instrument workstream)

## 0. The dependency graph (CFS-022 §2) — the keystone the program turns on

`Chrysalis 2.0 → Constitutional Runtime → Canonical Invariants (done) → Composable Assets (THE MISSING MIDDLE: Object Model + Asset Registry + Composition) → Atlas Plates → Picture Book.` The Atlas is blocked by the missing runtime, not by artwork. Keystone gaps: **G1 Constitutional Object Model** (contract-first, unblocks all) → **G2 Canonical Asset Registry** → **G3 Composition engine** (compose-not-generate) → G4 Publication unification → G5 Workspace/Factory shell. First leverage milestone: **P2 — compose ONE Atlas Plate end-to-end** (proves the machine; the book becomes routine after).

---

## 1. Workstreams at a glance

| Workstream | What it is | State |
|---|---|---|
| **A. CDE / Dev cockpit infra** | The Constitutional Development Environment surfaces + their reliability | Hardening — hang/flicker/env fixes shipping |
| **B. DCIR** | The interaction substrate (observe → recommend → afford). D0–D2 done; D3 in progress; D4 hook adopted on 4 surfaces | Active |
| **C. Constitutional Atlas / Representation** | CFS-021 representation system + the Bearing Instrument navigation primitive | Active |
| **D. Operator / infra** | Env provisioning, canister health, seed ingests — operator-side actions | Open items tracked below |
| **E. Homecoming — Constitutional Agent Sovereignty** | CFS-023 bridge programme; delegates stood up + Constitutional Presence ladder | Active |
| **F. Constitutional primitives (Identity + Artifacts)** | CFS-024 Constitutional Identity Hierarchy (person↔agent binding/assignment) + CFS-025 Artifact Runtime (three consequence tiers; constitutionality earned by promotion) | CFS-024 P0–P1 shipped; CFS-025 Phase 0 shipped, Phase 1/2 ratification-gated |

---

## 2. Shipped this session (2026-07-08 → 07-09)

**A. CDE infra**
- CDE tool viewports hang fix — `withTimeout` on IC/DVN/GitHub/Linear probes; client `personaFetchDeadline` (12s); `getSupabaseAccessToken` bounded (3s, GoTrue-lock hang) → falls through to localStorage.
- CDE pane flicker/stampede fix — non-memoized `onToolUsed` caused an infinite mount-effect loop (→ API stampede → token/key errors). Ref-stabilised each pane + memoised the parent observers.
- CDE route hang guard — `resolvePersonaOrTimeout` (6s) on all six dev-command-center routes → fast honest 503 naming the failing layer instead of a 12s client abort.
- DevTools tabs — Env/Canisters · Telemetry · DVN Pipeline · Escalation, with failure-flag dots.
- Env allowlist — `GITHUB_TOKEN`, `LINEAR_API_KEY`, `GITHUB_REPOSITORY`, `RQH_CANISTER_ID`, `REWARD_HUB_CANISTER_ID` added to `scripts/create-env-production.js` (Amplify console vars only reach the SSR runtime via this allowlist).

**A. House style**
- Corrected `agentiqLiquidGlass` interpretation from the white-hairline residual to the authoritative **slate** house style (hairline `#1E293B`, tint `rgba(15,23,42,0.4)`, no white inset). Codified the canonical surface-styling rule in `CLAUDE.md`.

**B. DCIR**
- D4: migrated the **IRL research copilot** to `useDcirSeam` — the last hand-wired seam. All four originally-named surfaces (DCC, aigentMe, Studio Composer, IRL) now on the one hook.
- D3: exposed the ratified **affordance engine by declaration** through `useDcirSeam` (`affordances` field); migrated the DCC to the single home. Observe-mode, suggest-only, capsule-contained.

**C. Constitutional Atlas**
- **Bearing Instrument v1.0** — `atlas` variant added to `components/representation/BearingInstrument.tsx` (role-driven; compact default + canaries untouched). Canonical anatomy: circular bezel + tick ring, Order/Reasoning/Action octants, Invariant Intelligence / Consequence Engineering poles, needle, six artefact glyphs, HDG/GS/ALT/TRK windows, standing ring + REGISTER marker, five layer labels, parchment (CCF) presentation. Canaries pin the new pure helpers + zero-literal gate.

---

## 3. In flight

| # | Item | Workstream | Notes |
|---|---|---|---|
| 32 | Bearing Instrument v1.0 atlas variant | C | Built + preview sent. Follow-ons: mount it in-app (Atlas hero / dashboard), fidelity polish toward the Breitling render (metal bezel, raised "ears" on the artefacts gauge), literal aviation numerics if wanted. |

---

## 4. Pending / backlog (ratification-gated where noted)

| # | Item | Workstream | Gate |
|---|---|---|---|
| 33 | **DCIR D3 — generic-vocabulary affordance derivations.** `generateAffordances` fires only on DCC dev-loop vocabulary; add derivations from generic event kinds (DocumentCreated / decisions / WorkflowAdvanced / NavigationOccurred) so affordances appear on aigentMe / Studio / IRL, capsule-contained, deduped vs DCC. Then render on ≥1 non-DCC surface. | B | Ratify before build (CFS-020 §87) |
| 34 | **DCIR D4 frontier** — adopt `useDcirSeam` on surfaces beyond the four: Aigent Z (most-integrated), then Marketa, then per-cartridge. | B | Each its own ratification |
| — | **Bearing Instrument** — mount in-app + fidelity polish (see #32). Use as the invariant nav primitive across Constitutional Atlas surfaces. | C | — |
| 35 | **Verify CDE env vars live** post-deploy — `env-check` shows GITHUB_TOKEN / LINEAR_API_KEY / REWARD_HUB_CANISTER_ID `[✓]`. Requires operator to set `REWARD_HUB_CANISTER_ID=lvo2w-jqaaa-aaaas-qc2wa-cai` on the dev Amplify app. | D | Operator |
| 36 | **DVN canister status** — `dfx canister --network ic status sp5ye-2qaaa-aaaao-qkqla-cai` (cycles / PEM / id-mismatch). **Seed ingests** — `inv.representation.121-129` + `inv.interaction.112-118` are `proposed` in the seed crystal; run the ingest to make them live (CFS docs are canon meanwhile). | D | Operator |
| — | DCIR D2 remaining snapshot fields harden against their organs (intent/goals/policies/persona/standing/…); Feedback Coordinator full design (component #12). | B | Ratify before build |
| — | Two Supabase migrations if not yet run: `dev_loop_sessions`, research objects table. | D | Operator |
| 47 | **CFS-025 Artifact Runtime (AR).** One runtime shepherding artifacts across three consequence tiers — disposable / operational / constitutional; constitutionality EARNED by promotion. **Ratified 2026-07-10.** Phase 0 (contract + canary 19/19), Phase 1 (skeleton: classify + tier router + profiles), Phase 2 (IRL `research` pilot: `artifact_published` DVN type + `/api/artifact/produce-research` + receipt-reconciliation adapter) all shipped. **Follow-up:** the 15-call-site ReceiptQube migration + retirement (incremental); anchoring registry `asset.published` is an opt-in (one-line flip) awaiting operator call. | F | Follow-ups only |
| 50 | **CFS-026 Constitutional Publishing Factory (CDS + CPS).** Three products: CCS (standards) · CDS (design system) · CPS (= the Artifact Runtime on document profiles). CDS v0.1 encoded as data + wired into native document production; delegate→AR convergence produces CCS-000 as the first factory output (seeded in the Homecoming workshop). **CFS-026 + CFS-027 RATIFIED 2026-07-12.** Plumbing shipped (plates as canonical assets · numbering registry · renderer map) + R1 plate renderer + artifact persistence (migration run). **R2/R3 (publication shell + derived PDF): SKIPPED per operator (2026-07-12)** — build on future request. IRL-0001 production + live drives: TBD (operator). | F | R2/R3 on request; drives TBD |
| 51 | **CP-008 candidate — the Constitutional Orientation compass (Bearing Instrument family).** Operator direction 2026-07-11: the compass in the v0.9 plate sheet is canonical imagery belonging to the Bearing Instrument (Canonical Asset 001). Decide at CFS-027 ratification: enters the plate set as CP-008, or stays the Bearing Instrument's own canonical rendering. Also from the v0.9 sheet: Hybrid-Intelligence-as-Venn (folded into CP-002 in v1.0 — likely stays derived). | F | Decide at CFS-027 ratification |
| 49 | **CFS-025 follow-up — ReceiptQube retirement (verify-in-place).** The receipt trail is unified (adapter double-write) and the raw-personaId T0 leak in `registry_receipts` is closed. Remaining: move the registry-ingestion READ path onto the unified `activity_receipts` table, then retire `registry_receipts` + drop the double-write. Touches the ingestion UI + money-adjacent paths — MUST be done against a running dev instance driving the ingestion UI, not speculatively. Also: the one-line opt-in to anchor registry `asset.published` (awaiting operator call). | F | Verify-in-place; operator-gated |
| 52 | **Consequential-artifacts plan — ALL 5 INCREMENTS SHIPPED 2026-07-12** (operator: "auto execute this whole plan"). (1) **Standing loop**: non-disposable production accrues standing to the PRODUCING delegate via canonical `accrueStanding` (delegated lane; CVS operational 2 / constitutional 5) with the trust-band ceiling (L2≥20 · L3≥50 · L4≥75 · L5≥100) surfaced on the Homecoming Test. (2) **Studio publish seam**: `POST /api/composition/publish` closes the composeArtifact PUBLISH SEAM — propose⇒operational record, publish⇒ONE anchored `artifact_published` receipt folded into provenance as a projection (engine untouched) + constitutional record. (3) **Business/AgentMe tiering**: `classifyBusinessArtifact` wired additively into create-artifact (gmail draft⇒disposable never persisted; doc/sheet/slides/calendar⇒operational records; constitutional structurally impossible on that path). (4) **Software pilot (D1-safe)**: `POST /api/artifact/produce-software` — the Implementation Pack IS the artifact body; operational tier, no receipt, executes/pushes/deploys NOTHING; `proposeDeployment` returns the documented pointer to the existing D1 route. (5) **Promotion UX**: promote route + Homecoming workshop chips lift a record operational→constitutional (receipt BEFORE lift; constitutional CVS accrues to the producing delegate). **Grant-gate decision RESOLVED 2026-07-12: option (c) — both, + admin accelerator.** Delegation route now dual-gated: grantor reputation (existing) AND, for L3+, the delegate's own server-resolved earned ceiling (`delegateStandingAllowsBand`; L1/L2 stay grantor-only as the bootstrap floor). Admins accelerate a delegate's standing via receipted `POST /api/homecoming/agent/standing` (+CVS 1–50 through the canonical accrual, `approval_granted` receipt first — the passportless-Polity-sponsorship shape) + a `+10` chip in the Homecoming workshop. | F | Done |
| 53 | **Linear lifecycle mirror — SHIPPED 2026-07-12 (operator-ratified).** One-way, best-effort projection of the production cycle into Linear (`services/linear/lifecycleMirror.ts`): issues keyed by a deterministic T2-safe `[AR:<hash12>]` marker on (delegate, profile, brief); phases map to portable Linear state TYPES (intent→backlog · pack→unstarted · produced→started · D1 proposed→started · published→completed); transition comments carry receipt ids/hash prefixes/standing. Wired into implementation-pack, deployment-proposal, produce-software, homecoming produce, promote, and composition publish. Soft-fails until the operator sets a write-scoped `LINEAR_API_KEY` + `LINEAR_TEAM_KEY` in Amplify (both allowlisted; the mirror lists available team keys in its warn when the team is unset). Linear is a MIRROR, never a source of truth — receipts stay canonical; only T2-safe content crosses the external boundary. | F | Operator: set env vars |
| 54 | **CFS-028 Capability Graph & Production Routing — RATIFIED + v1 SHIPPED 2026-07-12.** Producer (harness/model/delegate) × capability (AR profile) graph: contract types, seed graph (harnesses + delegates hand-seeded with stated reasons; model producers derived from the ModelQube registry), pure ranking core with the constitutional standing bar (`CONSTITUTIONAL_MIN_CEILING` = L3) applied to delegates, `GET /api/capability/producers` (admin-gated), routing strip on the Homecoming produce panel. `deployment-execution` edges seeded DORMANT until CFS-016 D2 ratification (operator direction: start with the Claude Code session as executor; server-side + other execution agents join as Producers earning the climb by standing). **Later, separately ratified:** receipt-learned fitness, real cost ingestion, live execution edges. | F | Done (follow-ons §4.5) |
| 55 | **Implementation Pack executed: video-article skill — SHIPPED 2026-07-13** (first live CFS-015 pack → harness → validation → receipts cycle). One skill, two corresponding artifacts: a 24-second video (2 × 12s segments — SkillVideoPlayer's provider cap makes 24s exactly two stitched clips) and a companion article drafted via `callSovereign('draft')` from the SAME invariant-grounded brief, so correspondence is structural. Composes the existing EXP-002 organs (`buildVideoInvariantBrief`, coherence engine, fixed player) — only the article half and the skill seam are new. Receipt plan closed: `artifact_created` on article (plan) + on video (the player's new additive `onCompleted` seam → mode:'video-complete'). Surface: metaMe IRL → "Video + Article" tab. **Pack-quality finding recorded**: the pack ran empty-canon (no invariant bindings, no areas-to-touch, no what-exists inventory) — honest about it (risk 59, low_confidence_knowledge) but all reconnaissance fell to the harness; feed the pack generator the CFS-028 capability graph + seed ingests (#36) so future packs draft areas-to-touch. **Follow-on**: converge the skill's article drafter with the EXISTING `skill:article_generation` (`/api/composer/article-draft` — route-inlined; extract to a shared service both consume). Also fix catalog drift: the catalog's `invokeEndpoint` says `/api/composer/article/generate` but the real route is `/api/composer/article-draft`. | F | Operator drive |
| 56 | **Workflow-gap fix: session findings now travel into pack generation — SHIPPED 2026-07-13.** Root cause (operator screenshots): the DCC's Context Pack (reuse signals), Gap Analysis (existing/missing, 60% reuse, Article Generation `use_directly`), and Consequence Canvas ("never duplicate capabilities") were populated but `ImplementationLayout.generate()` sent only `{ goal, domains }` — the inventory died in the session and the pack shipped "areas to touch: not drafted". Fix: `SessionFindings` threads Layout → route → `generateImplementationPack`; folds into the draft prompt with the anti-duplication mandate; seeds `areasToTouch` deterministically from session-named paths when the draft leaves it empty; echoes onto the pack (`pack.sessionFindings`) and renders as a "What exists vs what is needed" section in the pack markdown. **Regenerated pack verified by operator 2026-07-13**: areas-to-touch drafted from session paths, validation/receipt plans reorganised around the MISSING capabilities. **Delta executed same day**: `services/content/alignmentService.ts` (heuristic per-segment article⇄brief coverage; wired into the video-article plan + article receipt summary) + `services/rendering/optimization.ts` (structural render planning grounded in the real 12s/4-segment/2-3-clips-per-stitch constraints; 24s = one stitch pass) — both surfaced as chips on the Video + Article runner. Remaining pack weakness: empty-canon invariant bindings (seed ingests, #36). | F | Done |
| 57 | **CFS-029 Constitutional Capability Pipeline completion — ADOPTED + core SHIPPED 2026-07-13** (operator's "three things happening" review). (1) **CapabilityEvidence** re-homed from transport object to PERSISTED constitutional primitive (`services/constitutional/capabilityEvidence.ts` leaf + `capability_evidence` table, migration 20260713010000): pack generation persists fresh evidence and reads it back when none supplied — evidence outlives sessions. (2) **Constitutional Decision stage** before every pack draft: nine mechanisms + 'none' (capability exists — compose, build nothing); LLM via router + pure heuristic floor; recorded on the pack with rationale + alternatives; 'none' empties areasToTouch and mandates composition. (3) Renames: pack renders say Constitutional Validation / Constitutional Receipt; Capability Evidence section shows the persisted id. (4) **CS-001 case study** recorded: "Duplicate Capability as Constitutional Drift." `empty-canon` reclassified Constitutional Blindness (resolved same day via seed advancement + namespace widening). **§7 RATIFIED + SHIPPED 2026-07-13**: DCC Decision capsule (first-class ICE stage + /api/constitutional/decision; decision travels verbatim into pack generation) · evidence persistence receipted + ConstitutionalObject builder · freshness policy (7-day window, stale grounds loudly) · CS-001 registered as IRL-0002 (reserved). **Slug migration completed same day** (operator direction): `irl-cartridge` / `irl-dashboard` / `irl-research-copilot` with permanent legacy aliases + defensive DB migration. **Operator: run migrations 20260713010000 + 20260713020000 + 20260713030000.** | F | Migrations + drives |
| 58 | **Dogfood Run 001 — article convergence — SHIPPED 2026-07-13** (first end-to-end run of the completed pipeline on a real goal; D1 operating-history cycle #1). Evidence → Decision (`code`: extract + converge; `none` rejected — the drift was real) → execution: `services/composer/articleDraftService.ts` is the ONE drafting home (pure builders moved verbatim; the article call site migrated OpenAI-direct → `callSovereign`, one #45 site down); the route is a thin wrapper (response shape unchanged); `videoArticleSkill.draftArticleFromBrief` converged onto `draftCompanionMarkdown` (mandate + prompt byte-identical — correspondence contract untouched); catalog `invokeEndpoint` drift fixed. 8/8 seam drill + canary. Run record: `agentiq/updates/2026-07-13_dogfood-run-001-article-convergence.md`. **Canonized 2026-07-13 as CCE-006 — Constitutional Capability Convergence** (first Constitutional Computing Experiment; run renamed CVR-001; experiment taxonomy CCE/CIE/CAE/COE amended into CFS-019; Capability Integrity into CFS-028 §6; evidence split + Constitutional Ratification into CFS-029). **Operator: replay the cycle in the DCC for the receipted twin + propose the D1 deployment (cycle #1 evidence for D2, which explicitly recognizes CCE-006).** | F | Done |
| 59 | **Lab copilot AR/CPS observer wiring + stale-handoff fix — SHIPPED 2026-07-13** (operator report: copilot still handing off to EXP-004 after its run). Three fixes: (1) `/api/research/overview` now carries an `artifactProduction` block (recent AR records + Publication Register, T2-safe) and the IRL Research Copilot folds it into its ground context + renders an "Artifact production (observed)" panel — the observer informs the copilot of production state in its space. (2) Active-experiment default = the EARLIEST experiment below `published` (needs attention), never an arbitrary park. (3) Run-stage hand-off card now states honestly: an unpublished run does not exist constitutionally — publish from the runner tab and the hand-off clears. **Root cause of the stale EXP-004 hand-off is DATA-side: that run was never published to the canonical results store — operator: open the EXP-004 runner and hit Publish.** Canonical rule added to CLAUDE.md: all artifact-producing surfaces use AR/CPS + observer awareness. **Adoption inventory (each its own ratification): Studio Composer (largest gap — productions bypass AR records), Video+Article runner (receipts, no AR records), IRL experiment runners (canonical results, no AR bridge).** AR-native already: Homecoming workshop · Studio publish seam · business artifacts · delegate produce · software pilot. | F | Operator publish + adoption follow-ons |
| 60 | **DCC chain fixes from the CVR-002-candidate pack drive — SHIPPED 2026-07-13** (operator report: no auto-pass to Validate; returning to Implement loses the pack). Five fixes: (1) the generated pack is now SESSION state (`generatedPack` on DevLoopState, persisted with dev_loop_sessions) — the Implement capsule rehydrates it, never forces a regeneration; (2) **auto-pass to Validate**: pack generation satisfies the implementation gate and the loop advances + the Validate capsule mounts, no second click; (3) the Decide strip node, `getStageLabel` entry, and stage-index canary — all missed in the morning's stage insertion (esbuild parse gates don't type-check exhaustive Records; would have failed the Amplify tsc build) — completed; (4) **grounding domain-filter fallback**: a live canon with zero bindings meant the session's relatedCartridges domain filter matched no invariant rows — an empty filtered slice now retries unfiltered (fixes "_none resolved_" + empty preflight on session-driven packs); (5) canary walk updated. Regenerated pack verified by operator: real canon stamp, Constitutional Decision block (mechanism `automation`, three alternatives weighed), persisted evidence id `0bf56eb9`. | F | Done |
| 61 | **Live-drive round-2 fixes: observer reaches the operator's screen — SHIPPED 2026-07-14** (operator screenshots: copilot STILL stuck on EXP-004 after publish; lab hand-off button dead; runner blind to prior runs; DCC advances invisible; Implement still demands regeneration). Five root-caused fixes: (1) **derived-floor clamp in the copilot merge** — the loopExperiments working-object override let a stale session object drag a PUBLISHED experiment back below `published`; working lifecycles now clamp UP to the overview-derived floor (the same rule `overviewWithPersistedLifecycle` applies server-side). (2) **`codex:navigate-tab` listener inside the embed** — the only listener lived in the parent viewer window while tab components dispatch inside the embed IFRAME; CodexPanelDynamic (the embed's tab owner) now listens with the same visible-tab guard + legacy-slug resolution. (3) **EXP-004 runner observer-aware** — mounts with a "Canonical record (observed · AR/CPS)" panel from `/api/experiments/results` (published runs visible on return; refreshes after publish). Persisting UNpublished drill drafts stays a named follow-on. (4) **DCC advances are visible + complete**: `handleAdvanceStage` walks the gate-checked linear prefix PAST the viewed capsule's stage and mounts the destination capsule (gate failure mounts the BLOCKING stage's capsule instead of a dead button); `onPackGenerated` folds the pack's constitutional decision into the session and fast-forwards any parked session to Constitutional Validation (old guard only fired from stage === implementation); `ImplementationLayout` reads `session.generatedPack` EVERY render (initial-only useState missed hydration). Drill 11/11 incl. fork single-step (no runaway walk). (5) **remaining renamed slugs aliased** — `ccrl-experiment-lab/-charter/-protocols/-invariant-field/-invariant-registry` added to `LEGACY_TAB_SLUGS`. | F | Operator re-drive |
| 62 | **CVR-002 (candidate) — Studio Composer adopts AR/CPS + observer awareness — SHIPPED 2026-07-14** (operator: "Yes do this too"; executed evidence-first per the CFS-029 stage order by a delegated agent, verified by the session). `services/composer/studioArtifactTiering.ts` (pure classifier mirroring `businessArtifactTiering`: 9 named production kinds, unknown→disposable fail-safe, constitutional structurally impossible, whitelist-copy T2 body) + additive tiering on the four production routes (image generate: completed→operational multimedia; article draft: real-input-aware operational documentation; video invoke: submission-aware — submitted video doesn't exist yet, disposable; video stitch: the single-shot durable completion seam) + `GET /api/composer/artifact-production` observer route (spine-gated; same projection as research/overview). **Deliberate `none` decisions recorded**: video status poll (no idempotent upsert — would mint a record per poll) and bundle deploy (ExperienceQubes already canonical — double-record = CS-001 class). Canary + 20/20 drills (throwing-store stub proves disposable never touches storage; operational soft-fails). Run record: `agentiq/updates/2026-07-14_cvr-002-studio-composer-ar-adoption.md`. Honest limits: poll-completed videos unrecorded (needs idempotent seam), stitch re-runs can duplicate, Studio UI fold of the observer route deferred (13k-line surface). | F | Done (follow-ons named) |
| 63 | **aigentMe NBE observer-awareness + cockpit Active Work dedupe — SHIPPED 2026-07-14** (operator report on metame.live: after completing the daily brief's recommended actions — venture progress report, review block, claim standing — Move Forward re-recommended the SAME actions as the NBE, and the venture cockpit's Active Work showed the same actions repeated as IN_PROGRESS). The recommender now OBSERVES what its space produced (the AR/CPS rule applied to the NBE engine): (1) `selectNbeCandidates`/`selectTopNbeForCartridge` take `recentlyCompletedNames` — candidates whose label matches a recently completed intent name (exact-normalized or containment ≥10 chars, so LLM-contextualised titles still match their catalog parent) drop out of the pool, with a never-empty guard (all-done → unfiltered set). (2) `buildBrief` + `buildMoveForward` read the persona's completed intents (72h window, tolerant) and BOTH feed the selection filter AND fold a "Recently completed (observed) — do NOT re-recommend" block FIRST into the rerank's liveContext (survives the 600-char cap). (3) Venture cockpit: Active Work dedupes to the most recent row per (intent name, cartridge) — repeated acts no longer stack sibling rows — and Recommended filters BOTH sources (activation catalog + NBE fallback) by completed work with the same guard. Drill 10/10. Follow-on named: intents don't store their source NBE id (matching is name-based); packing the NBE id into the IntentQube extras would make suppression exact. **Delta same day (operator: "the cancelled completed clean up will be very helpful — pls add")**: `sweepSupersededIntents` — intent hygiene that cancels (1) SUPERSEDED live duplicates (an in_progress/awaiting_approval intent with a newer sibling of the same normalized name+cartridge — the newest row is the current state of that action) and (2) STALE live singletons older than 14 days (abandoned work that can never complete itself). Completed/failed/cancelled history is never touched; capped at 20 mutations per run so a backlog drains across a few loads; awaited in `/api/assistant/venture-progress` BEFORE the build so the cockpit's Active Work reflects the swept record in the same response. Drill 8/8 against the real bundled sweep (superseded twins, stale singleton, newest-kept, fresh-kept, history untouched, per-cartridge grouping). | F | Operator re-drive |
| 64 | **IRL-010 Constitutional Runtime Technical Specification v0.1 + IRL-010A Claims Traceability Matrix — AUTHORED 2026-07-14** (first external technical diligence on the IRL papers; Claude Code as *implementation witness* per operator/advisor direction — "inspect the current implementation, do not speculate"). IRL-010: formal definitions quoted from the ACTUAL stored representations (`InvariantRecord` verbatim, Law XII standing/reach, 12 edge types, composition laws, weakest-link manifests, T-tiers, consequence classes) · 14-subsystem architecture inventory with per-subsystem file anchors and ✅/◐/○ status · 10 algorithms as source-traceable pseudocode · experimental methods with the previously unpublished specifics (EXP-003 baseline = cold arm, 5 tasks/18-invariant collection, provider-reported tokens; EXP-001 judge protocol 4×15 + 0/1/2 contradiction scale) and honest n=1/internal-adjudication scoping · **falsification framework** (general statement + H0–H5 prediction/control/disconfirmation table; the reviewer's "prompt scaffold" alternative adopted as the H4 control) · implementation-status table naming what does NOT exist. IRL-010A: every IRL-000/001/002 claim classified I/D/P/F with anchors, ⚑ flags where paper phrasing outruns evidence (each with the precise supportable statement), and an honest register of unevidenced claims. Registered in the IRL foundation collection; run record in agentiq/updates. **Named priority controls (runnable on existing infra): EXP-001 ungrounded arm, EXP-004 scaffold-only arm, EXP-003 multi-run variance.** Possible extraction: Part VI → standalone IRL-003 for the paper series. **Delta 2026-07-14 (operator correction + ratification)**: (1) "identity continuity — planned" corrected to **personhood continuity — implemented** (Law XIII: identity is an optional derivative of individualization); IRL-010 §2.11 added as the formal definition with witnessed anchors. (2) The Law XIII **binding law seeded as `inv.constitutional.130`** (law-typed: "Personhood shall remain continuously individualizable without identity exposure…") alongside constituents 063–067; namespace-widening migration applied by operator; full 142-invariant crystal ingested; **family CANONIZED by operator ratification** (6/6 `canonical`/`document_verified`, verified in the operational store) — the first canonical-status invariants in the substrate, now grounding live reasoning via the canonical slice. DVN anchor of the canonization rides tracker #36. **Delta 2026-07-14b (discoverability → standing)**: operator insight — "it's hard for invariants to achieve standing if they can't be discovered." Confirmed mechanically (proposed invariants are invisible to `buildInvariantSlice` → never cited → never earn reach/standing), and the discovery↔composition gate distinction clarified (grounding injects statements; only `validateComposition` applies composition laws — so the 28 provisional-namespace invariants can be discoverable while their laws stay provisional). Recommendation refined to **advance all-but-`inv.constitutional.127`** (the one self-labeled hypothesis-standing entry). **Grounding-breadth measurement instrument shipped**: `scripts/benchmark-rediscovery.mjs --broad` adds a third arm grounded on the live domain-scoped crystal slice (mirrors buildInvariantSlice ranking) and reports the narrow→broad BREADTH DELTA — the extra rediscovery savings from a richer discoverable crystal. Syntax + summary-math verified on a fixture. Run record scaffold: `exp-003-rediscovery-savings/breadth-arm.md` (preconditions, run cmd, interpretation contract incl. the honest "zero-delta is equally publishable" branch, honest limits). Operator: advance the crystal, then run `--broad` for the measured number → candidate datum for the Austin evidence package + IRL-010A row 1.19. **Run 001 EXECUTED 2026-07-14 (operator, openai/gpt-4o-mini, temp 0, 5 tasks, crystal freshly advanced → 135 validated + 6 canonical, standing≈0):** grounding presence VALIDATED + replicated (narrow cut tokens −12.6%, grounded 65.8%→96.2%, contradictions 3→0; a same-day repeat gave −11.9%, 8→0). Raw breadth **NEGATIVE**: broad slice (24) used +14.7% MORE tokens than the curated 18 for +0.7pt grounded (already at ceiling). Honest reading: the freshly-advanced crystal ranks by confidence, not earned standing, so raw breadth injects less-relevant invariants — **curation beats un-earned breadth at t=0; the flywheel (does an EARNED crystal beat curation?) is the decisive Run-002 re-run after standing accrues.** Instrument honesty fix shipped: the summary's interpretation line was hardcoded-optimistic; now sign-aware (POSITIVE/NEGATIVE/NEUTRAL + grounded-share-fell guard), so the instrument reports the finding it measured. Result recorded in breadth-arm.md Run 001. A null/negative that demonstrates the lab publishes non-confirmatory results — strong for the evidence package. **Delta 2026-07-14c (advisor review of IRL-010 + results, operator-directed "address IRL-010, add IRL-011 as follow-on")**: IRL-010 sharpened — (1) **formal definition** added to §2.1 (`I = (S,E,C,V,P,L)` + Collection/Compose/Retrieve/Evaluate operator signatures) directly answering Austin's "what exactly is an invariant," with the full algebra forward-referenced to IRL-011; (2) **iQube proposition** formalized in §2.4 (`iQube = Intent + CuratedInvariants + ConstitutionalProvenance + Standing`; Retrieve = intent-guided constitutional curation, not RAG) — the operator's keystone insight "we're curating invariants, not information"; (3) **Law XIII chain refined** in §2.11 to `Personhood → Individualization → Standing → Authority → Consequence` with **Identity an off-chain projection** (explains anonymity+accountability coexisting); (4) EXP-003 breadth **reframed curation-dominates-accumulation** + `E=f(G,B,M)` (grounding/breadth/merit orthogonal), making Run 002 a true standing-vs-confidence-retrieval A/B. **IRL-011 (The Computational Model of Invariant Intelligence) opened as OUTLINE/commissioning note** (task #50) — 7-part structure + all formal seeds captured verbatim; proposes for operator ratification IRL Principle 004 (faithful instruments), the "organization before accumulation" emerging law, and EXP-003 → Reasoning Economics rename. Publication streams confirmed: Research (why) / Technical Spec (how) / Experimental Records (evidence). **Delta 2026-07-14d (all three ratified + IRL-011 AUTHORED)**: operator ratified (1) **IRL Principle 004** — instruments must faithfully report outcomes — seeded canonical `inv.epistemology.131` (normative commitment); (2) **emerging law** "knowledge quality scales through organization before accumulation" — seeded `inv.epistemology.132` as a GOVERNING PRINCIPLE with empirical confirmation PENDING EXP-003 Run 002 (deliberately NOT document-verified, preserving falsifiability); (3) **EXP-003 renamed Reasoning Economics** (machine id EXP-003 unchanged). Both invariants in seed (144 total) + appendix-a (same-commit Maintenance rule) — operator: ingest + canonize 131 (132 stays governing-hypothesis until Run 002). **IRL-011 (The Computational Model of Invariant Intelligence) authored v0.1** (outline superseded): 10 sections — formal definitions (invariant tuple, standing/reach axiom, iQube composite, weakest-link), computational primitives (typed operators + pre/post), composition algebra (legality, merit order, breadth-not-free proposition), runtime semantics (small-step intent→consequence + the curation flywheel fixpoint), coherence calculus, complexity + reasoning economics (`E=f(G,B,M)`, minimum-sufficient-substrate `K*`), relationship to statistical inference (vs RAG/ICL/constrained decoding), **model-level falsification (F1–F5, each a runnable experiment)**, ratified principles carried, honest limits. Every claim labelled implemented/partial/proposed-formalism. IRL-010A gains rows 1.21 (iQube curation proposition) + 1.22 (Principle 004). **Delta 2026-07-14e (v0.2 + shareability + flywheel redesign)**: (1) **IRL-011 v0.2** — added inference rules (§3.6), algebraic structure (§3.5: 5 properties + proof sketches — confidence min-semilattice, breadth-cost monotonicity, idempotence-up-to-subsumption, partial commutativity, normative force-preservation), `K*` elevated to centrepiece (§6.3: open problems P1–P5, Bellman/PageRank framing), convergence conjecture C1 + **versioned-crystal + receipted-standing + H1/H2 separation + living-constitution remark** (§4.2–4.5), §7A economics seam (capital/circulation/production/price/market-clearing/anti-inflation), falsifiers extended F6/F7. (2) **Shareability** — all three IRL reports de-personalized (no Austin/reviewer name; substance kept as "external review"); the v0.2 update record + draft note genericized + renamed (reviewer correspondence stays in the operator's direct channel). (3) **"EXP-003 Run 002" DEFERRED + redesigned → EXP-006 Constitutional Knowledge Evolution** (operator + advisor): a standing A/B under uniform standing (=f(timesValidated)=0) is a null-by-construction (recorded per Principle 004); earning standing IS the flywheel → the institute's first LONGITUDINAL series. Charter authored: 006A Accrual/H1 (receipted) → freeze `Crystal v1` → 006B Retrieval/H2/P2/F7 → 006C Φ-convergence/C1/F6 → 006D Transfer/P3 → 006E Version comparison; design invariants freeze-before-measure + receipted standing + independent H1/H2. Numbering: 005 held by Provider Choice → 006 (operator confirms designation). Deferral deliberate — stronger experiment > faster weaker answer. | F | Operator: ingest 131/132; EXP-006 = flagship longitudinal build when resumed |
| 65 | **CVR-003 (candidate) — runArtifact invariant grounding + cited-invariant recording — SHIPPED 2026-07-14** (operator: 'treat "runArtifact invariant grounding + cited-invariant recording" as its own CVR-style increment — lets build it now', after the AR/CPS audit found the composition engine grounded but the runtime blind). The Artifact Runtime is now invariant-aware: (a) `resolveGrounding` — composition-supplied `grounded.invariantIds` win (compose-never-fork), else a live profile-scoped `buildInvariantSlice` (limit 8; research→constitutional/epistemology/reasoning, software→constitutional/engineering, white-paper→constitutional/narrative/epistemology, multimedia→constitutional/style/narrative; default constitutional/engineering), any failure degrades to `none` — grounding informs, never gates; (b) cited ids ride the result (`groundingOf`, the `_evidence`-pattern additive extra), the ConstitutionalObject (payload `groundedInvariantIds`/`groundingSource` + `authority.governingInvariants: ['CFS-025', ...ids]`), and `artifact_records.cited_invariant_ids` (migration 20260714000000, operator-run; pre-migration grounded saves soft-fail); (c) operational + constitutional runs CITE via `citeInvariants` → Reach only (Law XII — Standing stays EXP-006A's receipted design); disposable never grounds, never cites; (d) softwarePilot + homecoming produce thread `citedInvariantIds`; research-overview + composer artifact-production observers project `groundedInvariants` counts. Validation: 6/6 parse gates; 30/30 stub-bundle drill (stub shapes verified against the real grounding module). Honest limits: tiering seams (studio/business) don't ground yet; live fallback is scope-ranked not content-matched; possible double-citation when composition already cited. Run record `updates/2026-07-14_cvr-003-runartifact-invariant-grounding.md`. | F | Done |
| 66 | **DCC Implementation Dispatch — the platform drives implementation — SHIPPED 2026-07-14** (operator: "no button or clear affordance that progresses this to implement — just copy pack… should now be at the stage where we can drive implementation, build and deployment within the platform"). The copy-paste break is closed: (a) `.github/workflows/claude-implement.yml` — `repository_dispatch` receiver running `anthropics/claude-code-action@v1` against the pack (materialized injection-safe via `toJSON()`→file) on an `aigentz/pack-*` branch (refused otherwise — `claude/**` would auto-merge to dev and collapse the D1 gate), opening a PR to dev, never merging; (b) `POST /api/dev-command-center/implement` — admin-gated, deterministic `dispatchBranchFor` (`aigentz/pack-<slug>-<sha8>`), 55k payload ceiling (honest 413, no silent truncation), best-effort `implementation_dispatched` receipt (new DVN-anchorable action type per the permitted-addition rule) — provenance chain pack → dispatch → PR → human merge; (c) `ImplementationLayout` gains the primary "Dispatch to Claude" affordance (single-shot personaFetch — experimentStep's retry could double-fire CI) + D1 propose box suggests the dispatched branch. D1 preserved: initiation is platform-driven + receipted; execution stays human at the PR merge. Validation: 4/4 parse gates, YAML clean, 7/7 branch-minting drill. Operator setup: `ANTHROPIC_API_KEY` repo secret + workflow onto main (repository_dispatch triggers only from the default branch). Run record `updates/2026-07-14_dcc-implementation-dispatch.md`. | F | Done |
| 67 | **In-app PR merge — the D1 human gate exercised in the platform — SHIPPED 2026-07-14** (operator: "Can I merge the PR from within the GitHub viewport… as opposed to having to go to github?"). `POST /api/dev-command-center/github/merge` (admin-gated; open + dev-base PRs only; descriptive merge-commit title per the CLAUDE.md rule; GitHub branch protection/checks still enforce; `deployment_authorized` receipt — merging dev IS authorizing the Amplify deploy) + `GitHubLayout` confirm-then-merge affordance on dev-base PR rows (`head → base` shown; two-click arming; sha + receipt in the result note; eyebrow "read + merge gate"). `_lib/github.ts` stays read-only — the write is route-inlined; `ghOpenPulls` gains `baseRef`. D1: the gate moved surfaces, not authority. Loop now fully in-platform: intent → pack → dispatch → CI implement → PR → in-app merge → deploy. 4/4 parse gates. Addendum in `updates/2026-07-14_dcc-implementation-dispatch.md`. | F | Done |
| 68 | **Merge validation gate + receipted override — validated code deploys — SHIPPED 2026-07-14** (operator-ratified: "we want validated code deployed" + "admin gated override… with a warning and dvn receipt"). Found + closed the missing receipt half: the DCC never wrote `constitutional_validation_recorded` (report lived in session state only) — `handleApproveProposal` now fires it on validation_report approval with verdict/counts/**packId**; validation-record route tags `pack=<id>`. Gate: `aigentz/pack-*` PRs merge only against the admin's own `verdict=pass pack=<match>` receipt (slug parity with `dispatchBranchFor`, drill-pinned); partial/fail blocked; non-pack PRs ungated; blocked → 409 + instructions. Override: reason ≥10 chars → merge + **`validation_override_granted`** receipt (new DVN-anchorable type) naming PR/branch/sha/reason + ⚠ unvalidated-deploy warning; GitHub capsule arms a reason-required override form on the blocked row. 6/6 parse gates, 12/12 drill. Limits: summary-tag correlation (payload field is the long-term home); persona-scoped receipt scan; pre-gate validations untagged. Addendum 2 in `updates/2026-07-14_dcc-implementation-dispatch.md`. | F | Done |
| 69 | **Dedicated validation runner + Deploy-Auth-hosted merge + descriptive auto-merge — SHIPPED 2026-07-14.** (a) Validate chat turns die at Amplify's hard ~30s SSR ceiling (`maxDuration` not honored — amplify-hosting#3475/#3223); the mega-prompt + longest generation can't fit. Fix: `POST /api/dev-command-center/validate` — one compact invariant-routed inference (`callSovereign('validation')`, ModelQube + sovereign ladder), canvas + 6k-bounded brief, honest unresolved-when-silent, returns a standard `validation_report` proposal into the EXISTING approve→receipt→merge-gate path; Validate capsule gains "Run validation". (b) Deployment Authorization capsule now hosts the pack-PR merge (operator: the final Deploy Auth stage is where the PR is actually merged/deployed) — lists open `aigentz/pack-*` PRs (session's pack preferred by slug), same merge route/gate/override as the GitHub capsule. (c) `merge-claude-to-dev.yml` fallback merge now titles itself branch + head subject — the forbidden `--no-edit` generic descriptor is gone from the Amplify deploy history. Canonical sequence: pack → dispatch → validate (approve report) → [remediate] → Deploy Auth: authorize + merge → Amplify deploys. Follow-on: validate against the PR's actual diff, not just the brief. | F | Done |
| 70 | **Dedicated remediation runner + guide-only auto-turns — SHIPPED 2026-07-14** (operator stranded at Remediation live: validation runner worked, report approved, loop advanced — then the auto-fired "produce the remediation_plan" chat turn died at the same Amplify ~30s ceiling; no button, no way to progress). Same cure as validation: `POST /api/dev-command-center/remediate` — one compact invariant-routed inference (`callSovereign('analysis')`), remedy + captured lesson per failed/partial/unresolved item, `revalidationRequired` defaults true (safe constitutional default), returns a standard `remediation_plan` proposal into the existing approve→record→re-validate path; Remediation capsule gains "Generate remediation plan" (sends everything not satisfied, not just the fork's high/critical). Auto-turns for the two heavy stages are now GUIDE-ONLY (2-3 sentences, explicitly no fence — the capsule button produces it), so the observed transition narrates reliably instead of dying. 3/3 parse gates. | F | Done |
| 71 | **The correction arc — remediation becomes code, validation judges the PR — SHIPPED 2026-07-15** (operator: "how would it resolve it if it doesn't implement and go back and write the code and redeploy for validation? … caught in a loop around remediation that is observing without correcting"). Confirmed live: PR #89 was already merged (operator, 13:50Z) and no remediation dispatch existed — the loop specified remedies but never implemented them, so re-validation re-failed the same build. Three-part close: (a) `claude-implement.yml` gains re-dispatch continuity — an existing `aigentz/pack-*` branch is checked out and AMENDED (never recreated from dev), and Claude updates the open PR (or opens a fresh one if the previous merged) instead of failing on `gh pr create`; (b) Remediation capsule gains **"Dispatch remedies to Claude"** — the approved plan travels as a remediation brief through the same dispatch route/branch (receipted `implementation_dispatched`); (c) the validation runner is **PR-aware** — given the pack id it folds the dispatched PR's body + changed-file list into the summary, so re-validation judges the ACTUAL amended work, not the stale brief. Canonical closed loop: Implement (pack + dispatch) → Validate → Remediate (plan → dispatch remedies) → CI amends the PR → re-Validate (PR-aware) → Deploy Auth (merge) → Amplify deploys. 3/3 parse gates + YAML. Operator setup: sync the updated workflow to main (repository_dispatch runs from the default branch). | F | Done |
| 72 | **Correction-arc first live run + receipt-constraint fix + flow-through UX + fast validation routing — SHIPPED 2026-07-15.** Live evidence: remediation dispatch run 7 SUCCEEDED → **PR #90** ("Video+Article remediation: Studio service integration + marketer/creator UX", both remedies additive, 32 tests) opened on the SAME pack branch — the closed loop works end-to-end; run 8 (double-click) collided harmlessly. Fixes shipped: (a) **migration 20260715000000** — the activity_receipts action_type CHECK had drifted since 2026-07-03; every dev-loop receipt since (incl. the merge gate's `constitutional_validation_recorded`) silently failed; rebuilt with the complete union (operator ran it; receipts now persist — dispatch receipt 994b09e3 confirmed); (b) chat prompt slim — `aigentMe` (empty-scaffold) KB search skipped, reclaiming up to 15s of the ~30s ceiling on every DCC/metaMe turn; (c) validate-runner 504 fix — validation stage default rerouted sonnet→haiku (mechanical checking; ModelQube/env override available) + maxTokens 2400→2000; (d) flow-through UX (operator direction): successful remedies dispatch pulses the Validate chip, deep-links into the Validate capsule, and fires a guide-only left-pane turn narrating the hand-off (CI ETA → Run validation → approve → Deploy Auth merge). 4/4 parse gates. | F | Done |
| 73 | **First fully-closed constitutional dev loop, live — PR #90 merged 2026-07-15** + stage→capsule reconciliation safety net. Operator drove the complete arc end to end: pack → dispatch (PR #89, merged) → validate (fail) → remediate (plan generated + approved) → dispatch remedies → CI amended the SAME pack branch → **PR #90** ("Video+Article remediation: Studio service integration + marketer/creator UX", 32 tests, additive) → re-validate → **merged to dev by the operator** — the loop this session built end to end actually shipped code. Gap found in the same drive: "when validation clears, Deploy capsule is not auto-opened." Fix: a decoupled `useEffect` in `DevCommandCenterTab` watching `session.stage` transitions — independent of the click-handler flow-through in `handleApproveProposal`/`handleAdvanceStage`, it reconciles the active capsule (+ re-pulses its chip) to whatever stage the session actually reaches, so the pane follows the loop even if a future stage-advancing path forgets the inline call. 1/1 parse gate. | F | Done |
| 74 | **Linear mirror: silent-failure fix + full dev-loop lifecycle wiring — SHIPPED 2026-07-15** (operator: "Linear tool is not showing any of the project work… Tool is working but no current project tasks being picked up"). Root cause: the pack-generation route already called `mirrorLifecycleToLinear` (phase `pack_generated`), but (a) its `linear` result was silently dropped by `ImplementationLayout` — if `LINEAR_TEAM_KEY` were unset or wrong, the mirror would soft-fail with zero operator-visible signal, breaking the honest-degradation convention every other CDE tool follows; (b) the dispatch and merge stages never called the mirror at all, so a shipped/merged pack's issue (if created) froze at "Todo" forever. Fixed: keying switched from free-text `goal` to **`packSlug(packId)`** (stable across a remediation redispatch, whose goal text differs — drill-confirmed 3-stage convergence); `implement/route.ts` now mirrors phase `artifact_produced` on successful dispatch; `github/merge/route.ts` now mirrors phase `published` (or `deployment_proposed` on an override) on successful merge of a pack PR; all three routes return `linear` in their JSON, and `ImplementationLayout`/`GitHubLayout`/`DeploymentAuthorizationLayout` now render it — success or failure, never silent. Canonical lifecycle: pack generated (Todo) → dispatched (In Progress) → merged (Done), one Linear issue per pack, tracked end to end. 6/6 parse gates, 3/3 convergence drill. Operator: if the note reads "Linear not tracking this pack: LINEAR_TEAM_KEY not set — available: …", set `LINEAR_TEAM_KEY` in Amplify to one of the listed team keys. | F | Done |
| 75 | **CFS-030 Constitutional Reconciliation + CCE-007 canonization — DRAFTED 2026-07-15, awaiting ratification.** Alethean's review of the dev-cycle-through-remediation drive proposed formalizing "Constitutional Reconciliation" as a primitive and canonizing today's cycle as an experiment. Numbering correction made before drafting (CS-001-class near-miss avoided): Alethean's proposed CFS-028 "Capability Evidence" + CFS-029 "Constitutional Decision" ALREADY EXIST under those exact numbers — CFS-029 (ratified 2026-07-13) already covers both; CFS-028 is already a different spec (Capability Graph Production Routing). "CCE-005" collides with the reserved slot (EXP-005 Provider Choice); the article-convergence dogfood run is already canonized as CCE-006 with near-identical framing to Alethean's independent wording. CFS-030 (genuinely free) drafted for the validate→remediate→dispatch-remedies→CI-amends→revalidate loop built 2026-07-14/15 (branch-continuity in `claude-implement.yml`, the Remediation capsule's dispatch button, PR-aware validation) — §3 names the generalization beyond code (registry/docs/ontology/schemas/VentureQubes) as unratified design, not built. CCE-007 drafted as a distinct SECOND experiment from CCE-006 (video/article skill's PR#89→validate-fail→remediate→PR#90→revalidate→merge cycle, driven by platform infrastructure rather than session narration), cross-referencing CCE-006, with an honest-limits section (N=1, no independent adversarial review of the remedy quality, no fresh execution of the amended code). Both files + collections.json registration pushed as candidates; ratification pending operator review. | F | Done |
| 76 | **CFS-030 + CCE-007 RATIFIED; three doctrinal amendments + CHRYSALIS_HISTORY.md created — 2026-07-15** (operator: "your corrections are completely good and stand accordingly… some nuggets here as well that might also be worth canonizing," relaying Alethean's second review). CFS-030 (Constitutional Reconciliation) and CCE-007 flipped from candidate to ratified/canonized. Triaged Alethean's second round the same way as the first — most points already canonical (CFS-029 pipeline; CFS-022's "constitutional operating environment, not IDE" already ratified 2026-07-09, near-verbatim; CFS-019's recursive mandate). Three genuine new nuggets recorded as amendments: (1) **Orchestration vs. Sovereignty** (CFS-018 amendment) — CCE-006/007 demonstrate constitutional ORCHESTRATION (memory, reasoning, governance, routing, evidence going platform-native); the sovereignty bundle's dimensions (`inv.sovereignty.10x`) are unmoved — inference still runs on frontier providers; naming this precisely prevents overclaiming. (2) **Constitutional Coherence — six recurring questions** (CFS-022 amendment) — what-exists / what-authority / what-capability / what-consequence / what-evidence / what-standing, mapped to the eight-facet object model + this session's own build as worked examples; descriptive, no new enforcement. (3) **The Lab↔Platform feedback loop, made explicit** (CFS-019 amendment) — the return path (Platform → Research Lab) CCE-006/007 make concrete for the first time, with an honest caveat that the loop is not yet automatic (an operator/witness still corrects numbering before ratification — this exact triage is the proof). Created `CHRYSALIS_HISTORY.md` (new document type, distinct from this tracker: narrative WHY, not engineering WHAT) with its first entry, Milestone 001, adapting Alethean's proposed historical paragraph with full honest-limits carried forward. All five files registered in `codexes/packs/irl/collections.json`. | F | Done |
| 77 | **CFS-031 The Constitutional Cybernetic Loop — RATIFIED 2026-07-15** (operator: "Some points from Alethean and I for reference and ratification," a macro synthesis following CCE-006/007). New foundational spec (next free number after CFS-030) formalizing: (1) **the deepened Constitutional Computing definition** — a cybernetic system where every action produces evidence improving both code and constitution, via a two-rate model (fast loop = code, intentionally high-variance; slow loop = constitution, intentionally low-variance); (2) **Standing as the membrane** carrying evidence from fast to slow loop (Action→Evidence→Standing→Confidence→Invariant Candidate→Ratification→Constitution) — naming a shape already scattered across delegate/invariant/object-model standing, not a new mechanism; (3) **Reconstitution** — the convergence half of CFS-030's cycle, the Chrysalis metaphor's stronger reading (continuous metamorphosis, no final form, not episodic versioning); (4) a **macro market↔research cybernetic loop** (Founder Office → Horizen → Verification → Research → Platform → Market) ratified explicitly as ARCHITECTURAL VISION, not delivered — Horizen has zero prior codebase footprint, Founder Office/Operation Leap remain deferred per CFS-015/023, checked before drafting to avoid the CS-001 defect class at the vision scale; (5) three hypothesis sources (market/constitutional/community-led) converging on the existing CFS-019 pipeline — only constitutional-led is exercised; (6) **Signals vs. Hypotheses** — vocabulary only, explicitly not yet a seeded invariant. Glossary amended: deepened Constitutional Computing entry + new entries (Reconstitution, Constitutional Coherence — merging last session's structural six-questions reading with this session's dynamic reading, Standing — previously undefined despite heavy use, Signal, Hypothesis). CHRYSALIS_HISTORY.md gained Milestone 002. Honest-limits sections in both the spec and the history entry are explicit about what's proven vs. envisioned. | F | Done |
| 48 | **CFS-024 Delegation consolidation — SHIPPED 2026-07-10.** Persona-first Delegation tab + persisted per-persona assignments (`persona_agent_assignments`, many delegates / one aigentMe / reassignable) + Wallet reflection (aigentMe star single-sourced from the resolver). Cross-persona aigentMe-creation fixed. Migration run. Follow-up: filter agent-personas out of the "Delegating as" selector (needs a reliable agent-persona marker). | F | Done (1 minor follow-up) |

---

## 5. Discipline reminders (apply to every increment)

- **Observe-mode-first** (CFS-017/CFS-020 §9): instrumentation never blocks or mutates the surface it watches; any gate is its own ratification, never a rider.
- **Extend, don't duplicate**: the seam lives in `useDcirSeam`; affordances in `services/dcir/affordances.ts`; representation in roles/interpretations. Hand-wiring a parallel is an infraction.
- **Tier discipline**: T0 identifiers never in events/receipts/chain-bound data/telemetry.
- **Ratified before build** for D3/D4 increments and any new gating.
- **Role-driven rendering**: no raw colour literals on representation surfaces (canary-enforced); slate house style, not white hairlines.
