# Runtime Specs — corpus area (Smart Wallet / Menu / Content)

**Discovery-Engine sub-domain:** `finance/qriptocent/runtime` (scope level: capability, PRD-MPY-001 §3.3).

## What belongs here

The QriptoCENT runtime source: the Smart Wallet, Menu, and Content runtime specifications — how QriptoCENT value is surfaced and transacted at runtime.

## Source status — OPERATOR-TO-PROVIDE (D2)

**Partial:** Smart Wallet surfaces exist in code (`SmartWalletDrawer`, `app/(shell)/moneypenny/`). The Menu and Content runtime specifications are **operator-to-provide via the Invariant Discovery Engine (D2) — not yet ingested; no source invented in the interim.**

Derived invariants (`inv.finance.*`) enter at status `proposed` only, never auto-canonical (`inv.reasoning.337`). This file is a structural placeholder — no fabricated runtime specification is written here.
