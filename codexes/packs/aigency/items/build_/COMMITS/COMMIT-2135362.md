# Commit Brief: `2135362` — Fix the classify-provenance 15s timeout N+1, and put the cohort action directly in Research Copilot

| Field | Value |
|-------|-------|
| SHA | [`2135362`](https://github.com/iQube-Protocol/AigentZBeta/commit/2135362985afabc11a38114fa5076b65c9e4b1ce) |
| Author | Claude |
| Date | 2026-09-04T16:38:24Z |
| Branch | dev (direct push) |
| Type | `fix` |
| Repo | iQube-Protocol/AigentZBeta |

## Commit Message

```
Fix the classify-provenance 15s timeout N+1, and put the cohort action directly in Research Copilot

Root cause of the 15s "programme state composition" timeout: the exception-
only-remainder feature added 2026-09-03 made triageUnclassifiedProvenance
call suggestClassification once PER unclassified invariant (53+ sequential
Supabase round-trips) inside loadTrack2ProgrammeState's hot path, which runs
on every Track 2 read. services/research/provenanceCohortPreparation.ts now
batches this into ONE discovery_evidence query across every invariant, with
the same deterministic no-evidence/incomplete-lineage/repo-internal-citation
triage output — verified against a live count (58 successor-scoped promoted
invariants, 53 currently unclassified at investigation time, matching the
reported 55 minus 2 the operator had already classified manually via the
per-record queue before reporting the timeout).

components/composer/IRLResearchCopilotTab.tsx: the Copilot's own remedy text
already pointed at GET .../provenance-cohort, but clicking through still
landed on Track2ProgrammePanel's per-record ClassificationQueue. Adds the
Copilot's own "Prepared — Classify Provenance" cohort card — total/ready/
exception counts, one rationale field, one "Ratify provenance cohort"
button — fetched and ratified through the SAME lightweight
/provenance-cohort route, fetched DIRECTLY (gated on
pendingDecision.stageId === 'classify-provenance') rather than derived from
the heavy full-composition read, so this bounded action's own availability
never depends on the 15s budget that was blocking it. On success, continues
the programme automatically via the existing runProgramme ("Run until you
need me") path — Validate runs inside the ratification route itself;
Relationships/Assignment proceed on the next run because they remain
genuine per-record Steward judgments, same as every other Track 2 stage.

Tests: batched triageUnclassifiedProvenance verified to make exactly ONE
discovery_evidence call regardless of invariant count (was N); the Copilot
card's fetch/ratify/render wiring; the card is a sibling of, never nested
inside, the existing decision-block span
(tests/track2-copilot-deep-link.test.ts's own guard against a prior
onOpenDetail-inside-decision-block regression stays green, unmodified).

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01S5Y1pnDdW3LyguwPdfJjXy
```

## Body

Root cause of the 15s "programme state composition" timeout: the exception-
only-remainder feature added 2026-09-03 made triageUnclassifiedProvenance
call suggestClassification once PER unclassified invariant (53+ sequential
Supabase round-trips) inside loadTrack2ProgrammeState's hot path, which runs
on every Track 2 read. services/research/provenanceCohortPreparation.ts now
batches this into ONE discovery_evidence query across every invariant, with
the same deterministic no-evidence/incomplete-lineage/repo-internal-citation
triage output — verified against a live count (58 successor-scoped promoted
invariants, 53 currently unclassified at investigation time, matching the
reported 55 minus 2 the operator had already classified manually via the
per-record queue before reporting the timeout).

components/composer/IRLResearchCopilotTab.tsx: the Copilot's own remedy text
already pointed at GET .../provenance-cohort, but clicking through still
landed on Track2ProgrammePanel's per-record ClassificationQueue. Adds the
Copilot's own "Prepared — Classify Provenance" cohort card — total/ready/
exception counts, one rationale field, one "Ratify provenance cohort"
button — fetched and ratified through the SAME lightweight
/provenance-cohort route, fetched DIRECTLY (gated on
pendingDecision.stageId === 'classify-provenance') rather than derived from
the heavy full-composition read, so this bounded action's own availability
never depends on the 15s budget that was blocking it. On success, continues
the programme automatically via the existing runProgramme ("Run until you
need me") path — Validate runs inside the ratification route itself;
Relationships/Assignment proceed on the next run because they remain
genuine per-record Steward judgments, same as every other Track 2 stage.

Tests: batched triageUnclassifiedProvenance verified to make exactly ONE
discovery_evidence call regardless of invariant count (was N); the Copilot
card's fetch/ratify/render wiring; the card is a sibling of, never nested
inside, the existing decision-block span
(tests/track2-copilot-deep-link.test.ts's own guard against a prior
onOpenDetail-inside-decision-block regression stays green, unmodified).

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01S5Y1pnDdW3LyguwPdfJjXy

## Files Changed

| Change | File |
|--------|------|
| Modified | `components/composer/IRLResearchCopilotTab.tsx` |
| Modified | `services/research/provenanceCohortPreparation.ts` |
| Added | `tests/provenance-cohort-copilot-card.test.ts` |
| Modified | `tests/provenance-cohort-preparation.test.ts` |

## Stats

 4 files changed, 558 insertions(+), 44 deletions(-)
