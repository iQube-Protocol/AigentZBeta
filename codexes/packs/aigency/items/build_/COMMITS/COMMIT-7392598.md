# Commit Brief: `7392598` — CFS-035 Phase 3: Field Extractor (Perception layer) v0 seam

| Field | Value |
|-------|-------|
| SHA | [`7392598`](https://github.com/iQube-Protocol/AigentZBeta/commit/73925989a9e217a63c6b19f3b7a48341162dafde) |
| Author | Claude |
| Date | 2026-07-16T23:01:14Z |
| Branch | dev (direct push) |
| Type | `push` |
| Repo | iQube-Protocol/AigentZBeta |

## Commit Message

```
CFS-035 Phase 3: Field Extractor (Perception layer) v0 seam

The one genuinely-new engine component (cognitive layer 1 / §9): estimate which
region of the constitutional field an arbitrary input activates, so grounding can
be DERIVED from raw content rather than a hand-specified context.

- services/invariants/perception.ts: extractField (v0, deterministic keyword
  overlap against a curated domain vocabulary) + groundFromInput (perception →
  Field Snapshot) + perceiveSlice. Guarded; empty match → unscoped fallback so a
  caller is never left empty. Verified: discovery/standing/epistemology inputs
  extract correctly; off-topic input returns empty.
- SCOPE DISCIPLINE (the plan's guard against over-investing): v0 is deliberately
  a cheap no-LLM heuristic; the inference-heavy semantic/embedding extractor is
  the Gen-3 follow-on the seam is designed to accept as a drop-in replacement of
  extractField — not a new integration.
- Canonize inv.reasoning.158 (Perception principle), proposed — the newest,
  least-proven layer. + appendix.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Y98FMjkM8zUDsYqJikzsMB
```

## Body

The one genuinely-new engine component (cognitive layer 1 / §9): estimate which
region of the constitutional field an arbitrary input activates, so grounding can
be DERIVED from raw content rather than a hand-specified context.

- services/invariants/perception.ts: extractField (v0, deterministic keyword
  overlap against a curated domain vocabulary) + groundFromInput (perception →
  Field Snapshot) + perceiveSlice. Guarded; empty match → unscoped fallback so a
  caller is never left empty. Verified: discovery/standing/epistemology inputs
  extract correctly; off-topic input returns empty.
- SCOPE DISCIPLINE (the plan's guard against over-investing): v0 is deliberately
  a cheap no-LLM heuristic; the inference-heavy semantic/embedding extractor is
  the Gen-3 follow-on the seam is designed to accept as a drop-in replacement of
  extractField — not a new integration.
- Canonize inv.reasoning.158 (Perception principle), proposed — the newest,
  least-proven layer. + appendix.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Y98FMjkM8zUDsYqJikzsMB

## Files Changed

| Change | File |
|--------|------|
| Modified | `codexes/packs/irl/foundation/appendix-a_canonical-invariants.md` |
| Modified | `codexes/packs/irl/foundation/canonical-invariants.seed.json` |
| Added | `services/invariants/perception.ts` |

## Stats

 3 files changed, 121 insertions(+)
