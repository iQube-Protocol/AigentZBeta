# Commit Brief: `4021791` — Remove broken batch effect, fix scroll-to-top for embed preview

| Field | Value |
|-------|-------|
| SHA | [`4021791`](https://github.com/iQube-Protocol/AigentZBeta/commit/4021791494d4ae68bde04388e0285fca37d06cee) |
| Author | Claude |
| Date | 2026-03-23T02:25:02Z |
| Branch | dev (direct push) |
| Type | `fix` |
| Repo | iQube-Protocol/AigentZBeta |

## Commit Message

```
Remove broken batch effect, fix scroll-to-top for embed preview

- Remove the batch video status check effect; it was calling
  setConfirmedCompleteGenerationId for every proxy-video experience,
  clobbering the state with a non-selected experience's genId and
  blocking the polling effect via completedVideoGenerationIds; the
  polling effect (unchanged) remains the sole authority on banner/
  video player state and correctly handles the selected experience

- Add data-embed-panel attribute to the ExperienceQube Runtime panel div
  in buildRuntimeCapsulePanel; add a useEffect that after 150ms scrolls
  this panel into view at the top, overriding CopilotKit's scrollToBottom
  which was landing on the thumbnail carousel instead of the hero content

https://claude.ai/code/session_01VcE6pnjSeAtYvhau1Q6GVM
```

## Files Changed

_File details not available in backfill — see commit link above._
