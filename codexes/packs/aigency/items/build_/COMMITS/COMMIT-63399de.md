# Commit Brief: `63399de` — build IRE Phase 0 (CFS-037): shadow resolution seam + Horizen pipeline observer

| Field | Value |
|-------|-------|
| SHA | [`63399de`](https://github.com/iQube-Protocol/AigentZBeta/commit/63399dec9785a4eddd31d0d4acea41d34551abdf) |
| Author | Claude |
| Date | 2026-07-17T17:00:38Z |
| Branch | dev (direct push) |
| Type | `push` |
| Repo | iQube-Protocol/AigentZBeta |

## Commit Message

```
build IRE Phase 0 (CFS-037): shadow resolution seam + Horizen pipeline observer

services/invariants/resolution.ts — the Invariant Resolution Engine, Phase 0:
resolveConstitutionalField(intent) composes the five phases — Qualify (v0
perception.extractField), Resolve (universal pass grounding in the
constitutional/epistemology proxy namespaces until the 16-node Universal
Invariant Library seeds — honest, named), Expand (perceived domains), Calibrate
(per-invariant STRUCTURAL coordinates derived from seeded axes
confidence/standing/reach with basis provenance — the IQubeScoreBlock pattern
lifted; constitutional-class coordinates carried as null, never faked;
field-level OPERATIONAL coordinates coverage/reuse/time-to-value with named
proxy bases), Assemble (ResolvedConstitutionalField extending FieldSnapshot,
phase marker p0-shadow). Read-only, gates nothing (CFS-017); DB failure
degrades to null-snapshot with qualification intact.

Horizen pipeline: optional resolveField dep (default wired) folds the IRE
trace line into step 1 — the first proving ground, observe-only.

Verified: 17/17 drill (library shape; qualification extracts the
ranking/standing/governance region + honest empty on nonsense; structural
calibration values+bases+clamps+null constitutional; operational
coverage/reuse/ttv incl. empty-field zeros; confidence weighting/bounds;
trace line).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Y98FMjkM8zUDsYqJikzsMB
```

## Body

services/invariants/resolution.ts — the Invariant Resolution Engine, Phase 0:
resolveConstitutionalField(intent) composes the five phases — Qualify (v0
perception.extractField), Resolve (universal pass grounding in the
constitutional/epistemology proxy namespaces until the 16-node Universal
Invariant Library seeds — honest, named), Expand (perceived domains), Calibrate
(per-invariant STRUCTURAL coordinates derived from seeded axes
confidence/standing/reach with basis provenance — the IQubeScoreBlock pattern
lifted; constitutional-class coordinates carried as null, never faked;
field-level OPERATIONAL coordinates coverage/reuse/time-to-value with named
proxy bases), Assemble (ResolvedConstitutionalField extending FieldSnapshot,
phase marker p0-shadow). Read-only, gates nothing (CFS-017); DB failure
degrades to null-snapshot with qualification intact.

Horizen pipeline: optional resolveField dep (default wired) folds the IRE
trace line into step 1 — the first proving ground, observe-only.

Verified: 17/17 drill (library shape; qualification extracts the
ranking/standing/governance region + honest empty on nonsense; structural
calibration values+bases+clamps+null constitutional; operational
coverage/reuse/ttv incl. empty-field zeros; confidence weighting/bounds;
trace line).

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01Y98FMjkM8zUDsYqJikzsMB

## Files Changed

| Change | File |
|--------|------|
| Modified | `services/constitutional/constitutionalServicePipeline.ts` |
| Added | `services/invariants/resolution.ts` |

## Stats

 2 files changed, 297 insertions(+), 1 deletion(-)
