# Commit Brief: `7379156` — Give DVN attestation processing scheduled liveness, secure its two mutation routes

| Field | Value |
|-------|-------|
| SHA | [`7379156`](https://github.com/iQube-Protocol/AigentZBeta/commit/737915672d0a5061832ec3710a4efbf48d137e9e) |
| Author | Claude |
| Date | 2026-08-09T19:51:43Z |
| Branch | dev (direct push) |
| Type | `push` |
| Repo | iQube-Protocol/AigentZBeta |

## Commit Message

```
Give DVN attestation processing scheduled liveness, secure its two mutation routes

Extract submit_attestation logic from the layerzero/process route into
services/ops/dvnAttestationProcessor.ts so it can be called by both the
operator UI and a new cron-driven scheduler route
(/api/ops/dvn/attestation-processor-cron), which decides liveness from DVN
pending count rather than the unrelated PoS reconciler. Add dual
cron-token/admin-persona auth (services/ops/opsAuth.ts) to
layerzero/process and dvn/attest, which previously had none, and switch
their two client call sites to personaFetch. New workflow is
workflow_dispatch-only pending a confirmed bounded pass against the live
canister (Horizen Pilot Closure Part B2/B3).

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01VKSCjcikJZkkibzBctiun7
```

## Body

Extract submit_attestation logic from the layerzero/process route into
services/ops/dvnAttestationProcessor.ts so it can be called by both the
operator UI and a new cron-driven scheduler route
(/api/ops/dvn/attestation-processor-cron), which decides liveness from DVN
pending count rather than the unrelated PoS reconciler. Add dual
cron-token/admin-persona auth (services/ops/opsAuth.ts) to
layerzero/process and dvn/attest, which previously had none, and switch
their two client call sites to personaFetch. New workflow is
workflow_dispatch-only pending a confirmed bounded pass against the live
canister (Horizen Pilot Closure Part B2/B3).

Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01VKSCjcikJZkkibzBctiun7

## Files Changed

| Change | File |
|--------|------|
| Added | `.github/workflows/dvn-attestation-processor.yml` |
| Modified | `app/(shell)/ops/page.tsx` |
| Modified | `app/api/ops/dvn/attest/route.ts` |
| Added | `app/api/ops/dvn/attestation-processor-cron/route.ts` |
| Modified | `app/api/ops/layerzero/process/route.ts` |
| Modified | `hooks/ops/useSyncStatus.ts` |
| Added | `services/ops/dvnAttestationProcessor.ts` |
| Added | `services/ops/opsAuth.ts` |
| Added | `tests/dvn-attestation-processor-liveness.test.ts` |
| Modified | `tests/layerzero-process-result-truthfulness.test.ts` |
| Added | `tests/ops-dvn-mutation-routes-auth.test.ts` |

## Stats

 11 files changed, 740 insertions(+), 175 deletions(-)
