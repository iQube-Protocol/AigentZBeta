# Netlify configuration for AigentZBeta monorepo
# This builds the thin client from apps/theqriptopian-web

[build]
  base = "/"
  command = "echo 'VITE_API_URL='$VITE_API_URL && echo 'VITE_AIGENTIQ_API_URL='$VITE_AIGENTIQ_API_URL && echo 'VITE_AIGENT_API_URL='$VITE_AIGENT_API_URL && npm install --legacy-peer-deps && cd apps/theqriptopian-web && npm run build"
  publish = "apps/theqriptopian-web/dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"
  # Force Node.js only build
  NODE_ENV = "production"
  # Disable all other language detection
  NETLIFY_USE_YARN = "false"
  NETLIFY_USE_PNPM = "false"
  # API URLs for thin client (injected at build time by Vite)
  VITE_AIGENTIQ_API_URL = "https://dev-beta.aigentz.me"
  VITE_AIGENT_API_URL = "https://dev-beta.aigentz.me"
  VITE_API_URL = "https://dev-beta.aigentz.me"

# Proxy /api/* requests to Amplify backend (prevents CORS issues and relative URL bugs)
[[redirects]]
  from = "/api/*"
  to = "https://dev-beta.aigentz.me/api/:splat"
  status = 200
  force = true

# SPA fallback (must be last)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
